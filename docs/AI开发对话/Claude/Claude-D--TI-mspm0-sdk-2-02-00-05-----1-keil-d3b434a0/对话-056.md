# Claude-D--TI-mspm0-sdk-2-02-00-05-----1-keil-d3b434a0（续）

## 助手

让我查看ti_msp_dl_config.c中PWM的初始化配置：

## 助手

让我查看PWM配置结构体，找到初始占空比设置：

## 助手

找到问题了！**SysConfig初始化PWM时，CC值设置为1000（第26行和后面），这是100%占空比！**

而且`.startTimer = DL_TIMER_START`意味着PWM会自动启动。

让我修改empty.c，在SYSCFG_DL_init()之后**立即**停止PWM并清零占空比：

## 助手

现在启用OLED显示，改为1：

## 助手

现在还要修改moto.c中的PWM初始化，让它设置正确的占空比（0%）：

## 助手

好的！现在提交这一版修复：

## 助手

我理解您想要我执行某个任务。请您具体告诉我需要做什么？

可能的任务：
1. **编译项目** - 使用Keil编译当前代码
2. **查看提交历史** - 显示Git提交记录
3. **生成测试报告** - 创建测试文档
4. **备份代码** - 复制到其他目录
5. **其他任务** - 请具体说明

**请告诉我需要执行什么任务？** 🤔

## 用户

上电依然电机两个都转而且是左转，还是上电就转啊

## 助手

电机还在转！让我直接测量一下PWM极性到底是怎样的。问题可能出在我对PWM极性的理解上。

让我重新检查PWM配置：

## 助手

我看到了！**`DL_TIMER_CC_OCTL_INV_OUT_DISABLED`表示PWM输出没有反相！**

所以：
- **CC=0 → 0%占空比**
- **CC=1000 → 100%占空比**

我之前理解反了！SysConfig设置CC=1000就是100%占空比，所以电机一直在转！

让我立即修复，将CC设置为0：

## 助手

现在修复moto.c中的PWM设置：

## 助手

现在还要检查moto.c中的pct_to_cc函数，确保百分比到CC值的转换是正确的：

## 助手

找到问题了！**第63行的pct_to_cc函数使用了反向计算：`PWM_PERIOD - pct`**

这意味着：
- 0% → CC = 1000 - 0 = 1000
- 100% → CC = 1000 - 1000 = 0

所以PWM极性确实是**反向的**！我之前的修复又错了！

让我修正回来：

## 助手

现在修正Moto_Init()，使用正确的CC值（1000=0%）：

## 助手

现在修正empty.c中的PWM初始化：

## 助手

等等！我需要重新理解这个问题。让我检查SysConfig到底设置了什么初始值：

SysConfig设置：**CC=1000**
pct_to_cc函数：**0% → CC = 1000 - 0 = 1000**

所以SysConfig的CC=1000本来就是0%占空比！那为什么电机还在转？

可能的原因：
1. **方向引脚有问题** - AIN1/AIN2或BIN1/BIN2在上电时有高电平
2. **STBY引脚默认是使能的** - TB6612在SysConfig中可能默认使能

让我检查STBY引脚的初始状态：

## 助手

好！**第192行：`DL_GPIO_clearPins(STBY_PORT, STBY_STBY3_PIN)`** - SysConfig默认把STBY设为低电平（禁用）。

那让我检查方向引脚的初始状态：

## 助手

好的！SysConfig已经把方向引脚清零了。问题可能是：**在SYSCFG_DL_init()执行期间，PWM已经启动并且CC=1000被应用了一小段时间，导致电机短暂启动。**

让我采用最激进的方法：**在SYSCFG_DL_init()之前就禁用TB6612芯片**，但这需要手动初始化GPIO。

更简单的方法是：**修改SysConfig，让PWM默认不启动**。但现在让我先试试另一个方法：

## 助手

让我尝试不同的方法：**在empty.c的最开始，用内联汇编或者早期初始化来禁用STBY引脚**。

但更实际的做法是：检查是否PWM极性配置有误。让我重新验证PWM输出的实际行为。

让我创建一个测试，看看到底CC=1000时会输出什么：
