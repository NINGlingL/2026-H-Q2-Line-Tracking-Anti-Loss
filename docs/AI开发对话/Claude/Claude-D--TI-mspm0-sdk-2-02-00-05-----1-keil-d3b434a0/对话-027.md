# Claude-D--TI-mspm0-sdk-2-02-00-05-----1-keil-d3b434a0（续）

## 助手

好的！现在我们有两个选择：

## 🔧 选项1：使用直接测试程序（推荐）

这个程序会**自动循环测试电机**，不依赖蓝牙，可以排除蓝牙通信问题。

### 步骤：
1. **将 `test_motor_direct.c` 添加到Keil项目**
2. **将 `empty.c` 从项目中排除**（右键→Options→勾选"Exclude from Build"）
3. **编译下载**
4. **观察电机**：应该每3秒自动前进→停止→后退→停止循环

---

## 🔧 选项2：检查当前程序状态

**请告诉我：**

### 发送蓝牙指令 `F` 后，OLED显示什么？

特别是这几行：
```
CMD: ?????          ← 显示什么指令？
M:? ??%             ← 方向和速度是什么？
Enc:????            ← 编码器数值
```

如果CMD显示了指令但M还是S 0%，说明**Control_Poll执行了但电机函数没被调用**。

---

## 🔍 可能的问题

### 1. TB6612的STBY引脚
**TB6612需要STBY引脚接高电平才能工作！**

请确认：
- **STBY引脚接到3.3V了吗？**
- 如果没接，电机永远不会转！

### 2. 电机电源
- **VM引脚接5V了吗？**
- **电源电流够吗？**（至少1A）

### 3. 电机本身
- **电机线接对了吗？**
- **用万用表测试PB0/PB1有PWM输出吗？**

---

**您想：**
1. 先用直接测试程序排查硬件？
2. 还是先告诉我当前OLED显示内容？

请选择！

## 用户

还有一个问题是OLED屏幕的右下角是乱码啊你调一下

## 助手

好的，OLED右下角乱码通常是显存越界导致的。让我检查并修复ssd1306.c：

## 助手

让我继续查看ShowString函数：

## 助手

我找到问题了！在 `_char` 函数中，边界检查不够严格。让我修复：

## 助手

现在修复字符写入时的边界检查：

## 助手

让我查看准确的代码：

## 助手

好的，让我修复边界检查，将 `col + 5 < SSD1306_WIDTH` 改为 `col + 5 <= SSD1306_WIDTH`：

## 助手

完美！现在重新编译测试：

## ✅ OLED乱码问题修复

### 修复内容：
1. ✅ 在 `_char()` 函数中添加了 `col` 边界检查
2. ✅ 修正边界条件：`col + 5 <= SSD1306_WIDTH`（原来是 `<`）
3. ✅ 防止写入超出128列的显存

---

## 🔨 现在请编译测试

### 方案A: 测试当前完整程序（empty.c）
```
1. 在Keil中按F7编译
2. 按F8下载
3. 观察OLED右下角是否还有乱码
4. 用蓝牙发送指令测试电机
```

### 方案B: 使用直接测试程序（推荐排查电机问题）
```
1. 在Keil项目中：
   - 将 test_motor_direct.c 添加到项目
   - 将 empty.c 排除（右键→Options→Exclude from Build）
2. 按F7编译
3. 按F8下载
4. 观察：电机会自动循环测试（前进3秒→停2秒→后退3秒→停2秒）
```

---

## ⚠️ 电机不转的硬件检查清单

在测试前，请务必确认：

### 1. TB6612的STBY引脚 ⚠️ 最重要
```
STBY必须接3.3V！
如果悬空或接地，电机永远不会转！
```

### 2. 电源连接
```
✅ VM (电机电源) → 5V，电流≥1A
✅ VCC (逻辑电源) → 3.3V
✅ GND → 公共地
```
