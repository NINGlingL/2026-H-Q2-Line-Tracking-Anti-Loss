# Claude-D--TI-mspm0-sdk-2-02-00-05-----1-keil-d3b434a0（续）

## 助手

好的！现在重新启用软件编码器：

## 助手

完美！现在重新编译测试：

## ✅ 所有修改完成

### 引脚配置正确：
```
电机控制（TB6612）：
- AIN1: PA14
- AIN2: PA15
- BIN1: PA12
- BIN2: PA13 ✅ 已修改

软件编码器（左轮）：
- E2A: PA17 ✅
- E2B: PA16 ✅

硬件编码器（右轮）：
- QEI: PB10/PB11 ✅

无冲突！
```

---

## 🔨 请重新编译测试

### 1. 编译下载
```
Keil按F7编译
0 Error(s), 0 Warning(s)
按F8下载
```

### 2. 测试结果应该正常了
```
✅ 上电后电机静止（不再一直转）
✅ 蓝牙指令正常控制电机
✅ 左轮编码器L有数值变化
✅ 右轮编码器R有数值变化
```

### 3. 测试功能
```
发送F → 前进，L和R都应该增加
发送B → 后退，L和R都应该减少
发送L → 左转，L减少R增加
发送R → 右转，L增加R减少
转动左轮 → L变化
转动右轮 → R变化
```

### 4. OLED显示
```
M:F S:50%
L:1234    ← 左轮编码器应该变化了
R:5678    ← 右轮编码器应该变化
```

**请编译测试并告诉我结果！** 🚀

## 用户

左编码器还是0不变啊，还有做循迹PID是不是需要两边轮子的速度

## 助手

您说得对！循迹PID确实需要双轮速度控制。但我们先解决左编码器为0的问题。

## 🔍 左编码器为0的原因分析

软件编码器依赖GPIO中断，可能原因：
1. **中断没有触发** - GPIO配置问题
2. **中断被触发但没进入处理函数** - 中断处理函数有问题
3. **编码器没有脉冲** - 硬件连接问题

让我添加调试代码验证中断是否工作：

## 助手

添加调试函数声明：

## 助手

现在修改显示函数，显示中断计数用于调试：

## 助手

完美！现在重新编译测试：

## ✅ 添加了调试功能

### 修改内容：
- 在第1页显示：`L:1234 I:5678`
  - **L**: 左轮编码器计数
  - **I**: GPIO中断触发次数（用于诊断）

---

## 🔨 请重新编译测试

### 1. 编译下载
```
Keil按F7编译
按F8下载
```

### 2. 观察OLED第1页
```
L:0 I:0     ← 初始状态
```

### 3. 手动转动左轮，观察：

**情况A：I计数增加，但L还是0**
```
L:0 I:123   ← 中断工作，但解码逻辑有问题
```
→ 说明GPIO中断触发了，但编码器解码算法不对

**情况B：I和L都是0**
```
L:0 I:0     ← 中断没触发
```
→ 说明GPIO中断根本没工作，可能是：
  1. 编码器没接或接错
  2. SysConfig中断配置不对
  3. 中断被禁用

**情况C：L和I都在增加**
```
L:123 I:456 ← 正常工作！
