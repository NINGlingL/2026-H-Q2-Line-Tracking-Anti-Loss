# Claude-D--A----32project-01-IDE-6284ffef（续）

## 助手

---

### 🥉 最简方案：pyOCD CLI（无需额外安装服务器）

你已经有 Python 的话，一行搞定：

```bash
pip install pyocd
pyocd pack install MSPM0G3507
```

然后我就能通过 Bash 直接调 pyOCD 命令烧录和读寄存器。不装任何 MCP 也能用，只是功能没有上面两个全。

---

## 建议你现在做的事

装 **embedded-debugger-mcp**，步骤：

```bash
# 1. 装 Rust（如果没装）
# https://rustup.rs  → 默认安装即可

# 2. 克隆 + 编译
git clone https://github.com/adancurusul/embedded-debugger-mcp.git
cd embedded-debugger-mcp
cargo build --release

# 3. 安装 Claude Code 插件
./target/release/embedded-debugger-mcp skill install --target claude

# 4. 验证 DAP 能不能识别
./target/release/embedded-debugger-mcp probes list
```

装完之后告诉我，我就能直接接管烧录调试了 — 你写需求，我写代码、编译、烧录、如果出 bug 就读寄存器排查，全流程不用你碰 Keil。

## 用户

你帮我装上吧，就第一个方案，放到全局可用下

## 助手

先装 Rust 工具链：

## 助手

Rust OK。现在克隆并编译 embedded-debugger-mcp：

## 助手

编译成功！现在安装到全局并配置 Claude Code 插件：

## 助手

DAP 调试器已识别！现在安装 Claude Code 技能插件：

## 助手

最后一步 — 配置 MCP 服务器让 Claude Code 能直接调用：

## 助手

`mcpServers` 需要放到 `.mcp.json` 而不是 `settings.json`：

## 助手

## ✅ 全部安装完成！

### 安装结果总览

| 步骤 | 状态 |
|------|------|
| Rust 工具链 | ✅ v1.97.1 |
| embedded-debugger-mcp 编译 | ✅ v0.3.0 |
| 全局安装 `~/.cargo/bin/` | ✅ 任意目录可用 |
| DAP 调试器识别 | ✅ `CMSIS-DAP_LU (C251:F001)` |
| Claude Code 技能 | ✅ `embedded-debugger` skill |
| MCP 服务器配置 | ✅ `.mcp.json` → `embedded-debugger` |

---

### 我现在能做的事

重启会话后，我就能通过 `embedded-debugger` MCP 直接：

| 操作 | 能力 |
|------|------|
| **烧录** | 一键烧录 `.elf` / `.hex` / `.bin` 到 MSPM0 |
| **擦除** | 全片擦除 |
| **读寄存器** | 读 CPU 寄存器 + 内存映射外设寄存器 |
| **写寄存器** | 修改寄存器值（调试用） |
| **读内存** | dump 任意地址内容到文件 |
| **断点** | 设硬件断点 + 单步执行 |
| **RTT** | 实时读 MCU printf 输出 |

---

### 下一步

下一轮对话中告诉我你的引脚分配，我直接：
1. 去 `UART` 项目里写好完整代码（电机+循迹+避障+OLED+蓝牙）
2. 在 Keil 里编译
3. 用 embedded-debugger 烧录到板子

全程你只要告诉我引脚怎么接，其余都不用管。

## 用户

现在你可以直接帮我写代码烧录查状态了吗

## 用户

Base directory for this skill: C:\Users\16484\.claude\skills\embedded-debugger

# Embedded Debugger

Use the local `embedded-debugger-mcp` binary as the source of truth. Prefer CLI
checks first, then MCP tools when an MCP client is available.

## Entry Decision

1. If the user has an MCP client configured, start or verify the server:
   `embedded-debugger-mcp serve`
2. If the user wants no MCP install, use CLI-first mode:
   `embedded-debugger-mcp doctor`, `embedded-debugger-mcp probes list`, and
   `embedded-debugger-mcp skill print-prompt`.
3. If hardware access is required, confirm the probe and target are connected
   before destructive actions such as flash erase or program.

## CLI Workflow

Run these in order and report the exact outcome:

```bash
