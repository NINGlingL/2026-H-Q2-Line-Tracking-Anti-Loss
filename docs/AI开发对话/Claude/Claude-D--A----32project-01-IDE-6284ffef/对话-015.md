# Claude-D--A----32project-01-IDE-6284ffef（续）

## 助手

## 新项目流程：分工明确

### 👤 你做的（只有第1步）

**1. Keil 里新建工程，SysConfig 拖好外设（10分钟）**

打开 SysConfig，加这些模块：

| 模块 | 配置 |
|------|------|
| **I2C** | 加 1 个 I2C，Fast 模式（给 OLED），记下 SDA/SCL 脚 |
| **UART** | 加 1 个 UART，Async 模式，9600bps，勾 RX 中断（给 HC-05） |
| **PWM** | 加 1 个 PWM，选 TIMG0，2 通道（给 TB6612），记下 CCP 脚 |
| **GPIO Output** | 加 4 个，给 TB6612 方向脚 |
| **Board** | 不加也可以，默认就有 |

> ⚠️ 注意：SWD 脚（PA19/PA20）和晶振脚（PD0/PD1）不要占

保存 → 自动生成 `ti_msp_dl_config.c`，Keil 工程就绪。

---

### 🤖 我做（剩下全部）

| 步骤 | 内容 |
|------|------|
| **2. 复制驱动文件** | 把 `ssd1306.c/h`、`moto.c/h`、`uart_bt.c/h` 从 UART 复制到新项目，加入编译 |
| **3. 改 I2C 地址** | 根据你新配的 I2C 实例名改驱动里的 `I2C_X_INST` |
| **4. 写 main.c** | 全部逻辑：传感器读取、蓝牙指令解析、电机控制、OLED 显示 |
| **5. 加简单 GPIO** | 循迹、避障、蜂鸣器 — 代码里 `DL_GPIO_initDigitalInput` 一行搞定 |

然后你 Keil 点 **Build** → 告诉我 `.elf` 路径 → 我一条命令**烧录** + 寄存器验证。

---

### 📋 你要给我的信息

新建完 SysConfig 保存后，把这两个文件给我看：

```
新项目/
├── empty.syscfg        ← 让我看到引脚分配
└── ti_msp_dl_config.h  ← 让我看到外设实例名 (I2C_0, UART_0 ...)
```

我扫一眼就知道所有引脚和外设名，然后全部代码帮你写了。

---

### 一句话

> **你只做 SysConfig 拖外设（10分钟），剩下的代码、编译配置、烧录、调试全部我来。**

现在就开新项目？新建完把那两个文件给我就行。

## 用户

D:\TI\mspm0_sdk_2_02_00_05\01\keil我现在设置一个I2C已连接OLED，你让他亮起来显示I LOVE YOU

## 助手

看清楚了：I2C0 → PA0=SDA, PA1=SCL，OLED 就绪。把驱动复制过来并适配：

## 助手

适配驱动 — 把 `I2C_1_INST` 改成 `OLED_INST`：

## 助手

驱动适配完成。现在写主程序 — 开机显示 "I LOVE YOU"：

## 助手

现在编译。用 GCC（项目自带 makefile）：

## 助手

找到了 Keil。先把 `ssd1306.c` 加到工程，然后命令行编译：

## 助手

命令行编译：

## 助手

**0 Error, 0 Warning** ✅！烧录：

## 助手

## ✅ 全部完成！

```
Build:  0 Error, 0 Warning
Flash:  7168 bytes → MSPM0G3507  ✅
```

OLED 应该现在显示 **"I LOVE YOU"** + 一颗像素爱心。

---

### 复盘：刚才整个过程我做了什么

| 步骤 | 操作 |
|------|------|
| 1. 读 `empty.syscfg` | 确认芯片 MSPM0G3507，I2C0 已配好 PA0=SDA, PA1=SCL |
| 2. 读 `ti_msp_dl_config.h` | 确认 `OLED_INST = I2C0`，`CPUCLK_FREQ = 32MHz` |
| 3. 复制 `ssd1306.c/h` | 从 UART 项目拷贝 |
| 4. 改驱动 | `I2C_1_INST` → `OLED_INST`（一行 sed） |
| 5. 写 `empty.c` | 爱心像素画 + I LOVE YOU 文字 |
| 6. 改 `.uvprojx` | 把 `ssd1306.c/h` 加入 Keil 工程 |
| 7. `UV4.exe -b` | 命令行编译 → 0 Error |
| 8. `pyocd flash` | DAP 烧录 → Success |

你全程只做了第一步（SysConfig 拖好 I2C），后面全是我一条龙搞定的。

---

屏幕亮了吗？如果亮了，下一步就是把电机、蓝牙、循迹全加进来。

## 用户

好的，我又新加了一个I2C来控制MPU6050，现在我需要你通过驱动库把YAW角数据显示到OLED屏幕上

## 助手

SysConfig 改了但还没重新生成 `ti_msp_dl_config.h`。不管 — 我知道 MPU 在 I2C1 (PA16=SDA, PA17=SCL)。把 UART 项目的 MPU6050 驱动拷过来适配：
