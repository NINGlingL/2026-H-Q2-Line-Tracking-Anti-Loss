# Claude-C--Users-16484-Desktop-967b32d1（续）

## 助手

解压完成。嵌套了一层，整理一下：

## 助手

装好了。路径：`D:\工具\ClicknTranslate\`

直接双击 `ClicknTranslate.exe` 就能运行，启动后缩到系统托盘：

| 快捷键 | 功能 |
|--------|------|
| `Ctrl+Alt+T` | 框选屏幕区域 → OCR + 翻译弹窗 |
| `Ctrl+Alt+C` | 框选屏幕区域 → OCR 复制到剪贴板 |

和 Snipaste 配合：Snipaste 截图贴图 → `Ctrl+Alt+T` 框选贴图区域 → 翻译。

## 用户

删掉吧，很不好用，你再找找star高的翻译软件呢

## 助手

已删。按 Star 数排，最靠谱的是 **Pot**：

| 排名 | 工具 | Stars | 特点 |
|------|------|------|------|
| ⭐1 | **[Pot](https://github.com/pot-app/pot-desktop)** | 13.5K | 划词翻译 + 截图OCR翻译 + 输入翻译 + 剪贴板监听，插件系统 |
| 2 | **[eSearch](https://github.com/xushengfeng/eSearch)** | ~10K | 截图+OCR+翻译+贴图+录屏，功能最全但偏重 |
| 3 | **[STranslate](https://github.com/ZGGSONG/STranslate)** | 7.4K | WPF 原生 Windows，划词+OCR，轻量 |

---

**Pot 最匹配你的需求**：
- `Ctrl+Alt+Q` 划词翻译（选中 Snipaste 贴图上的文字 → 弹窗翻译）
- 支持 DeepL / OpenAI / Google / 百度 / 有道，可切换
- 一条命令安装：`winget install Pylogmon.pot`

要不要装 Pot？

## 用户

试试吧，把它装在D盘工具文件夹下

## 助手

代理又不稳定。重试：

## 助手

GitHub 下载一直断。你代理开着吗？或者你自己下载更快：

1. 打开：https://github.com/pot-app/pot-desktop/releases
2. 下载 `pot_3.0.7_x64-setup.exe`
3. 安装时选择路径 `D:\工具\Pot`
4. 完成后双击运行

或者我这边用 winget 直接装（不走 GitHub）：

```
winget install Pylogmon.pot
```

winget 可能装到 C 盘默认位置。你要哪个方式？

## 用户

我给你写的永久记忆区文件和skill是只有你这个对话中才能使用吗，为什么我在别的对话中还是默认？

## 助手

好问题。两个东西的作用范围不同：

---

## 当前状态

### Memory（记忆文件）

位置：`~/.claude/projects/C--Users-16484-Desktop/memory/`

**这是按项目隔离的。** 项目名 `C--Users-16484-Desktop` 意味着它只在你工作目录是 `C:\Users\16484\Desktop` 时才会加载。你在别的目录开 Claude Code，这些记忆就不会出现。

### Skill

位置：`~/.claude/skills/`

**这是全局的。** 从你上面那条系统提示可以看到——`my-note` 已经在别的对话中出现了。Skill 在任何对话中都生效。

---

## 为什么"别的对话还是默认"

根本原因是**记忆文件只在 Desktop 目录下加载**，换个目录就没了。

解决办法——把记忆键到全局：

## 助手

只有 Desktop 这一份记忆。问题清楚了：

---

## 原因

```
~/.claude/projects/C--Users-16484-Desktop/memory/     ← 只在 Desktop 目录下加载
~/.claude/projects/C--Users-16484-别的项目/memory/    ← 不存在！
```

你在哪个目录开 Claude Code，它就找对应项目的记忆。其他项目没有记忆文件，所以"还是默认"。

---

## 解决办法

把用户偏好写进 `C:\Users\16484\CLAUDE.md`——这个文件**全局生效，任何目录都加载**：

## 助手

改好了。现在 `C:\Users\16484\CLAUDE.md` 的结构：

```
┌─────────────────────────────┐
│ 用户偏好（全局）              │ ← 新加的，任何对话都加载
│   - 中文、耐心、严谨          │
│   - 第一性原理思考            │
├─────────────────────────────┤
│ 嵌入式开发规范                │ ← 原来的
│   - STM32 / MSPM0 规则       │
│   - 调试四步法、禁止事项      │
└─────────────────────────────┘
```

**之前**：记忆文件只在 Desktop 目录生效，别的目录加载不到。

**之后**：`CLAUDE.md` 在 `C:\Users\16484\`（家目录），Claude Code 在任何目录启动都会先读这个文件。别的对话里也会自动有中文回复、耐心解释、严谨客观这些设定。
