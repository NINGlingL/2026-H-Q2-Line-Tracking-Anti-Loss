# Claude-D--A--------------97e992e1（续）

## 助手

PB1(PWM) + PB8/PB9(DIR)   → TB6612 右电机

PB10(STEP) + PB11(DIR) + PA14(EN) → TMC2208 步进电机

PB22/PB23 → 硬件编码器(左轮)
PA16      → 软件编码器(右轮, EXTI中断)
```

PDF 里每个引脚都标注了在 Arduino 排针上的位置，方便你插杜邦线。你看看有没有问题？

## 用户

D:\A嵌入式\资料库\3507\立创·天猛星MSPM0G3507开发板资料-241115下主控板资料在这个文件夹下了，你再检查一下，如果符合那你就生成pdf文件并且告诉我

## 助手

让我看官方 Keil 例程里是怎么配引脚的，交叉验证：

## 助手

官方例程确认：**PB22=LED, PB21=按键**。这俩也被板子占了，得调整编码器引脚。让我最终确认并重新生成：

## 助手

---

## ✅ PDF 已生成！

桌面文件：**`H_MSPM0G3507_PinMap_Final.pdf`**（6 页，169KB）

---

## 🔍 交叉验证结果

对比了三个来源确认无误：

| 验证来源 | 关键发现 |
|----------|---------|
| **原理图** | PA0/PA1=I2C0, PB2/PB3=I2C1, PA10/PA11=CH340 |
| **官方Keil LED例程** | PB22=板载LED, PB21=板载按键 |
| **官方Keil 按键例程** | 同LED例程, PB21=按键(PULL_UP), PB22=LED |

---

## 📋 最终引脚分配速查

```
【已占用·不可用】
  UART0: PA10/PA11 → 板载CH340 (USB-C)
  SWD:   PA19/PA20 → 调试接口
  LED:   PB22      → 板载LED
  KEY:   PB21      → 板载按键
  BSL:   PA18      → BSL按钮

【UART·3路可用】
  UART1: PA8/PA9      → 相机 (921600)
  UART2: PB15/PB16    → HC-05蓝牙 (115200)
  UART3: PB12/PB13    → 8路循迹 (115200)

【I2C·2路】
  I2C0: PA0(SDA) + PA1(SCL)  → SSD1306 OLED
  I2C1: PB2(SCL) + PB3(SDA)  → ICM-20948

【TB6612 直流电机】
  左轮: PB0(PWM) + PA12(IN1) + PA13(IN2)
  右轮: PB1(PWM) + PA14(IN1) + PA15(IN2)

【TMC2208 步进电机(摆杆)】
  STEP: PB10  DIR: PB11  EN: PB14

【编码器】
  硬件(左轮): PB8(A相) + PB9(B相) → TIMG6
  软件(右轮): PA16 → EXTI中断计数
```

PDF 里每个引脚都标注了在 40P Arduino 排针上的具体位置和丝印，直接对着插杜邦线就行。

## 用户

软件编码器不应该有两个引脚？还有我的使能连哪个引脚？

## 助手

你说得对，两处都要修正：

1. **软件编码器**：即使是软件计数，正交编码器也需要 A+B 两路才能判方向。只用一路分不清正反转。
2. **使能引脚**：TMC2208 EN 我给了 PB14，但没说清楚。

修正后再生成：

## 助手

同时更新排针表里 PA16/PA17 的标注，以及补上使能引脚说明：

## 助手

加上使能引脚的专项说明：

## 助手

等一下，我重新理清使能分配——TB6612 也需要 STBY 引脚。让我更新 TMC2208 和左右排针表：

## 助手

更新系统框图里的编码器和使能信号：

## 助手

字符编码问题导致匹配失败。直接重新生成整个 PDF：

## 助手

PDF 文件被打开了锁住了。最新的已经生成好了：

---

## ✅ 桌面文件：`H_MSPM0G3507_PinMap_v3.pdf`（7 页）

旧文件 `PinMap_Final.pdf` 被你打开了所以没法覆盖，关掉后删了就行。

---

## 🔧 v3 修正的两处

### 1. 软件编码器 → 两个引脚

你说得对，即使是软件方式，正交编码器必须 **A+B 两相**才能判断方向：

```
右轮软件编码器:
  PA16 → A相 (EXTI 双沿中断)
  PA17 → B相 (EXTI 双沿中断)
```

A 相比 B 相超前 90° = 正转，滞后 90° = 反转，在两个中断里判断相位差即可。
