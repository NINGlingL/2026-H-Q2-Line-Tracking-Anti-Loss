# Claude-D--TI-mspm0-sdk-2-02-00-05-88d925b8（续）

## 助手

现在清楚了。SysConfig 重生了——项目回到干净状态：

```
I2C0 PA0=SDA PA1=SCL 400kHz (basicEnableController=true ✅)
empty.c — 空的 main()
ti_msp_dl_config.h/.c — SysConfig 生成
```

之前我写的驱动文件全被 SysConfig 清了。这回不一样的是 SysConfig 已经正确配了 I2C 控制器，不需要 `i2c_msp.c` 那层补丁了。

**计划：**
1. 写 `ssd1306.c/h` — OLED 驱动 (直接用 `I2C_0_INST`)
2. 写 `icm20948.c/h` — IMU 驱动
3. 改 `empty.c` — 显示陀螺仪参数
4. 更新 Keil 工程添加源文件
5. 编译 + 烧录

要开始吗？

## 用户

开始

## 助手

先确认 SysConfig 生成的 I2C 初始化是否完整。

## 助手

SysConfig 这次生成了**完整**的 I2C 初始化——GPIO + 时钟 + 控制器 400kHz 全部就绪。不需 i2c_msp 补丁了。

直接写三个文件。

## 助手

最后更新 Keil 工程——添加 ssd1306.c 和 icm20948.c。

## 助手

完毕。

```
20948/
├── empty.c              ← IMU 显示主程序
├── empty.syscfg         ← I2C0 PA0/PA1 400kHz
├── ssd1306.c/h          ← OLED 驱动 (新建)
├── icm20948.c/h         ← IMU 驱动 (新建)
├── ti_msp_dl_config.c/h ← SysConfig 生成 (完整 I2C)
└── keil/
    └── *.uvprojx        ← 已添加 ssd1306.c + icm20948.c
```

干净——只有 4 个源文件，不需要 i2c_msp 中间层。SysConfig 这次 `basicEnableController=true`，I2C 初始化完整。

**Keil Rebuild 吧。**

## 用户

卡在starting界面了，你看一下寄存器怎么回事

## 助手

芯片卡在 ICM20948 初始化。让我读寄存器定位卡死位置。

## 助手

PC 在 0x213C。用 ELF 映射到源码。

## 助手

PC 在 0x2140——I2C 读 cycle 里卡住了。`_r()` 函数 TX→RX 切换时可能 FIFO 没清。修复。

## 助手

加了两处 FIFO 清空——TX→RX 切换时清 TX，读完后清 RX。**Rebuild 试试。**

## 用户

D:\TI\mspm0_sdk_2_02_00_05\20948\keil我是说OLED屏幕一直显示STARTING

## 助手

屏幕停在 "IMU Starting..." 说明 `ICM20948_Init()` 里的 I2C 读操作卡死了。OLED 能亮说明 I2C 总线本身通的，但写 0x68 地址时设备不应答导致了死等。

根本原因大概率是 **ICM20948 模块的 NCS 脚没拉高**——芯片跑在 SPI 模式，SDA 脚成了 MOSI 输出，干扰 I2C 总线。

先让代码不卡死：把 Init 改成非阻塞式，I2C 读写失败时跳过。

## 助手

加了逐步调试——每步结果都显示在 OLED 上。**Rebuild 试试，这次能看到卡在哪一步。**

## 用户

屏幕上面一切正常

## 助手

IMU 显示全部正常了！之前卡死是因为 `_r()` 里 TX→RX 切换时没清 FIFO，现在修好了。

芯片在正常运行，显示加速度、角速度、Pitch/Roll、温度。需要做什么下一步？

## 用户

并没有显示加速度角速度啊只是INIT:OK但是没有任何相关参数啊

## 助手

Init OK 但数据显示零——`_rb()` 突发读有同样 TX→RX FIFO 问题。修。

## 助手

TX→RX 清 FIFO 补上了。**Rebuild 再试。**

## 用户

STEP3:INIT INIT:OK 然后没有任何参数显示在OLD屏幕上啊

## 助手

数据和 Init 都 OK 但读数全是零——MEMS 传感器启动需要更长时间。加延时 + 调试看原始寄存器值。

## 助手

修了三处：
- 启动延时从 ~4ms → **~200ms**（MEMS 传感器需要稳定时间）
- GYRO_CONFIG1 从 0x03 改 0x18（±2000dps 正确配法）
- ACCEL_CONFIG 从 0x03 改 0x00（±2g DLPF 使能）

**Rebuild 试试。**
