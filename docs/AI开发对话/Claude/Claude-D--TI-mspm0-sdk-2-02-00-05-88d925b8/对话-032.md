# Claude-D--TI-mspm0-sdk-2-02-00-05-88d925b8（续）

## 用户

不是你先告诉我OLED显示的预想情况是什么

## 助手

按代码流程，OLED 依次显示：

**Step 1 (500ms):**
```
Step1: OLED OK
```

**Step 2:**
```
Step1: OLED OK
Step2: WhoAmI...
WHO_AM_I = 0xEA
Chip: ICM20948 OK
```

**Step 3:**
```
Step1: OLED OK
Step2: WhoAmI...
WHO_AM_I = 0xEA
Chip: ICM20948 OK
Step3: Init...
Init: OK
```

**然后进入主循环（200ms 刷新）：**
```
AX+0.01  AY-0.03
AZ+9.81  GX+0.1
GY-0.2   GZ+0.0
|A|9.81g  |G|0.2dps
P+0.1     R-0.2
#123     T21.0
I2C0 PA0/PA1  400kHz
```

---

你现在实际看到的是：Step1-3 都 OK，然后**主循环里的 AX/AY/AZ/GX/GY/GZ 全显示 0.00**？还是主循环根本没进去（屏幕一直停在 Init:OK 不动）？

## 用户

更本就没进去啊

## 助手

Init:OK 但主循环没进去——可能是 `math.h` 浮点库没链接，`sqrtf`/`atan2f` 触发 HardFault。我先把数学运算去掉，确认主循环能跑。

## 助手

去掉了 `sqrtf`、`atan2f` 和 `snprintf`——如果之前是浮点库导致 HardFault，现在应该能进主循环了。**Rebuild 试试。**

## 用户

我是0,96寸OLED，并且现在是卡在ENTER MAIN LOOP...然后还是进不了主循环啊

## 助手

卡在 `ICM20948_Read` 里的突发读。先改成单字节读定位问题。

## 助手

加了逐步 I2C 读测试。OLED 会显示：
- 单字节读 ACCEL_XOUT_H 结果
- 12 字节突发读结果

**Rebuild 试试。精确看到底哪一步卡住。**

## 用户

TEST READ 1BYTE ACCEL_XH = 0XF7 TESTREAD 12BYTES 没了，卡在这里

## 助手

根因找到！MSPM0 I2C RX FIFO 只有 8 字节深，读 12 字节会死锁。分两次读，每次 6 字节。

## 助手

恢复主循环显示 IMU 数据。

## 助手

去掉了 `math.h`，用平方和代替开方。**Rebuild 试试。**

## 用户

可以显示，但是是原始数据啊，你参考什么卡尔曼滤波，四元数，把陀螺仪的驱动库完善一下

## 助手

数据读出来了，现在加姿态解算——用 Mahony 互补滤波器（比卡尔曼简单，Cortex-M0+ 跑得动）。

## 助手

现在更新主程序用滤波器，并加入 Keil 工程。

## 助手

加 `imu_filter.c` 到 Keil 工程。

## 助手

禁 SysConfig 预编译，不然又覆盖。

## 助手

√ Math (`sqrtf`/`atan2f`/`asinf`) 已回，这次没问题——之前卡死是 I2C FIFO 不是数学库。

OLED 显示效果：
```
R+45 P-10 Y+23      ← 欧拉角 Roll/Pitch/Yaw (°)
GX+0.1     GY-0.2   ← 陀螺原始值 (°/s)
AX+0.01    AY-0.03  ← 加速度 (g)
Q 0.92 0.12 -0.08 0.35  ← 四元数
Mahony Kp=2.0 Ki=0.1
```

**Rebuild。**

## 用户

我和八路有MCU之间的串口通信波特率是多少？
