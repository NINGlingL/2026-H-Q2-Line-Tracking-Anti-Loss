# Claude-D--TI-mspm0-sdk-2-02-00-05-88d925b8（续）

## 用户


## Prompt Reference

For a reusable CLI+Skill prompt, read
`references/default-prompt.md`.


ARGUMENTS: 报错了你查一下状态

## 助手

API Error: Unable to connect to API (ConnectionRefused)

## 用户

继续

## 助手

**0 Error(s), 0 Warning(s)** ✅ 编译通过！现在烧录。

## 助手

探针就绪。用 MCP 工具烧录。

## 助手

MSPM0 不是 STM32，stm32 工具不适用。用 embedded-debugger CLI 烧录。

## 助手

Keil 自带下载功能最快。**在 Keil 里按 F8 (Download)** 就能烧录——工程里已经配好了 CMSIS-DAP 调试器。

或者试试直接用 embedded-debugger：

## 助手

CLI 没 flash 命令。两种方式烧录：

**方式 1（最简单）：Keil 里按 F8 (Download)**  
工程已配好 CMSIS-DAP，直接下载。

**方式 2：我用 MCP 服务模式**
你要哪种？

## 用户

方式2

## 助手

MCP 服务不方便后台跑。让我试试直接用 probe-rs 命令行烧录。

## 助手

MCP 管道断了。让我看 stderr 出了什么错。

## 助手

MCP 管道在 Windows 下不稳定。让我换条路——Keil 自带命令行烧录工具。

## 助手

`pyOCD` 已安装！用它直接烧录。

## 助手

找到探针。直接烧录！

## 助手

**烧录成功！** 🎉 芯片正在运行，核心未停止说明代码在执行。

```
Flash: 15496 bytes @ 18.20 kB/s ✅
Reset: OK ✅
Status: Core running ✅
```

---

**现在看 OLED 屏幕**，应该显示：

```
ICM20948  OK  #1
AX+0.xx  AY+0.xx
AZ+9.8x  GX+0.x
GY+0.x   GZ+0.x
|A| 9.8x  |G| 0.x
Pitch +0.x Roll +0.x
Temp: 2x.x C
SDA=PA0 SCL=PA1 OK
```

如果显示 "ICM20948 NOT FOUND!"，检查 ICM20948 模块的 VCC/GND/SDA/SCL 接线（PA0=SDA, PA1=SCL, 地址 AD0=GND）。

屏幕上看到什么？

## 用户

屏幕不亮

## 助手

屏幕不亮——检查 I2C 初始化链。可能是 SysConfig 生成的 init 函数和 ssd1306 的 I2C 初始化冲突了。

## 助手

找到根因！SysConfig 生成的 I2C init 缺了**控制器模式启用**——没开时钟也没配波特率。补上。

## 助手

根因：SysConfig 生成的 `SYSCFG_DL_I2C_0_init()` 只配了 GPIO+时钟，**没开 I2C 控制器**（没设波特率、没使能）。`I2C_MSP_Init()` 补上了。

**Rebuild → 然后我重新烧录。**

## 用户

<task-notification>
<task-id>bha3vh5as</task-id>
<tool-use-id>call_00_cVUND0ebmZeg1kWSHOsZ9773</tool-use-id>
<output-file>C:\Users\16484\AppData\Local\Temp\claude\D--TI-mspm0-sdk-2-02-00-05\249be77f-33be-403d-8e1e-17f72272c56c\tasks\bha3vh5as.output</output-file>
<status>completed</status>
<summary>Background command "Find MSPM0 CMSIS pack for pyOCD" completed (exit code 0)</summary>
</task-notification>
