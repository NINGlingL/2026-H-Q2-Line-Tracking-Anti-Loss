# Claude-D--TI-mspm0-sdk-2-02-00-05-88d925b8（续）

## 用户

| 闭环控制器 | PID 控制器、LQR 控制器、ADRC 控制器 |
| 储能电源 | 锂电池 (2S/3S/4S)、超级电容、镍氢电池组 |
| 通信总线 | I2C、SPI、UART、CAN |
| 主控 | STM32、ESP32、GD32、ATmega |
| 调试接口 | SWD (ARM Cortex)、JTAG、ISP |
| 非易失存储器 | 内部 Flash、外部 EEPROM (AT24C02)、FRAM |
| 反馈异常 | 编码器空转（车轮打滑）、传感器失准、信号干扰 |
| 执行器驱动 | 电机驱动 IC (TB6612、DRV8833、L298N)、MOSFET H 桥 |

## 附录 B：规则冲突裁决速查表

| 场景 | 规则 A | 规则 B | 裁决 | 理由 |
|------|--------|--------|:----:|------|
| 急停 vs 日志 | P0-5 急停不调函数 | P2-2 记录错误日志 | **P0-5** | 人身安全 > 诊断 |
| 限幅 vs 优化 | P0-1 硬编码限幅 | P2-5 减少分支指令 | **P0-1** | 硬件安全 > 执行效率 |
| 断电 vs 保存 | P0-2 低电禁止使能 | P2-1 参数持久化保存 | **P0-2** | 电池保护 > 参数保存 |
| 停止 vs 完成 | P1-4 超时→安全状态 | 业务逻辑: 超时→STOP | **P1-4** | 安全状态 ≠ 业务 STOP，以安全为准 |
| 重试 vs 复位 | P1-3 先重试 3 次 | P1-3 超时后硬件复位 | **不冲突** | 同一 SOP 的顺序步骤 |
| 降额 vs 性能 | P1-6 前馈增益 ≤ 1.5 | 竞速需求: 最大功率输出 | **P1-6** | 1.5× 已是上限，超过则储能崩溃 |

重要: 此表仅列已知冲突。遇到表中未列出的冲突时，严格按 P0 > P1 > P2 裁决，同级时选择更保守(更安全)的规则。


ARGUMENTS: 审查目录 D:\TI\mspm0_sdk_2_02_00_05\通用驱动库\ 下的所有驱动代码，目标芯片是 天猛星 MSPM0 G3507。需要检查：
1. 是否所有引用的外设（TIMG0, I2C0, I2C1, UART2 等）都在 G3507 上存在
2. 引脚分配是否与 G3507 兼容（PA12/PA13 PWM, PB15/PB16/PB2/PB3 电机控制, PA0/PA1 I2C, PA21/PA22 UART2）
3. 是否有危险的硬件配置（如未初始化的引脚浮空、短路风险、缺少上下拉等）
4. 中断处理是否正确（UART RX 中断）
5. 时钟和电源配置是否安全
6. 代码是否有逻辑错误或潜在的硬件损坏风险

## 助手

好的，skill 已加载。让我先确认 G3507 的外设能力，然后系统审查所有文件。

## 助手

现在我已经收集了足够的信息。让我也确认一下 G3507 上 SWD 调试口的位置和 I2C1 的配置。

## 助手

数据齐全，现在开始全面审查。先更新任务状态。

## 助手

---

# 🔒 通用驱动库 硬件安全审查报告

**目标芯片**: 天猛星 MSPM0 G3507 (CONFIG_MSPM0G3507)
**审查日期**: 2026-07-28
**审查范围**: `通用驱动库\` 下 4 个模块共 8 个文件

---

## ✅ 外设兼容性验证

| 外设 | 驱动库使用 | G3507 可用 | 状态 |
|------|-----------|-----------|:--:|
| TIMG0 | PWM_0_INST (PWM 电机) | ✅ TIMG0_BASE 存在 | ✅ |
| I2C0 | 可用于 OLED | ✅ I2C0_BASE 存在 | ✅ |
| I2C1 | SSD1306 使用 `I2C_1_INST` | ✅ I2C1_BASE 存在 | ✅ |
| UART2 | UART_0_INST (HC-05) | ✅ UART2_BASE 存在 | ✅ |
| GPIOA/B | 方向/通信引脚 | ✅ 两个 GPIO 端口 | ✅ |
| SWD 接口 | PA19(SWDIO)/PA20(SWCLK) | 未被占用 | ✅ |

---

## 🔴 P0 级别 — 必须修复

### P0-2 ❌ 储能电源监测缺失

**文件**: `moto.c`, 全局架构
**问题**: 整个驱动库完全没有电池电压 ADC 监测。MSPM0 G3507 内部有 ADC0/ADC1，可用于读取电池分压。当前电机控制没有任何低压保护——电池电压过低时，电机仍会尝试全功率运行，可能导致：
- 锂电池过放永久损坏
- TB6612 在欠压状态下行为不可预测
- 系统在主控低压复位前可能失控

**修复建议**:
```c
// moto.c 增加:
#define BATT_ADC_CHANNEL  DL_ADC12_CHANNEL_0  // 根据实际引脚调整
#define BATT_LOW_THRESHOLD_MV  3300  // 2S 锂电池低压阈值
#define BATT_CRIT_THRESHOLD_MV 3000

static void Moto_CheckBattery(void)
{
    uint16_t batt_mv = ADC_ReadBattery();
    if (batt_mv < BATT_CRIT_THRESHOLD_MV) {
        Moto_Stop();  // 紧急断电
        // 进入不可自恢复报警状态
    }
}
```

---

### P0-4 ❌ 独立看门狗缺失

**文件**: 全局
**问题**: 没有任何看门狗配置代码。G3507 有 WWDT0/WWDT1 两个窗口看门狗。如果主循环因 I2C 挂死等原因卡住，电机将持续以最后设置的 PWM 运行，小车失控。

**修复建议**: 在 `main()` 中初始化 WWDT0，使用 LFOSC (32kHz 独立时钟)，主循环末尾喂狗，ISR 内严禁喂狗。

---

### P0-3 ⚠️ TB6612 STBY 硬连线到 3.3V

**文件**: `moto.h` 第 8 行注释
```c
// STBY 接 3.3V (使能)
```
**问题**: STBY 引脚硬连线，软件无法控制电机使能/禁用。如果使用一个 GPIO 接 STBY，急停时可以直接拉低 STBY 禁用所有电机。

**修复建议**: 将 STBY 接到一个 GPIO（如 PB4），在 `Moto_Init()` 中拉高，`Moto_Stop()` 中也拉低。

---

## 🟡 P1 级别 — 强烈建议修复

### P1-3 ❌ I2C 无超时 — 会导致主循环永久卡死

**文件**: `ssd1306.c` 第 112-121 行
```c
static void I2C_WriteBytes(uint8_t *data, uint8_t len)
{
    DL_I2C_fillControllerTXFIFO(I2C_1_INST, data, len);
    while (!(DL_I2C_getControllerStatus(I2C_1_INST) & DL_I2C_CONTROLLER_STATUS_IDLE));
    //  ↑ 如果 I2C 总线被拉低（OLED 松脱/短路），这里永远不返回！
    DL_I2C_startControllerTransfer(I2C_1_INST, SSD1306_I2C_ADDR, ...);
    while (DL_I2C_getControllerStatus(I2C_1_INST) & DL_I2C_CONTROLLER_STATUS_BUSY_BUS);
    //  ↑ 同理，总线卡死时永久阻塞
    while (!(DL_I2C_getControllerStatus(I2C_1_INST) & DL_I2C_CONTROLLER_STATUS_IDLE));
}
```
