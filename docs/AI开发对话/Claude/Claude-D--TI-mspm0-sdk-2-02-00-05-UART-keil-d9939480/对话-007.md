# Claude-D--TI-mspm0-sdk-2-02-00-05-UART-keil-d9939480（续）

## 助手

改好了。现在 OLED 刷新是主循环**第一件事**——后面蓝牙和 MPU6050 即使卡住，屏幕也已经更新了。

编译测试。旋转传感器，看 YAW 值变不变。

## 用户

YAW值为0晃动不变

## 助手

YAW 不动说明 `g_tick_ms` 可能没在走——`ComplementaryFilterUpdate` 里 `elapsed==0` 就直接 return 了。加一行显示 tick 来验证：

## 助手

编译下载。看 OLED 第 1 行 `T:XXXXX` 的值：

- **数字在涨** → SysTick 正常，问题在 MPU6050 的 I2C 读取
- **一直是 0 或不变** → SysTick 没触发，`elapsed==0` 导致滤波函数直接返回

## 用户

一直在涨，YAW晃动仍然不变

## 助手

Tick 在涨说明 SysTick 正常。直接测试 I2C_1 在**主循环里**能不能读到数据——每轮显示 WHO_AM_I：

## 助手

编译下载。OLED 第 0 行会显示 `WHO:0xXX Y:0`。

- **`WHO:0x68`** → I2C 通信正常，问题在 `ComplementaryFilterUpdate` 内部
- **`WHO:0xFF`** → I2C 在主循环中读失败了（超时），校准时的短时读写和主循环的间隔读写有差异

告诉我 WHO 值是多少。

## 用户

0X68 Y不变

## 助手

WHO_AM_I 能读到说明单字节 I2C 正常。问题可能在多字节 `ReadRegs`。直接显示原始 gyro_z 数据确认：

## 助手

Old duplicate line 1 removal:

## 助手

编译下载。现在 OLED 显示：

| 行 | 内容 | 说明 |
|:-:|------|------|
| 0 | `W:0x68 GZ:+1234` | WHO + 原始陀螺仪 Z 值 |
| 1 | `Y:0.0 T:12345 F50%` | YAW + Tick + 方向速度 |

**晃动传感器，看 `GZ` 值变不变：**

- **GZ 在 ±几百到几千之间跳** → 多字节 I2C 读取正常，问题在互补滤波内部
- **GZ 始终是 +0 或固定值不变** → 多字节 `ReadRegs` 失败，len=6 的读取有问题

## 用户

GZ开机固定值不变W OX68 Y始终为0

## 助手

`GZ` 不变说明多字节 `ReadRegs` 在**主循环里超时了**。校准时没有 SysTick/UART 中断干扰所以能过，主循环有中断抢 CPU 导致 5000 次循环不够。**把所有超时放大 20 倍**：

## 助手

这就是问题！新驱动的 `while (!BUSY)` 和 SSD1306 的 `while (BUSY)` **逻辑相反**。改成 SSD1306 验证过的模式：

## 助手

同样修复 `ReadRegs`：

## 助手

**关键 bug**：新驱动的等待逻辑 `while (!BUSY)` 和 SSD1306（已验证工作）的 `while (BUSY)` **完全相反**。

| 驱动 | 写法 | 含义 |
|------|------|------|
| SSD1306 ✅ | `while (BUSY)` | 等传输**完成** |
| 旧 MPU6050 ❌ | `while (!BUSY)` | 等传输**开始**（但多字节时可能已经完成，直接跳过） |

已全部改成 SSD1306 的 `while (BUSY)` 模式。编译下载，晃动传感器看 `GZ` 和 `Y` 会不会变了。

## 用户

GZ开机固定值不变（每次重启值都不一样）W OX68 Y始终为0

## 助手

`GZ` 每次开机不同说明读到的是栈上垃圾——`ReadRegs` 超时没写数据。问题是 `ReadWhoAmI` (单字节) 和 `ReadGyroRaw` (多字节) 连续调用时 I2C 状态乱了。

只读 GZ、不读 WHO，排除前后干扰：

## 助手

编译下载，晃动传感器看 `GZ` 和 `GX`。如果变了说明之前是 ReadWhoAmI+ReadGyroRaw 连续调用的干扰。如果还是不变，那就是多字节 ReadRegs 本身在主循环里一直超时。

## 用户

把其他模块删掉吧我重新接了一下线，PA0 PA1 接的是MPU6050 PA15 PA16是OLED你重新写一下代码

## 助手

好，重新来过。PA0/PA1→MPU6050 (I2C_0)，PA15/PA16→OLED (I2C_1)。写一个干净版本，只保留核心。

先确认当前 syscfg 的 I2C 配置：

## 助手

I2C_0 (PA0/PA1) → MPU6050, I2C_1 (PA15/PA16) → OLED。修正所有 I2C 引用 + 简洁版代码：

## 助手

现在写简洁版 `empty.c`——只要 MPU6050 + OLED：

## 助手

Keil 引用的是 `..\..\OLED\ssd1306.c`，也要修：
