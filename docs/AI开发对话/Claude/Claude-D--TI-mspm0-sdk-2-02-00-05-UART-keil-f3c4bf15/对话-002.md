# Claude-D--TI-mspm0-sdk-2-02-00-05-UART-keil-f3c4bf15（续）

## 助手

| `UART/empty.c` | ✅ 已修复 (disable→config→enable) |
| `UART/ti_msp_dl_config.h` | ✅ 已添加 I2C_0 + UART_0 宏定义 |
| `UART/ti_msp_dl_config.c` | ✅ 已添加 I2C_0 + UART_0 初始化 |
| `UART/ssd1306.c` / `UART/ssd1306.h` | ✅ OLED 驱动 (复制自 OLED 项目) |
| `UART/empty.syscfg` | ✅ 已配置 I2C + UART (SysConfig 会据此生成代码) |

## 硬件接线

| MSPM0G3507 | HC-05 |
|:-:|:-:|
| PA22 (UART2 RX) | TX |
| PA21 (UART2 TX) | RX |
| 3.3V | VCC |
| GND | GND |

**注意**: HC-05 的 TX 接 MSPM0 的 RX (PA22)，HC-05 的 RX 接 MSPM0 的 TX (PA21) — 交叉连接。

重新编译下载后，手机通过蓝牙连接 HC-05，发送文字就会在 OLED 上显示，RX 计数也会增加。如果还是 RX:0，请检查：
1. HC-05 的 TX/RX 有没有接反
2. HC-05 波特率是否确实是 9600（用 AT 命令确认：`AT+UART?`）
3. 手机是否已成功配对并连接 HC-05

## 用户

非常好，完美显示内容了，好的下一步参考D:\TI\mspm0_sdk_2_02_00_05\moto\keil这个代码，实现蓝牙控制前进后退加速减速，在开机时先轮子转1s来确认程序运行，OLED屏幕上显示相关参数相关针脚我已经在图形化配置中配好了你弄一下

## 助手

Good — the SysConfig has already regenerated these files with I2C and UART, but PWM and GPIO motor pins from the GUI config aren't reflected yet. Let me add them now:

## 助手

Now update `ti_msp_dl_config.c`:

## 助手

Now the main program — motor control with Bluetooth + OLED:

## 助手

全部修改完成！以下是总结：

---

## 修改的文件

### 1. `UART/empty.c` — 主程序 (完全重写)

| 功能 | 说明 |
|------|------|
| **开机自检** | 启动后车轮前进 1 秒，OLED 显示 "SELF TEST..." |
| **蓝牙控制** | HC-05 接收指令实时控制小车 |
| **OLED 显示** | 8 行信息面板 (方向/速度/PWM/GPIO/指令) |
| **电机驱动** | PWM 调速 + GPIO 方向控制 (L298N) |

### 2. `UART/ti_msp_dl_config.h` — 新增 PWM + GPIO 宏定义

```c
PWM_0:  TIMG0, CCP0=PA12, CCP1=PA13
AIN2:   PB15  (Motor A 方向)
AIN1:   PB16  (Motor A 方向)
BN1:    PB2   (Motor B 方向)
BIN2:   PB3   (Motor B 方向)
```

### 3. `UART/ti_msp_dl_config.c` — 新增 PWM + GPIO 初始化

---

## 蓝牙控制指令

| 按键 | 功能 |
|:-:|:-:|
| **F** | 前进 |
| **B** | 后退 |
| **L** | 左转 |
| **R** | 右转 |
| **S** / 空格 | 停止 |
| **+** | 加速 +10% |
| **-** | 减速 -10% |
| **0-9** | 速度档位 (10%~90%) |

## OLED 显示内容

| 行 | 内容 |
|:-:|------|
| 0 | `BT Car Control` 标题 |
| 1 | 当前方向 + 速度百分比 |
| 2 | RX 接收字节数 + 运行时间 |
| 3 | 分隔线 |
| 4 | PWM0/PWM1 占空比值 |
| 5 | GPIOB 引脚状态 (十六进制) |
| 6 | UART 参数 `9600 8N1` |
| 7 | 最后收到的指令 |

## 硬件接线

| MSPM0G3507 | L298N | HC-05 | SSD1306 |
|:-:|:-:|:-:|:-:|
| PA12 (TIMG0 CCP0) | ENA | | |
| PA13 (TIMG0 CCP1) | ENB | | |
| PB16 (AIN1) | IN1 | | |
| PB15 (AIN2) | IN2 | | |
| PB2 (BN1) | IN3 | | |
| PB3 (BIN2) | IN4 | | |
| PA22 (UART2 RX) | | TX | |
| PA21 (UART2 TX) | | RX | |
| PA0 (I2C0 SDA) | | | SDA |
| PA1 (I2C0 SCL) | | | SCL |

编译下载后，开机车轮会转 1 秒确认程序运行，然后 OLED 显示就绪界面。手机通过蓝牙发送 `F`/`B`/`L`/`R` 等指令即可遥控小车。

## 用户

显然，你把串口又改错了，OLED显示RX 4294967295 电机正常启动自检包括T 0s

## 助手

问题很清楚：RX=4294967295 (即 `0xFFFFFFFF`) 是因为 `snprintf` 的 `%lu` 格式在 MSPM0 上有问题，以及 UART 被重复配置造成了寄存器混乱。让我修复这两个问题：

## 助手

API Error: Connection closed mid-response. The response above may be incomplete.
