# TI开发与安全流程（续）

## 用户

安全性审查做了吗

## 助手

还没对新代码做！按我们的工作流程，烧录前必须先审。现在就跑。

## 助手

[external_agent_tool_call: Skill]
input: {"args":"审查 D:\\TI\\mspm0_sdk_2_02_00_05\\电赛准备\\ 下的新驱动代码，目标芯片 天猛星 MSPM0 G3507。重点检查：\n1. motor.c - TIMA0 PWM + GPIOB 方向的 TB6612 电机控制，是否有 P0-1 限幅、P1-5 上电顺序\n2. encoder.c - TIMG8 QEI + GPIOB 中断的软编码器，中断处理是否正确，GROUP1_IRQHandler 是否冲突\n3. pid.c - PID 控制器的 NaN 保护、积分限幅\n4. main.c - PID 循迹主循环，看门狗、电池监测是否缺失\n5. board.h - 引脚分配是否与 SysConfig 一致\n6. 整体是否有 I2C 无超时等之前审查发现的问题","skill":"embedded-hardware-safety-review"}
[/external_agent_tool_call]

## 助手

[external_agent_tool_result]
Launching skill: embedded-hardware-safety-review
[/external_agent_tool_result]

## 助手

逐文件审查新代码。先确认所有文件最新内容。

## 助手

[external_agent_tool_call: Read]
file: D:\TI\mspm0_sdk_2_02_00_05\电赛准备\motor.c
[/external_agent_tool_call]

## 助手

[external_agent_tool_result]
1	/**
2	 *  TB6612 双电机驱动 实现
3	 */
4	#include "motor.h"
5	#include "board.h"
6	
7	static int16_t g_speed_left  = 0;
8	static int16_t g_speed_right = 0;
9	static bool    g_enabled     = false;
10	
11	/* ========== 辅助: 写 PWM ========== */
12	
13	static inline void pwm_set(uint32_t cc0, uint32_t cc1)
14	{
15	    DL_Timer_setCaptureCompareValue(M_PWM_INST, cc0, M_PWM_CC0);
16	    DL_Timer_setCaptureCompareValue(M_PWM_INST, cc1, M_PWM_CC1);
17	}
18	
19	/* speed: -1000~+1000 → CC 值 (INIT_VAL_LOW 极性: CC=period 时占空比 0%) */
20	static inline uint32_t speed_to_cc(int16_t speed)
21	{
22	    uint16_t duty;
23	    if (speed < 0) {
24	        duty = (uint16_t)(-speed);
25	    } else {
26	        duty = (uint16_t)speed;
27	    }
28	    if (duty > MOTOR_MAX_DUTY) duty = MOTOR_MAX_DUTY;
29	    return (uint32_t)(MOTOR_PWM_PERIOD - duty);
30	}
31	
32	/* ========== 初始化 ========== */
33	
34	void Motor_Init(void)
35	{
36	    /* PWM 归零 */
37	    pwm_set(MOTOR_PWM_PERIOD, MOTOR_PWM_PERIOD);
38	
39	    /* 方向引脚清零 */
40	    DL_GPIO_clearPins(M_PORT,
41	        M_AIN1_PIN | M_AIN2_PIN | M_BIN1_PIN | M_BIN2_PIN);
42	
43	    /* STBY 拉低 (禁用) */
44	    DL_GPIO_clearPins(M_PORT, M_STBY_PIN);
45	
46	    g_speed_left  = 0;
47	    g_speed_right = 0;
48	    g_enabled     = false;
49	}
50	
51	/* ========== 使能/禁用 ========== */
52	
53	void Motor_Enable(void)
54	{
55	    DL_GPIO_setPins(M_PORT, M_STBY_PIN);
56	    g_enabled = true;
57	}
58	
59	void Motor_Disable(void)
60	{
61	    Motor_Stop();
62	    DL_GPIO_clearPins(M_PORT, M_STBY_PIN);
63	    g_enabled = false;
64	}
65	
66	/* ========== 单路控制 ========== */
67	
68	void Motor_Left(int16_t speed)
69	{
70	    if (speed >  1000) speed =  1000;
71	    if (speed < -1000) speed = -1000;
72	
73	    uint32_t cc = speed_to_cc(speed);
74	
75	    /* 先清方向, 再设方向, 最后 PWM */
76	    DL_GPIO_clearPins(M_PORT, M_AIN1_PIN | M_AIN2_PIN);
77	
78	    if (speed > 0) {
79	        DL_GPIO_setPins(M_PORT, M_AIN1_PIN);   /* AIN1=1 AIN2=0 → 正转 */
80	    } else if (speed < 0) {
81	        DL_GPIO_setPins(M_PORT, M_AIN2_PIN);   /* AIN1=0 AIN2=1 → 反转 */
82	    }
83	    /* speed==0: 两个都清零 → 刹车 */
84	
85	    DL_Timer_setCaptureCompareValue(M_PWM_INST, cc, M_PWM_CC0);
86	    g_speed_left = speed;
87	}
88	
89	void Motor_Right(int16_t speed)
90	{
91	    if (speed >  1000) speed =  1000;
92	    if (speed < -1000) speed = -1000;
93	
94	    uint32_t cc = speed_to_cc(speed);
95	
96	    DL_GPIO_clearPins(M_PORT, M_BIN1_PIN | M_BIN2_PIN);
97	
98	    if (speed > 0) {
99	        DL_GPIO_setPins(M_PORT, M_BIN1_PIN);   /* BIN1=1 BIN2=0 → 正转 */
100	    } else if (speed < 0) {
101	        DL_GPIO_setPins(M_PORT, M_BIN2_PIN);   /* BIN1=0 BIN2=1 → 反转 */
102	    }
103	
104	    DL_Timer_setCaptureCompareValue(M_PWM_INST, cc, M_PWM_CC1);
