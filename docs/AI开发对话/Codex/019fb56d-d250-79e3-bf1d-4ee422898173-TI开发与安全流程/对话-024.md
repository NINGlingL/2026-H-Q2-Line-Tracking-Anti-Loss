# TI开发与安全流程（续）

## 助手

|---|---|---|
| 实现方式 | 🔧 **TIMG6 硬件 QEI** | 📝 **GPIOB 中断 + 软件** |
| 引脚 | PA5 + PA6 | PB0 + PB1 |
| 计数精度 | 4x 倍频，硬件自动计数 | 4x 倍频，ISR 软件计数 |
| CPU 开销 | 零 (硬件) | 低速时可忽略，高速需关注 |
| 中断 | 可选 (溢出/方向变化中断) | GPIOB 双边沿中断 |

**为什么 G3507 只有一个硬件编码器？** 你的判断是对的。G3507 有多个 TIMG，但要同时跑两个硬件 QEI 且不与其他外设冲突比较困难。TIMG6 的 CCP0/CCP1 恰好映射在 PA5/PA6 这两个"空闲"引脚上，非常适合做硬件编码器。另一个用软件实现，PB0/PB1 共享 GPIOB 的单个中断向量。

### 2. OLED + ICM20948 共用 I2C0

| 设备 | I2C 地址 | 速率 |
|------|---------|------|
| SSD1306 OLED | 0x3C | 400kHz |
| ICM20948 IMU | 0x68 (AD0=GND) | 400kHz |

两个地址不冲突，一条 I2C 总线搞定。比用两个 I2C 省 2 个引脚。

### 3. TB6612 STBY 引脚 (PB4)

原来 `moto.h` 里 STBY 硬连线到 3.3V（无法软件控制），现在通过 PB4 控制：
- 急停/故障时拉低 STBY → 所有电机立即断电
- 不增加额外引脚开销

### 4. 5 路循迹全在 GPIOB

PB5-PB9 五个引脚都在 GPIOB 上，可以一次读 `DL_GPIO_readPins(GPIOB, 0x03E0)` 拿到全部 5 路状态，效率高。

---

## 与外设的兼容性检查

```
✅ TIMG0   → PA12/PA13 PWM     (G3507 支持)
✅ TIMG6   → PA5/PA6 QEI       (G3507 支持, QEI 模式在 dl_timerg.h)
✅ I2C0    → PA0/PA1           (G3507 支持)
✅ UART2   → PA21/PA22         (G3507 支持)
✅ GPIOB   → PB0-PB9, PB15-16  (G3507 全部可用, 中断在 GPIOB_INT_IRQn)
✅ SWD     → PA19/PA20         (未被占用, 开发阶段安全)
```

---

## 替代/灵活选项

如果你希望**两个编码器都用硬件 QEI**（更高精度、零 CPU 开销），还有一个备选方案：

| 编码器 | 定时器 | 引脚 |
|--------|--------|------|
| 编码器 1 | TIMG6 | PA5 + PA6 |
| 编码器 2 | TIMG12 | PB13 + PB14 |

代价是循迹模块需要挪到其他 GPIO（如 PA2-PA4 等），引脚数也增加。对于电赛小车的一般速度，硬件 + 软件混合方案绰绰有余。

---

这个方案你觉得怎么样？需要调整的地方告诉我。确认后我帮你出每个模块的驱动代码框架。

## 用户

好的你先把审查的问题和接线的安排都保存（分开保存）到D:\TI\mspm0_sdk_2_02_00_05\A问题下，以pdf格式

## 助手

好的，让我先创建目录，然后用 Markdown 转 PDF 的方式生成两份文件。

## 助手

[external_agent_tool_call: Bash]
description: Create A问题 directory
command: mkdir -p "D:/TI/mspm0_sdk_2_02_00_05/A问题"
[/external_agent_tool_call]

## 助手

[external_agent_tool_result]
(Bash completed with no output)
[/external_agent_tool_result]

## 助手

[external_agent_tool_call: Bash]
description: Check available PDF generation tools
command: which pandoc 2>/dev/null; which wkhtmltopdf 2>/dev/null; python3 -c "import markdown; print('md ok')" 2>/dev/null; python3 -c "from reportlab.lib.pagesizes import A4; print('reportlab ok')" 2>/dev/null; echo "---check done---"
[/external_agent_tool_call]

## 助手

[external_agent_tool_result]
---check done---
[/external_agent_tool_result]

## 助手

[external_agent_tool_call: Bash]
description: Check Python availability and PDF packages
command: python --version 2>&1; python -c "print('hello')" 2>&1; pip list 2>&1 | grep -iE "reportlab|fpdf|weasy|pdfkit|markdown"
[/external_agent_tool_call]

## 助手

[external_agent_tool_result]
Python 3.14.3
hello
fpdf2                     2.8.7
[/external_agent_tool_result]

## 助手

[external_agent_tool_call: Bash]
description: Check for Chinese fonts on Windows
command: python -c "
import os
# Check for common Chinese fonts on Windows
candidates = [
    'C:/Windows/Fonts/simsun.ttc',
    'C:/Windows/Fonts/simhei.ttf',
    'C:/Windows/Fonts/msyh.ttc',
    'C:/Windows/Fonts/msyhbd.ttc',
    'C:/Windows/Fonts/simkai.ttf',
]
for f in candidates:
    if os.path.exists(f):
        print(f'FOUND: {f}')
    else:
        print(f'NOT: {f}')
"
[/external_agent_tool_call]
