# TI开发与安全流程（续）

## 助手

[external_agent_tool_result]
File created successfully at: D:\TI\mspm0_sdk_2_02_00_05\电赛准备\line_sensor.c (file state is current in your context — no need to Read it back)
[/external_agent_tool_result]

## 助手

现在更新 `empty.c` 使用新驱动库。

## 助手

[external_agent_tool_call: Read]
file: D:\TI\mspm0_sdk_2_02_00_05\电赛准备\empty.c
[/external_agent_tool_call]

## 助手

[external_agent_tool_result]
1	/*
2	 * MSPM0 G3507 循迹显示程序
3	 * OLED (I2C0 PA0/PA1) + 5路循迹 (PB5~PB9)
4	 */
5	
6	#include "ti_msp_dl_config.h"
7	#include "ssd1306.h"
8	
9	/* ========== 循迹传感器 (全部在 GPIOB 上) ========== */
10	#define LS_PORT   GPIOB
11	#define LS_MASK   (DL_GPIO_PIN_5 | DL_GPIO_PIN_6 | DL_GPIO_PIN_7 | \
12	                   DL_GPIO_PIN_8 | DL_GPIO_PIN_9)
13	#define LS_SHIFT  5U             /* 从 PB5 开始, 右移 5 位得到 bit[0:4] */
14	#define LS_COUNT  5U
15	
16	/* 触发极性: 1=高电平触发, 0=低电平触发 (普通循迹模块通常低电平触发) */
17	#define LS_ACTIVE_LOW  1
18	
19	/* ========== 读取循迹传感器 ========== */
20	
21	/**
22	 * 一次性读取 5 路传感器, 返回 bit[0:4] = 传感器1~5 的触发状态
23	 *   bit=1 表示触发 (检测到黑线)
24	 *   bit=0 表示未触发
25	 */
26	static uint8_t LineSense_Read(void)
27	{
28	    uint32_t raw = DL_GPIO_readPins(LS_PORT, LS_MASK);
29	    uint8_t  val = (uint8_t)((raw >> LS_SHIFT) & 0x1F);
30	
31	#if LS_ACTIVE_LOW
32	    val ^= 0x1F;   /* 低电平触发时取反 */
33	#endif
34	    return val;
35	}
36	
37	/* ========== OLED 显示循迹状态 ========== */
38	
39	/**
40	 * 在 OLED 上显示 5 路循迹传感器状态
41	 * 示例:
42	 *   Xun Ji Status
43	 *   1 2 3 4 5
44	 *   X O O X O       X=触发(黑线)  O=未触发
45	 */
46	static void LineSense_Display(uint8_t status)
47	{
48	    char buf[12];
49	
50	    /* 标题行 */
51	    SSD1306_ShowString(0, 0, "Xun Ji Status");
52	
53	    /* 传感器编号 */
54	    SSD1306_ShowString(1, 0, "1 2 3 4 5");
55	
56	    /* 触发状态 */
57	    for (uint8_t i = 0; i < LS_COUNT; i++) {
58	        buf[i * 2]     = (status & (1 << i)) ? 'X' : 'O';
59	        buf[i * 2 + 1] = ' ';
60	    }
61	    buf[10] = '\0';
62	    SSD1306_ShowString(2, 0, buf);
63	
64	    /* 二进制值 (调试用) */
65	    SSD1306_ShowString(4, 0, "Raw: 0x");
66	    buf[0] = "0123456789ABCDEF"[(status >> 4) & 0x0F];
67	    buf[1] = "0123456789ABCDEF"[status & 0x0F];
68	    buf[2] = '\0';
69	    SSD1306_ShowString(4, 36, buf);
70	}
71	
72	/* ========== 主程序 ========== */
73	
74	int main(void)
75	{
76	    SYSCFG_DL_init();
77	    SSD1306_Init();
78	
79	    /* 开机画面 */
80	    SSD1306_Clear();
81	    SSD1306_ShowString(1, 18, "MSPM0 G3507");
82	    SSD1306_ShowString(3, 12, "Line Tracing");
83	    SSD1306_Update();
84	    delay_cycles(CPUCLK_FREQ * 1);  /* 延时 1 秒 */
85	
86	    uint8_t last_status = 0xFF;     /* 上次状态, 初始化为不可能值以触发首次刷新 */
87	
88	    while (1) {
89	        uint8_t status = LineSense_Read();
90	
91	        /* 只在状态变化时才刷新屏幕, 减少 I2C 通信 */
92	        if (status != last_status) {
93	            last_status = status;
94	
95	            SSD1306_Clear();
96	            LineSense_Display(status);
97	            SSD1306_Update();
98	        }
99	
100	        /* 约 10ms 轮询周期 */
101	        delay_cycles(CPUCLK_FREQ / 100);
102	    }
103	}
104	
[/external_agent_tool_result]

## 助手

[external_agent_tool_call: Write]
file: D:\TI\mspm0_sdk_2_02_00_05\电赛准备\empty.c
[/external_agent_tool_call]
