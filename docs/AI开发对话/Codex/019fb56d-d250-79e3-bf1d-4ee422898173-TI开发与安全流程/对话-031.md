# TI开发与安全流程（续）

## 助手

[external_agent_tool_call: Bash]
description: Find generated config file
command: find "D:/TI/mspm0_sdk_2_02_00_05/电赛准备" -name "ti_msp_dl_config.h" 2>/dev/null; find "D:/TI/mspm0_sdk_2_02_00_05/电赛准备/keil" -type f | head -20
[/external_agent_tool_call]

## 助手

[external_agent_tool_result]
D:/TI/mspm0_sdk_2_02_00_05/电赛准备/keil/empty_LP_MSPM0G3507_nortos_keil.uvguix.16484
D:/TI/mspm0_sdk_2_02_00_05/电赛准备/keil/empty_LP_MSPM0G3507_nortos_keil.uvoptx
D:/TI/mspm0_sdk_2_02_00_05/电赛准备/keil/empty_LP_MSPM0G3507_nortos_keil.uvprojx
D:/TI/mspm0_sdk_2_02_00_05/电赛准备/keil/mspm0g3507.sct
D:/TI/mspm0_sdk_2_02_00_05/电赛准备/keil/startup_mspm0g350x_uvision.s
[/external_agent_tool_result]

## 助手

看清楚了。你的 SysConfig 配置：
- **OLED**: I2C0 → PA0(SDA) + PA1(SCL)
- **循迹**: GPIO_GRP_0 → PB5/PB6/PB7/PB8/PB9

但要跑起来有一个**必须修复的问题**：

---

### ⚠️ ssd1306.c 和你的配置不兼容

`ssd1306.c` 里硬编码用的是 `I2C_1_INST`，但你 SysConfig 生成的是 `OLED_INST`（映射到 I2C0）。直接用编译不过。

**需要改 4 处**（把 `I2C_1_INST` → `OLED_INST`）：
```c
// ssd1306.c 第114行
DL_I2C_fillControllerTXFIFO(OLED_INST, data, len);    // 原来是 I2C_1_INST
// 第116行  
DL_I2C_startControllerTransfer(OLED_INST, ...);        // 原来是 I2C_1_INST
// 第118行
while (DL_I2C_getControllerStatus(OLED_INST) & ...);   // 原来是 I2C_1_INST
// 第119行
while (!(DL_I2C_getControllerStatus(OLED_INST) & ...)); // 原来是 I2C_1_INST
// 第120行
DL_I2C_flushControllerTXFIFO(OLED_INST);               // 原来是 I2C_1_INST
```

---

改完之后，`empty.c` 的循迹显示代码可以这样写：

```c
#include "ti_msp_dl_config.h"
#include "ssd1306.h"

// 循迹传感器引脚掩码 (全部在 GPIOB 上)
#define LS_MASK (DL_GPIO_PIN_5 | DL_GPIO_PIN_6 | DL_GPIO_PIN_7 | \
                 DL_GPIO_PIN_8 | DL_GPIO_PIN_9)
#define LS_SHIFT 5  // 从 PB5 开始

int main(void)
{
    SYSCFG_DL_init();
    SSD1306_Init();
    SSD1306_Clear();

    while (1) {
        // 一次性读取 5 路循迹
        uint32_t raw = DL_GPIO_readPins(GPIOB, LS_MASK);
        
        // 解码: 位[5:9] = 对应 PB5~PB9
        uint8_t status = (uint8_t)((raw >> LS_SHIFT) & 0x1F);

        // 显示到 OLED
        SSD1306_ShowString(0, 0, "Xun Ji Status");
        SSD1306_ShowString(1, 0, "1 2 3 4 5");
        
        char line[12];
        for (int i = 0; i < 5; i++) {
            // 假设 LOW=触发(黑线), HIGH=未触发(白底)
            line[i * 2]     = (status & (1 << i)) ? 'O' : 'X';
            line[i * 2 + 1] = ' ';
        }
        line[10] = '\0';
        SSD1306_ShowString(2, 0, line);
        
        SSD1306_Update();
        
        delay_cycles(CPUCLK_FREQ / 10);  // 100ms 刷新
    }
}
```

OLED 上会显示类似这样：
```
Xun Ji Status
1 2 3 4 5
X O O X O      ← X=触发(黑线) O=未触发
```

---

要我帮你改吗？需要改 `ssd1306.c`（替换 I2C 实例名）和 `empty.c`（加循迹显示逻辑）两个文件。

## 用户

改

## 助手

先确认电赛准备目录下的 ssd1306.c 和 empty.c 内容。

## 助手

[external_agent_tool_call: Read]
file: D:\TI\mspm0_sdk_2_02_00_05\电赛准备\ssd1306.c
[/external_agent_tool_call]

## 助手

[external_agent_tool_result]
1	/**
2	 * SSD1306 驱动实现 (I2C)
3	 * MSPM0G3507 + DriverLib
4	 */
5	
6	#include "ssd1306.h"
7	
8	/* ========== 显示缓冲区 (128x64 = 1024 字节) ========== */
9	static uint8_t OLED_Buffer[SSD1306_WIDTH * SSD1306_HEIGHT / 8];
10	
11	/* ========== 6x8 ASCII 字库 (字符 0x20 ~ 0x7E) ========== */
12	static const uint8_t Font6x8[][6] = {
13	    {0x00,0x00,0x00,0x00,0x00,0x00}, /* 空格 */
14	    {0x00,0x00,0x5F,0x00,0x00,0x00}, /* ! */
15	    {0x00,0x07,0x00,0x07,0x00,0x00}, /* " */
16	    {0x14,0x7F,0x14,0x7F,0x14,0x00}, /* # */
17	    {0x24,0x2A,0x7F,0x2A,0x12,0x00}, /* $ */
18	    {0x23,0x13,0x08,0x64,0x62,0x00}, /* % */
