# TI开发与安全流程（续）

## 助手

[external_agent_tool_call: mcp__stm32__stm32_list_probes]
input: {}
[/external_agent_tool_call]

## 助手

[external_agent_tool_result]
{"result":"No ST-Link probes detected. Check USB connections."}
[/external_agent_tool_result]

## 助手

Keil 自带下载功能最快。**在 Keil 里按 F8 (Download)** 就能烧录——工程里已经配好了 CMSIS-DAP 调试器。

或者试试直接用 embedded-debugger：

## 助手

[external_agent_tool_call: Bash]
description: Flash with embedded-debugger CLI
command: embedded-debugger-mcp flash --chip MSPM0G3507 --file "D:/TI/mspm0_sdk_2_02_00_05/20948/keil/Objects/empty_LP_MSPM0G3507_nortos_keil.hex" 2>&1
[/external_agent_tool_call]

## 助手

[external_agent_tool_result: error]
Exit code 2
error: unrecognized subcommand 'flash'

Usage: embedded-debugger-mcp.exe [OPTIONS] [COMMAND]

For more information, try '--help'.
[/external_agent_tool_result]

## 助手

[external_agent_tool_call: Bash]
description: Check available CLI commands
command: embedded-debugger-mcp --help 2>&1
[/external_agent_tool_call]

## 助手

[external_agent_tool_result]
A Model Context Protocol server for embedded debugging

Usage: embedded-debugger-mcp.exe [OPTIONS] [COMMAND]

Commands:
  serve   Serve the MCP server over stdio
  config  Configuration utilities
  probes  Debug probe utilities
  doctor  Run environment checks for CLI, MCP, and hardware discovery
  skill   Skill helper commands for agent-driven workflows
  help    Print this message or the help of the given subcommand(s)

Options:
  -c, --config <CONFIG>                          Path to configuration file
      --log-level <LOG_LEVEL>                    Log level (error, warn, info, debug, trace)
      --log-file <LOG_FILE>                      Log file path
      --max-sessions <MAX_SESSIONS>              Maximum number of concurrent debug sessions
      --session-timeout <SESSION_TIMEOUT>        Session timeout in seconds
      --default-speed <DEFAULT_SPEED>            Default debugger speed in kHz
      --connection-timeout <CONNECTION_TIMEOUT>  Connection timeout in milliseconds
      --retry-count <RETRY_COUNT>                Connection retry count
      --rtt-buffer-size <RTT_BUFFER_SIZE>        RTT buffer size in bytes
      --rtt-poll-interval <RTT_POLL_INTERVAL>    RTT poll interval in milliseconds
      --allow-flash-erase                        Allow flash erase operations
      --restrict-memory-access                   Restrict memory access to safe ranges
      --generate-config                          Generate default configuration file
      --validate-config                          Validate configuration and exit
      --show-config                              Show current configuration and exit
  -h, --help                                     Print help
  -V, --version                                  Print version
[/external_agent_tool_result]

## 助手

CLI 没 flash 命令。两种方式烧录：

**方式 1（最简单）：Keil 里按 F8 (Download)**  
工程已配好 CMSIS-DAP，直接下载。

**方式 2：我用 MCP 服务模式**
你要哪种？

## 用户

方式2

## 助手

MCP 服务不方便后台跑。让我试试直接用 probe-rs 命令行烧录。

## 助手

[external_agent_tool_call: Bash]
description: Check if probe-rs CLI is available
command: which probe-rs 2>/dev/null && probe-rs --version 2>&1 || echo "no probe-rs"
[/external_agent_tool_call]

## 助手

[external_agent_tool_result]
no probe-rs
[/external_agent_tool_result]

## 助手

[external_agent_tool_call: Bash]
description: Check OpenOCD availability
command: which openocd 2>/dev/null && openocd --version 2>&1 | head -3 || echo "no openocd"
[/external_agent_tool_call]

## 助手

[external_agent_tool_result]
no openocd
[/external_agent_tool_result]

## 助手

[external_agent_tool_call: Bash]
description: Check probes subcommands
command: embedded-debugger-mcp probes --help 2>&1
[/external_agent_tool_call]
