# 第三问滚球控制（续）

## 助手

我已按安全清单核对现有闭环保护：串口超时会关电机、PID 有积分与输出限幅、步进目标在驱动入口再次限位、看门狗只在主循环末尾喂。现在开始加入两页轮播 OLED：第一页以运行状态为主，第二页显示 PID、速度/加速度、超时、65 mm 零位等配置。

## 助手

OLED 页面已经接入现有状态机：`U:OK/WAIT/BAD/OLD` 表示串口数据状态，`M:OFF/IDLE/MOVE` 表示驱动禁用、已使能静止、正在出步；同时显示球位置/目标/速度/误差、PID 输出、步数、收包与拒包计数。下面我会实际编译并检查屏幕布局对应的字符串长度与工程配置一致性。

## 助手

编译已经通过：0 个错误、0 个警告，并重新生成了可烧录 HEX。安全显示逻辑也做了一个细节处理：一旦进入 SAFE，OLED 不再轮播参数页，而是持续显示故障码和 `MOTOR OFF`，避免故障信息被遮住。现在进行最终差异检查和 Git 提交。

## 助手

已完成 OLED 状态显示，并按现有工程配置编译、提交 Git。

OLED 每 2 秒轮播两页：

- 状态页：串口 `WAIT/OK/BAD/OLD`、数据延迟、步进电机 `OFF/IDLE/MOVE`、任务阶段、球坐标、目标、速度、误差、PID 输出、步数、收包/拒包计数。
- 参数页：Kp/Ki/Kd、容差、输出限幅、变化率、步进速度/加速度、串口超时、稳定时间、65 mm 平衡位置、800 步/mm、UART 115200 PB0/PB1。
- 进入 `SAFE` 后停止轮播，持续显示故障码和 `MOTOR OFF`。

代码位置：[empty.c](D:/TI/mspm0_sdk_2_02_00_05/TMC2208/empty.c:655)

验证结果：

- Keil：`0 Error(s), 0 Warning(s)`
- HEX：[empty_LP_MSPM0G3507_nortos_keil.hex](D:/TI/mspm0_sdk_2_02_00_05/TMC2208/keil/Objects/empty_LP_MSPM0G3507_nortos_keil.hex)
- 视觉 Python 哈希核对一致，没有修改
- Git 工作区干净
- 提交：`28c9fe2 feat: show PID and link status on OLED`

上电前仍需人工把移动端放在距电机端 65 mm 的水平位置。

::git-stage{cwd="D:\TI\mspm0_sdk_2_02_00_05\TMC2208"}
::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\TMC2208"}

## 用户

OLED屏幕乱码然后你先一个由按键PB21来设置平衡位置，手动调不了

## 助手

我先处理两个点：OLED 乱码优先检查并修正 SSD1306 的寻址模式；PB21 按键会按工程配置加入去抖和明确的校准状态。由于没有原点开关，不能让电机盲目撞机械端点，我会把流程设计成“机构先位于电机端的已知起点，按 PB21 后自动走到距电机端 65 mm”，运行过程中再按不会误触发。视觉 Python 仍然不修改。此次仍按电机安全检查清单复核，并在完成后编译和提交 Git。

## 用户

可以参考D:\TI\mspm0_sdk_2_02_00_05\电赛备用2\keil的OLED驱动，然后改吧

## 助手

我会以“电赛备用2”里的 OLED 初始化、寻址和刷新方式为基准修正当前驱动，再把 PB21 按键加入当前工程配置。按键流程沿用安全前提：机构上电时位于电机端起点，按下 PB21 后电机自动走 65 mm 到水平位；校准完成前不会进入视觉 PID，运行中按键无效，避免误动作。视觉代码仍只读。此次电机与按键改动继续按嵌入式安全清单复核。

## 助手

参考工程里已经有完全相同的 PB21 按键配置，也明确使用 SSD1306 的页寻址模式；当前驱动却设置成水平寻址、刷新时又按页写，这是乱码的直接原因。我会同步参考工程的已验证初始化序列，并照它的方式把 PB21 配成上拉输入。

## 助手

PB21 已按参考工程配置为内部上拉、低电平按下，并加入 30 ms 去抖。控制流程现在是：等待按键 → 从电机端坐标自动走 65 mm → 到位保持 → 等待视觉 → 执行第三问；校准运动设有 15 秒超时，超时立即关闭驱动并显示故障。接下来编译并检查状态机边界。

## 助手

编译已通过（0 错误、0 警告）。OLED 初始化现在与参考工程一致，关键修正是 `page addressing`；PB21 也通过 SysConfig 验证为 GPIOB.21 上拉输入。最后我会提交这轮全部源文件和自动生成的配置文件，并再次核对视觉 Python 未变化。

## 助手

已按“电赛备用2”修改完成。

- OLED 乱码：原驱动使用水平寻址，但刷新按页写入；现已改成参考工程的页寻址初始化序列。
- PB21：配置为内部上拉，按键接地有效，带 30 ms 去抖。
- 上电后 OLED 提示 `PRESS PB21`。
- 按下后步进电机自动从电机端起点移动 65 mm。
- 到位后保持平衡位置，再等待视觉数据启动 PID。
- 校准过程中按键不会重复触发；15 秒未到位会关闭电机并进入 SAFE。
- 视觉 Python 未修改。
- Keil 编译：`0 Error(s), 0 Warning(s)`。
- Git 提交：`e0ff42f`

主要代码：[empty.c](D:/TI/mspm0_sdk_2_02_00_05/TMC2208/empty.c:414)  
OLED 修复：[ssd1306.c](D:/TI/mspm0_sdk_2_02_00_05/TMC2208/ssd1306.c:173)  
可烧录文件：[HEX](D:/TI/mspm0_sdk_2_02_00_05/TMC2208/keil/Objects/empty_LP_MSPM0G3507_nortos_keil.hex)

首次测试必须确认正方向是“移动端远离电机端”。如果机构上电时无法位于电机端起点，则必须增加原点限位开关，否则软件无法判断绝对的 65 mm 位置。

::git-stage{cwd="D:\TI\mspm0_sdk_2_02_00_05\TMC2208"}
::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\TMC2208"}

## 用户

多了，先55mm吧，然后显示串口相关信息啊，我的OLED上只有BALANCE STUP

## 助手

这说明上一版只成功写出了 OLED 的第 0 页，后续页在 I²C 刷新中断掉了。我会直接采用“电赛备用2”更稳的 GPIO 模拟 I²C 分页发送方式，并把串口状态放到第 0 行，确保即使显示刚启动也能看到 `UART:OK/WAIT/OLD/BAD`。平衡距离同时从 65 mm 全面改成 55 mm，而不是只改屏幕文字。视觉代码继续不动。

## 助手

已把 55 mm 同步到软件零点、硬限位、自动移动步数和 OLED 文案；不是只改显示。OLED 第 0 行现在固定显示串口状态和最近数据年龄，后续行显示收包/拒包、视觉位置和速度。驱动也换成参考工程的整页发送，避免只写出标题页。现在进行编译验证。

## 助手

最终编译仍为 0 错误、0 警告。现在第 0 行会直接显示 `UART:WAIT/OK/OLD/BAD` 和数据年龄；初始化后第一次刷新就发送第 0～2 页，不会再出现只剩标题、串口状态看不到的情况。下面提交本轮 Git。

## 助手

已改为 55 mm，并修复 OLED 只显示标题的问题。

OLED 启动后应显示：

```text
UART:WAIT A:---ms
BAL SET 55.0MM
KEY:PB21 M:OFF
PRESS TO AUTO MOVE
Rx:00000 Rj:000
...
```

收到视觉数据后，`UART:WAIT` 会变成 `UART:OK`，并显示数据年龄、收包数、拒包数、位置和速度。

同时：

- 自动平衡行程改为 55 mm。
- 软件零点和步进电机硬限位同步改为 55 mm。
- OLED 改用参考工程的稳定分页发送方式。
- Keil 编译：0 错误、0 警告。
- 新 HEX：[烧录文件](D:/TI/mspm0_sdk_2_02_00_05/TMC2208/keil/Objects/empty_LP_MSPM0G3507_nortos_keil.hex)
- Git 提交：`85dc08e`
- 视觉 Python 未修改。

::git-stage{cwd="D:\TI\mspm0_sdk_2_02_00_05\TMC2208"}
::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\TMC2208"}
