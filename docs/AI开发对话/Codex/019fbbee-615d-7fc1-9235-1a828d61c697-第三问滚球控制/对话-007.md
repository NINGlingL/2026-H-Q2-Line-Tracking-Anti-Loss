# 第三问滚球控制（续）

## 助手

一次性恢复固件已烧录并运行，按照片记录的 `+4800` 步执行了回落到相对零点。现在撤掉一次性偏移，重新生成正式固件；否则下次重启会重复下降。

## 助手

原因是照片中的 `FAULT:03`：相机短时漏检后，旧程序立即锁存故障并关闭电机，所以后来即使显示 `UART:OK` 也不会继续。

现已修复并烧录：

- `+4800` 步对应球向负方向，闭环方向已纠正。
- 视觉中断超过 150 ms：先停止步进并保持。
- 700 ms 内恢复：自动继续 PID。
- 超过 700 ms：才进入 `FAULT:03`。
- 已根据照片记录先下降 4800 步，恢复原 62 mm 平衡位置。
- 正式固件已重新烧录，`Verify OK`。
- 最新提交：`7b4b913`。

现在将球放到 O 点，短按一次 PB21。OLED 应依次显示：

```text
READY → GO+5 → GO-5 → HOLD
```

如果仍异常，请不要复位，直接拍 OLED；保留的 `St` 和故障码能确定实际运动过程。

::git-stage{cwd="D:\TI\mspm0_sdk_2_02_00_05\TMC2208"}
::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\TMC2208"}

## 用户

我现在有个问题，就是最开始我的平衡位置的问题，你是没有办法记住当前位置吗？如果这样那上电你可以做一个下降到最低端，，然后上升到62mm可以吗？而且我觉得你的PID不灵敏啊不是+5 -5 来回震荡啊

## 助手

这个问题要分开看：当前位置可以“在软件运行期间记住”，但断电/重新烧录后，步进电机没有编码器，MCU 也无法知道丝杆是否被移动过。上电盲目向下撞机械端点并不能检测“已经到底”，只会让电机持续堵转，因此我不能直接把它写成无反馈撞限位。  
我会按电机安全约束检查当前工程是否已有最低端限位输入；同时核对现有 PID 的实际输出是否已经饱和，再给你明确的可执行方案。

## 助手

可以实现“下降归零 → 上升 62 mm”，但必须增加最低端限位开关。当前工程只有 STEP、DIR、EN 和 PB21，没有任何信号告诉 MCU 已经到底；直接撞机械端点会持续堵转，不能作为可靠归零方式。

推荐流程：

1. 上电低速下降。
2. 最低端限位开关触发，立即停止。
3. 当前位置记为 0。
4. 上升 `62 mm = 49600` 微步。
5. 到位后显示 `READY`。
6. 短按 PB21，开始第三问 PID。
7. 限位未在规定时间触发则关闭电机并报错。

请安装一个最低端微动开关，告诉我接到 MSPM0G3507 的哪个空闲 GPIO，我就直接按上述流程改代码。

另外，第三问不是持续在 `+5/-5` 间振荡，而是只执行一次：

```text
O → +5 cm → 折返 → -5 cm → 稳定保持
```

当前 `Kp=1.8` 时，5 cm 误差已经产生 9 mm 指令并被限制到 6 mm，所以照片中的“不灵敏”不是 Kp 太小，而是之前方向错误和 `FAULT:03` 停机。上一版已经修正这两点。建议先测试刚烧录的版本；若要方便调 PID，我可以另外增加“连续 ±5 cm 往返测试模式”，正式比赛模式仍按题目在 `-5 cm` 停住。

## 用户

你先上移5mm我看看是不是平衡位置

## 助手

可以。我先切成一次性的“平衡点上移 5 mm 测试版”：烧录后从当前 62 mm 位置上升到约 67 mm（`+4000` 微步），PB21 和 PID 暂时禁用，避免测试时误启动闭环。确认位置后我再根据你的结果恢复正式程序。这个动作会驱动电机，我会保留 ±10 mm 限位并先编译、提交再烧录。

## 助手

上移测试版编译通过，`0 Error / 0 Warning`；目标严格限制为 `+4000` 微步（5 mm），PID 已禁用。现在提交并烧录，烧录后电机会自动完成这一次上移。

## 助手

已完成上移 5 mm：

- 从原平衡点约 62 mm 上升至约 67 mm。
- 移动量：`+4000` 微步。
- PID 和 PB21 已暂时禁用。
- 烧录验证成功：`Verify OK`。
- Git 提交：`261ac87`。

OLED 应显示 `UP TEST:62 TO 67MM`，电机完成后为 `M:IDLE`。

现在请观察水管是否水平、小球能否自然停在 O 点。先不要复位或重新上电，否则测试程序会再次上移 5 mm。告诉我“这里是否平衡”，我再立即恢复正式程序并记录最终平衡高度。

::git-stage{cwd="D:\TI\mspm0_sdk_2_02_00_05\TMC2208"}
::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\TMC2208"}

## 用户

那这样，我现在调好位置了，当前就是0点然后你的每一次上下移动你都要做一个计算上加下减，在下一次上电或者重启时移动相应数据到零点，然后PID我觉得很不灵敏而且现在OLED卡屏幕了，不需要保持，我只需要你来回震荡，快做

## 助手

明白，按你现在的要求改成“测试模式”：当前位置就是新的机械零点；PB21 启动后在 `+5 cm / -5 cm` 之间持续往返，不再在 -5 cm 保持。我会把控制改得更激进，并让 OLED 只做低频状态显示，不参与控制时序。

但“断电后精确回零”仅靠步进计数无法百分之百保证：断电可能发生在两个脉冲之间，也可能有人手动转动丝杆。我先检查 G3507 工程现有的非易失存储接口，做带校验、限频写入的相对步数记录；如果无法安全写入，就只做复位保留并明确限制，不会用撞机械端点代替传感器。由于涉及连续电机运动和 Flash 寿命，我会按电机安全检查保留失联停机及 ±10 mm 硬限位。

## 助手

检查结果：G3507 这块工程没有可在断电后保留的 VBAT Scratchpad；连续 PID 中把每个步数写入主 Flash 会很快耗尽擦写寿命，所以我不会这样做。现有步进中断已经严格执行“上一步 `+1`、下一步 `-1`”，OLED 的 `St` 就是累计相对步数；复位期间想无传感器精确恢复仍然做不到。

我先完成你最急的功能：撤销刚才的 +5 mm 单测，上电把你现在调好的机械位置作为 0，PB21 启动连续 `+5 ↔ -5`，并明显提高响应速度。

## 助手

连续往返版已编译通过，`0 Error / 0 Warning`。响应参数已从 `Kp 1.8 / Kd 1.4 / 输出±6 / 斜率18` 调整为 `Kp 2.4 / Kd 0.65 / 输出±8 / 斜率60`；到 `+4.5 cm` 就切换目标到 `-5`，到 `-4.5 cm` 再切回 `+5`，不再等待稳定或保持。驱动层仍限制在相对零点 ±10 mm。现在提交并烧录。

## 助手

已完成并烧录：

- 当前机械位置作为新零点。
- PB21 启动连续 `+5 cm ↔ -5 cm` 往返。
- 到 `+4.5 cm` 立即折返，到 `-4.5 cm` 再折返。
- PID 响应明显加快：`Kp=2.4、Kd=0.65、输出±8 mm、变化率60 mm/s`。
- OLED 显示 `OSC PID STATUS`，`St` 为上加下减的实时累计步数。
- 编译 `0 Error / 0 Warning`，烧录 `Verify OK`。
- 提交：`8ae5eca`。

现在把球放在 O 点，短按 PB21。

注意：运行中不要断电或复位。没有编码器、最低限位开关和断电保持存储时，断电瞬间的真实位置无法可靠保存；若要实现任何时刻重新上电自动回零，必须增加最低端限位开关。当前程序不能安全地盲撞端点。

::git-stage{cwd="D:\TI\mspm0_sdk_2_02_00_05\TMC2208"}
::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\TMC2208"}
