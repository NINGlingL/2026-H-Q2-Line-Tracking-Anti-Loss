# 第三问滚球控制（续）

## 用户

现在允许使用MCP探针一键烧录调试，这一版烧录显示只有UART:WAIT A:---ms，其他都没有显示，然后步进电机转反了

## 助手

我会使用 `embedded-debugger` 技能通过 MCP 探针完成烧录和在线检查：先确认探针/芯片连接，再修正步进方向，烧录后直接查看 OLED 刷新状态、页索引、串口计数和控制阶段，判断是“缓冲区没写”还是“I²C 在第 1 页后失败”。不会再只靠猜测。视觉 Python 仍不修改。

## 助手

探针已确认：`CMSIS-DAP_LU`（序列号 `LU_2022_8888`）在线。接下来先只读连接 MSPM0G3507；确认目标身份和运行状态后，我再修改、编译并烧录，避免把固件写到错误芯片。

## 助手

只读连接确认目标确实是 `MSPM0G3507`，当前已由探针暂停。在线 RAM 里能看到视觉串口曾收到有效帧，但烧录中的旧版与本地新调试版符号布局已不同，因此我不会用错位地址下结论。现在先把 DIR 极性全局反转、编译并提交，再用探针烧录新 ELF；烧录后符号和固件一致，再读取 OLED 页状态与串口状态。

## 助手

MCP 已连接正确芯片，但第一次烧录被本机安全配置拦截：当前 `allow_flash_erase=false`。这是工具权限限制，不是芯片故障；你已经明确授权烧录，我会只开启本次所需的 flash erase 权限，然后重新连接、烧录并校验，不执行整片手工擦除。

## 助手

配置已更新并验证，但 Codex 内置的 MCP 进程重启后本轮传输通道没有自动恢复。我会继续使用同一个 `embedded-debugger-mcp` 程序，通过其标准 MCP 通道完成烧录和校验；不会退回到未验证的普通拷贝方式。

## 助手

新固件已通过 MCP 成功烧录，校验通过。探针运行 2 秒后的 RAM 表明主循环和 SysTick 正常，但驱动内部静态符号的调试地址与实际布局不可靠；我会改用已确认的 OLED 帧缓冲地址，逐页统计内容，并在断开前恢复运行。这样能确定屏幕缺行究竟发生在绘制层还是总线发送层。

## 助手

探针读取到的 8 个 OLED 帧缓冲页全部有文字，说明绘制层没有丢内容；问题已经缩小到“屏幕控制器/总线只接受了第 0 页”。我会加入两个极小的在线诊断量（刷新次数、I²C 健康状态），再烧录一次并由探针读取。这样可以区分总线在第 1 页 NACK，还是这块屏实际是 128×32、初始化参数不匹配。

## 助手

我已经通过探针把问题定位到一个很具体的原因：OLED 刷新路径实际需要约 304 字节栈，但工程启动文件只分配了 256 字节，刷新时会越界破坏 OLED 状态，所以缓冲区里虽然八行都有内容，屏上却可能只剩第一行。现在我先把栈空间扩到安全范围，重新编译、提交，再用探针烧录并读取运行参数确认；电机反向修正也会保留在这一版。

## 助手

修正已经编译通过：0 错误、0 警告。静态分析仍显示 OLED 最深调用约 304 字节，而现在已分配 1024 字节，给中断也留出了余量。接下来我会先按你的要求提交这一轮，再烧录并用探针确认 OLED 刷新计数和通信状态没有再被栈越界破坏。

## 助手

新版正在烧录并校验，随后我会让程序实际运行两秒，再暂停读取 OLED 的健康标志、刷新次数和八页显存。这样能区分“程序没画出来”和“屏幕/接线没显示出来”。

## 助手

烧录进程比上一轮耗时异常，我正在检查探针会话是否卡在写入或校验阶段；不会在未确认目标重新运行前结束。若会话本身超时，我会安全断开后重新连接再烧录一次。

## 用户

我自己烧录吧

## 助手

可以，你自己烧录最新工程即可。

- 最新提交：`23481d2 fix: prevent OLED refresh stack overflow`
- 已修复 OLED 栈溢出：256 B → 1024 B
- 已修复步进电机方向相反
- 视觉 Python 文件未修改
- 工程编译：0 错误、0 警告
- 固件位置：[empty_LP_MSPM0G3507_nortos_keil.axf](D:\TI\mspm0_sdk_2_02_00_05\TMC2208\keil\Objects\empty_LP_MSPM0G3507_nortos_keil.axf)

刚才探针会话在烧录阶段被停止，目标板可能处于暂停或固件不完整状态，直接用 Keil 完整下载一次并复位即可。

::git-stage{cwd="D:\TI\mspm0_sdk_2_02_00_05\TMC2208"}
::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\TMC2208"}

## 用户

55改成77，屏幕正常，但是UART是WAIT形态啊，连上相机也是WAIT PRESS T0 T0键是什么？

## 助手

我先按当前工程把 55 mm 改成 77 mm，并核对 `UART:WAIT` 与 `PRESS T0` 的实际触发条件和按键引脚映射。这里涉及步进电机动作，我会按嵌入式硬件安全检查流程确认限位、使能和按键逻辑；视觉 Python 仍只读不改，完成后照常提交 Git。

## 助手

已经确认屏上的不是“T0 键”，而是英文 `PRESS TO AUTO MOVE` 里的 `TO`，OLED 字体把字母 O 显得像数字 0。它的意思是“按 PB21 自动移动到平衡位”。我会把这一行直接改成明确的 `PRESS PB21 TO MOVE`。

`UART:WAIT` 的含义也查清了：主控尚未收到一帧合格的 `P:位置,V:速度` 数据。视觉程序只有识别到小球时才发送，所以“相机已接线”本身不会变成 `OK`；屏上的 `Rx`/`Rj` 可以进一步区分是完全没收到还是收到但格式被拒绝。

## 助手

77 mm 与屏幕提示已经改好。编译器已经更新了固件文件，但 Keil 当前有两个已打开的窗口，命令行没有生成构建日志；我正在用独立构建方式补做一次明确的错误/警告检查，不会关闭你正在开的 Keil。

## 助手

这一版的物理范围也核对过：77 mm 平衡点对应从电机端移动 61,600 微步，软件限位变为相对平衡点 `-77 mm～+23 mm`，仍严格落在 100 mm 丝杆行程内；按键前电机保持禁用，运动超时仍为 15 秒。视觉文件哈希与 Git 中完全一致，没有被修改。现在提交这次更迭。

## 助手

已改为 77 mm，并完成提交：

- 平衡位置：77 mm
- OLED 提示改为 `PRESS PB21 TO MOVE`
- 编译：0 错误、0 警告
- Git：`1ab86fd fix: set balance position to 77mm`
- 视觉 Python 未修改

`PRESS T0` 实际是 `PRESS TO`，字母 O 看起来像数字 0，并不存在 T0 键。现在直接显示 PB21，不会再误解。按 PB21 前，移动端必须位于电机端起点，按下后自动移动 77 mm。

`UART:WAIT` 表示还没收到合格坐标帧：

- `Rx:00000 Rj:000`：完全没收到数据。MaixCAM Pro 的 A21（TX）接 MSPM0 的 PB1（RX），两块板必须共地。A22→PB0 目前可选。
- `Rj` 增加或显示 `BAD`：收到数据，但格式或数值不合格。
- 视觉程序只有识别到小球后才发送；仅连接相机但未识别到球，仍然显示 `WAIT`。
- 正常时 MaixCAM 控制台应显示 `/dev/ttyS4 @ 115200`、`TX=A21`，OLED 随后显示 `UART:OK`。

注意：视觉代码里的旧注释写了 PA10/PA11，但本工程实际串口是 PB0/PB1，应以工程配置为准。

最新固件：[empty_LP_MSPM0G3507_nortos_keil.axf](/D:/TI/mspm0_sdk_2_02_00_05/TMC2208/keil/Objects/empty_LP_MSPM0G3507_nortos_keil.axf)

::git-stage{cwd="D:\TI\mspm0_sdk_2_02_00_05\TMC2208"}
::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\TMC2208"}

## 用户

可以通讯了，这一版代码我没有用，现在是丝杆最低的状态，在23481d2中重新写步进电机的代码，现在已经是最低，上升77mm就是平衡状态，先完成按下PB21，上升77mm功能

## 助手

明白：以 `23481d2` 的版本为基准，不沿用后来那套“当前位置标成 -77 mm 再走到 0”的写法。现在机构处于丝杆最低点，我会把启动流程改成更直观的绝对坐标：最低点记为 0，按下 PB21 后只执行一次上升 77 mm；到位后再把该位置设为平衡零点。视觉代码仍只读，先把这一项单独做稳并提交 Git。

## 助手

这次改动会直接驱动丝杆上升，我正在按电机安全检查流程核对使能默认状态、方向、100 mm 软件限位和运动超时，避免按键抖动导致重复运行。
