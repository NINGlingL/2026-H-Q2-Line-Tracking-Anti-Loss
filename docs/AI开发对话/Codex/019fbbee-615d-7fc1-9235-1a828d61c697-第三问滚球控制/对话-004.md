# 第三问滚球控制（续）

## 助手

我会把这一版明确做成“单功能验证模式”：上电时最低点为 0 mm，PB21 去抖后只允许触发一次，目标为 `77 × 800 = 61,600` 微步；到位后进入 `BALANCE READY` 并保持使能，不会因为相机已经通讯就继续启动 PID。这样你可以先单独确认机械上升方向和 77 mm 行程。

## 助手

单功能版本已经编译通过，0 错误、0 警告。当前逻辑到 77 mm 后会停脉冲但保持 TMC2208 使能以提供保持力，PB21 再按不会重复上升，相机数据也不会让电机继续动作。接下来我再做一次代码级限位与状态检查，然后提交；按你上一条安排，不替你烧录。

## 助手

已完成“PB21 按一次，从最低点上升 77 mm”的单功能版本。

- 最低点定义为绝对 `0 mm`
- PB21 按下后目标为 `61,600` 微步，即 `77 mm`
- 预计运行约 `12.53 秒`
- 15 秒未到位则关闭电机并进入故障状态
- 到位显示 `BALANCE READY`
- 到位后停止 STEP 脉冲、保持电机使能
- PB21 锁定，重复按不会再次移动
- 视觉 PID 暂时禁用，即使 UART 正常也不会驱动电机
- 物理软件限位为 `0～100 mm`
- 编译为 0 错误、0 警告
- 提交：`4d234fa feat: raise actuator 77mm from lowest point`

最新固件：[empty_LP_MSPM0G3507_nortos_keil.axf](/D:/TI/mspm0_sdk_2_02_00_05/TMC2208/keil/Objects/empty_LP_MSPM0G3507_nortos_keil.axf)

烧录后先松开 PB21，再按一次。由于没有最低限位开关，如果电机意外继续向下顶，应立即断开电机电源；按当前方向配置应从最低点向上运行。

::git-stage{cwd="D:\TI\mspm0_sdk_2_02_00_05\TMC2208"}
::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\TMC2208"}

## 用户

反了，电机向下转但是已经在最低点了，你改一下

## 助手

收到，当前“向上”的 DIR 电平定义反了。我只翻转上升方向映射，不改 77 mm 步数、速度、限位或按键逻辑；视觉代码仍不动。完成后重新编译并提交 Git，你再烧录测试。

## 助手

方向映射已翻转：现在逻辑“向上”会把 DIR 从低电平改为高电平，其余仍是单次上升 77 mm、0～100 mm 限位、15 秒超时、到位保持。编译已通过，正在做最后差异检查并提交。

## 助手

已改正方向：

- “向上”由 DIR 低电平改为高电平
- 仍是按 PB21 单次上升 77 mm
- 0～100 mm 软件限位、15 秒超时保持不变
- 到位后保持使能，视觉 PID 不启动
- 编译：0 错误、0 警告
- 提交：`f634f5d fix: reverse actuator upward direction`

最新固件：[empty_LP_MSPM0G3507_nortos_keil.axf](/D:/TI/mspm0_sdk_2_02_00_05/TMC2208/keil/Objects/empty_LP_MSPM0G3507_nortos_keil.axf)

烧录并复位后，先松开 PB21，再按下测试；这次应从最低点向上运行。

::git-stage{cwd="D:\TI\mspm0_sdk_2_02_00_05\TMC2208"}
::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\TMC2208"}

## 用户

向下降10mm

## 助手

按你当前连续调试状态处理：机构现在已经在 77 mm 平衡位。下一版上电把当前位置记为 77 mm，按 PB21 后只向下移动 10 mm，到 67 mm 停止并保持；不会回零，也不会启动视觉 PID。若机构实际不在 77 mm，请先不要按键。视觉代码仍不修改，完成后编译并提交 Git。

## 助手

下降测试逻辑已经编译通过：起点按 77 mm 记入，PB21 后目标是 67 mm，共 8,000 微步，预计约 1.81 秒；5 秒未完成会关闭电机。方向使用刚刚实机确认的“向下”电平，不改驱动方向配置。

## 助手

已改为下降 10 mm 测试：

- 当前机构必须位于 77 mm
- 上电将当前位置记为 77 mm
- 按 PB21 后下降至 67 mm
- 共运行 8,000 微步，预计约 1.81 秒
- 到位停止脉冲并保持使能
- 5 秒超时保护
- PB21 只响应一次
- 视觉 PID 保持禁用
- 编译：0 错误、0 警告
- 提交：`ecde9fa feat: lower actuator 10mm from balance`

最新固件：[empty_LP_MSPM0G3507_nortos_keil.axf](/D:/TI/mspm0_sdk_2_02_00_05/TMC2208/keil/Objects/empty_LP_MSPM0G3507_nortos_keil.axf)

如果机构当前不在 77 mm，暂时不要按 PB21，否则软件坐标与实际位置不一致。

::git-stage{cwd="D:\TI\mspm0_sdk_2_02_00_05\TMC2208"}
::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\TMC2208"}

## 用户

在下降5mm

## 助手

按连续调试状态继续：当前机构已在 67 mm。下一版上电把当前位置记为 67 mm，按 PB21 后再下降 5 mm，到 62 mm 停止并保持；其余方向、限位和视觉禁用逻辑不变。若机构实际不在 67 mm，请先不要按键。

## 助手

已改成从 67 mm 再下降 5 mm：目标 62 mm，共 4,000 微步，预计约 1 秒；仍保留 5 秒超时和单次按键锁定。编译已通过，正在提交这一轮。

## 助手

已改为从 67 mm 再下降 5 mm：

- 起点：67 mm
- 目标：62 mm
- 行程：4,000 微步
- 预计约 1 秒完成
- PB21 单次触发
- 到位保持，视觉 PID 禁用
- 编译：0 错误、0 警告
- 提交：`a2a12f8 feat: lower actuator another 5mm`

最新固件：[empty_LP_MSPM0G3507_nortos_keil.axf](/D:/TI/mspm0_sdk_2_02_00_05/TMC2208/keil/Objects/empty_LP_MSPM0G3507_nortos_keil.axf)

当前机构必须在 67 mm，再烧录并按 PB21。

::git-stage{cwd="D:\TI\mspm0_sdk_2_02_00_05\TMC2208"}
::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\TMC2208"}
