# 第三问滚球控制（续）

## 用户

记录当前位置为零点，重新审查视觉代码，检查闭环PID，回到最开始的问题，完成第三问

## 助手

我会以你现在的机械位置作为新的平衡零点，恢复完整第三问闭环：重新只读审查视觉 Python 的坐标、速度和串口协议；重新核对题面第三问；再审查并重写 MSPM0 的状态机、PID、限位和 OLED。视觉 Python 不修改，每一轮固件更改继续单独 Git 提交。

这次会依次使用 PDF/Word 读取流程核对题面，以及嵌入式硬件安全检查流程审查电机和 PID。

## 助手

题面和 Word/PDF 只用于核对要求，不会改动原文件；文档读取流程要求完整核对相关页面与内容，我正在先完成这一项，再进入代码修改。

## 助手

我会把“当前位置”作为新的机械零点：PB21 只执行清零并启动闭环，不再先移动丝杆。现在先完成题面与视觉协议复核，重点核对视觉速度的方向信息和第三问的目标顺序；视觉 Python 只读不改。随后恢复完整闭环、编译并提交这一版 Git。文档读取流程要求我把题面对应页完整检查后再落代码，我正在按这个约束执行。

## 助手

题面已核对：第三问确实是 `O → +5 cm → -5 cm`，总时限 5 秒，两个目标点最大误差绝对值不超过 1 cm。视觉协议复查也发现一个关键问题：相机发送的 `V` 是二维速度大小，恒为非负，不能直接当作一维有符号速度用于 PID 微分项；这一版会保留协议兼容，但由 MSPM0 根据连续位置和时间自行计算有符号速度。嵌入式安全审查同时要求闭环加入输出限幅、积分限幅、串口失联停机和状态超时，这些会一起落实。

## 助手

闭环逻辑现已恢复，并按视觉代码的真实坐标系修正为：相机 `0～25 cm` 先转换成 MCU 内部 `-12.5～+12.5 cm`；否则旧代码不仅会把大于 13.5 cm 的有效数据误判为坏帧，而且永远无法正确追踪 `-5 cm`。PB21 现在不移动电机，只清零并使能；驱动层限制在零点 ±10 mm，PID 再限制到 ±6 mm。接下来正在做编译和逐项安全复核。

## 助手

编译已通过（0 错误、0 警告）。安全复核结果：上电 EN 默认关闭；PB21 前无 STEP；PID 有积分分离/积分限幅、输出限幅和斜率限制；执行器入口另有 ±10 mm 硬限位；视觉数据有范围、跳变和超时检查；失联 250 ms、任务超时或数值异常都会停脉冲并关闭电机。视觉文件的 Git 对象哈希与当前提交完全一致，确认未修改。现在进行本版 Git 提交。

## 助手

第三问闭环已完成，当前工作区干净，未烧录。

核心改动：

- PB21 按下时把机构当前位置记录为电机零点，不产生预置移动。
- 按题面执行 `O → +5 cm → -5 cm`，最终稳定在 `-5 cm`，任务超时 5 秒。题面依据：:codex-file-citation{path="D:\A嵌入式\资料库\电赛资料\H题_车载平衡滚球运动控制系统.pdf" purpose="source" artifact_kind="pdf" page_number="2"} :codex-file-citation{path="D:\A嵌入式\资料库\电赛资料\H题_车载平衡滚球运动控制系统.docx" purpose="source" artifact_kind="document" page_number="2"}
- 修正视觉坐标：相机发送 `0～25 cm`，MCU 转换为以 O 点为零的 `-12.5～+12.5 cm`。
- 相机发送的 `V` 没有方向，MCU 改用连续位置计算有符号速度，避免 PID 微分项方向错误。
- PID 参数更新为 `Kp=1.80、Ki=0.04、Kd=1.40`。
- 步进速度 `12000 step/s`，加速度 `80000 step/s²`。
- PID 输出限制 ±6 mm，驱动硬限位 ±10 mm。
- UART 失联、PID 数值异常或任务超时会停机。
- OLED 显示 UART、数据年龄、球位置/速度、目标、误差、PID 输出、电机状态、任务阶段和故障码。
- 视觉 Python 哈希与 HEAD 一致，确认没有修改。

主要代码：[empty.c](D:/TI/mspm0_sdk_2_02_00_05/TMC2208/empty.c:39)、[tmc2208.c](D:/TI/mspm0_sdk_2_02_00_05/TMC2208/tmc2208.c:20)、[tmc2208.h](D:/TI/mspm0_sdk_2_02_00_05/TMC2208/tmc2208.h:31)。

测试方法：保持机构当前位置不动，把球放在 O 点，等待 OLED 显示 `UART:OK`、`P` 接近 `+0.00`，然后按一次 PB21，即清零并启动第三问。

Keil 编译：`0 Error(s), 0 Warning(s)`  
Git 提交：`f26f142 feat: complete task 3 closed-loop control from current zero`

::git-stage{cwd="D:\TI\mspm0_sdk_2_02_00_05\TMC2208"}
::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\TMC2208"}

## 用户

# Files mentioned by the user:

## main.py: C:/Users/16484/Documents/xwechat_files/wxid_dd7rk2gb74hv22_5fa6/msg/file/2026-08/main.py

## My request for Codex:
串口好像又连不上了，你看看，上面文件是视觉手的最新代码，只需阅读依照此文件来做第三问，修改你的代码吧

## 助手

我会把这个最新 `main.py` 当作唯一视觉协议依据，只读不修改；先逐项核对它的串口设备、引脚、波特率、发送格式、坐标零点和丢球行为，再对照当前 MSPM0 接收端找出为何重新变成 WAIT。因为涉及步进电机闭环，我也会按嵌入式安全审查检查失联停机和限位；改完会重新编译并单独提交 Git。

## 助手

原因已经定位：最新版视觉代码不再发送 `0～25 cm + 无符号速度`，而是直接发送以中心为零的有符号数据：

`P:-12.50～+10.50,V:+/-速度`

当前 MSPM0 仍按上一版协议处理，会拒绝负位置和负速度，还会错误地再减一次 `12.5 cm`；所以即使偶尔显示 UART 有数据，闭环坐标也会完全错误。我会改为直接接收最新版的有符号坐标/速度，并在 OLED 增加“收到字节数/完整行数”，这样能区分接线无数据和协议解析失败。

## 助手

最新版视觉还有一个容易误判成“串口断开”的行为：它只在检测到球时才发送串口帧；画面显示 `BALL LOST` 时完全不发数据，所以 OLED 会一直 `WAIT`，这不一定是接线故障。新版 OLED 诊断将显示：

- `By`：实际收到的串口字节数
- `Ln`：收到的完整文本行数
- `A/R/O`：接受帧、拒绝帧、缓冲溢出

这样烧录后可以直接判断是“相机没发”“接线没收到”还是“协议被拒绝”。实际工程接线仍是 Maix TX → PB1、共地；视觉文件里写的 PA11 是旧注释，不符合这个工程配置。

## 助手

已按最新 [main.py](C:/Users/16484/Documents/xwechat_files/wxid_dd7rk2gb74hv22_5fa6/msg/file/2026-08/main.py:453) 修改完成，视觉文件只读、未改动。

主要修正：

- 直接接收最新版坐标范围 `-12.5～+10.5 cm`，不再错误减去 `12.5 cm`。
- 允许视觉发送的正负速度。
- 融合视觉有符号速度与 MCU 位置差分速度供 PID 使用。
- OLED 增加 `By/Ln/A/R/O` 串口诊断。
- 保留 PB21 当前位置清零、±6 mm PID 输出和±10 mm硬限位。
- Keil 编译：`0 Error(s), 0 Warning(s)`。
- Git 提交：`419cd99`。

OLED 判断方法：

- `WAIT，By=00000`：完全没收到字节。确认 Maix TX 接 MSPM0 `PB1` 并共地；视觉代码中的 `PA11` 是错误的旧注释。
- `DATA`：收到字节但尚无完整换行帧。
- `BAD`：收到完整帧，但格式或范围不符合协议。
- `OK`：通信和解析都正常。
- 最新视觉只有检测到球时才发送；画面为 `BALL LOST` 时，OLED 保持 `WAIT` 属于正常现象。

修改位置：[empty.c](D:/TI/mspm0_sdk_2_02_00_05/TMC2208/empty.c:227)。未执行烧录。

::git-stage{cwd="D:\TI\mspm0_sdk_2_02_00_05\TMC2208"}
::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\TMC2208"}

## 用户

# Files mentioned by the user:

## 微信图片_20260801182336_239_65.jpg: D:/A嵌入式/资料库/电赛资料/微信图片_20260801182336_239_65.jpg

## My request for Codex:
 你自己看，第三题要实现小车在静止状态时，要求摆杆控制装置控制小球从摆杆中心点O 往+5cm
处运行，到达后折返，再运行到-5cm 处并稳定在该点附近，要求运行时间≤5s，
±5cm 处的最大误差绝对值≤1cm。你调试吧，允许使用MCP工具一键烧录调试

<image name=[Image #1] path="D:\A嵌入式\资料库\电赛资料\微信图片_20260801182336_239_65.jpg">

</image>
