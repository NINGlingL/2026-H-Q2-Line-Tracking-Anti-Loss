# 第三问滚球控制（续）

## 用户

不是你电机就不动啊

## 助手

先别继续按键。我不再盲调 PID，先直接查“按键状态、运行阶段、UART 帧数、PID 输出、目标步数、EN 状态”这几个量。电机完全不动通常不是灵敏度问题，而是程序仍停在 `READY/WAIT`、视觉帧没有触发控制，或已经进入安全状态。你之前允许探针调试，我现在用探针做只读检查；如果探针通道仍不可用，我会烧录一个不依赖 OLED 的电机自检版来隔离问题。

## 助手

探针服务仍然在调用前断开，无法读取运行变量。我现在切换为最小化电机自检：暂时绕过视觉、PID、PB21 和 OLED 控制路径，烧录后自动在当前零点附近做 `+3 mm ↔ -3 mm` 往返。它只用于确认 STEP/DIR/EN 是否真的在工作，范围仍小于 ±10 mm。若这一版仍不动，问题就在驱动使能、STEP 接线或供电；若能动，问题就在视觉/PID触发链。

## 助手

已停止操作。电机自检代码尚未提交、尚未烧录；板上仍是之前的连续 PID 版本。工作区可能保留未提交的自检改动，等你下一步指示。

## 用户

继续自建，速度

## 助手

继续电机自检。先确认刚才中断时的编译结果；通过后立即提交并烧录，烧录后会自动在当前零点附近 `+3 mm ↔ -3 mm` 往返。

## 助手

电机自检版已烧录并运行：

- 自动在当前零点附近 `+3 mm ↔ -3 mm` 往返。
- 不依赖相机、PID、PB21 或 OLED。
- 编译 `0 Error / 0 Warning`。
- 烧录 `Verify OK`。
- 提交：`d7672c6`。

现在不需要按 PB21，电机应该已经自动往返。告诉我“动了”或“仍然不动”，即可确定是控制逻辑还是硬件问题。

::git-stage{cwd="D:\TI\mspm0_sdk_2_02_00_05\TMC2208"}
::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\TMC2208"}

## 用户

不动啊

## 助手

自检版完全绕过了视觉和 PID，仍然不动，说明问题已经缩小到步进底层：要么 TIMA0 没有真正从 PA8 输出 STEP，要么 EN/DIR/供电链路有问题。下一步我会再绕过 TIMA0，直接把 PA8 改为 GPIO 手动发脉冲；这样可以快速区分“定时器配置问题”和“驱动/接线问题”。烧录后仍会限制在零点附近小范围往返。

## 助手

这版已彻底绕过定时器和 OLED：PA8 直接以 GPIO 输出约 1 kHz STEP，PA13 每 1600 步反向，PA15 保持使能。若仍不动，就可以确定不是 PID、相机、按钮、OLED 或 TIMA0，而是 EN/STEP/DIR 接线、TMC2208 供电/逻辑电平或驱动模块本身。现在编译烧录。

## 助手

GPIO 直发 STEP 自检版已烧录：

- PA8：约 1 kHz STEP 脉冲。
- PA13：每 1600 步反向。
- PA15：持续低电平使能。
- OLED、PID、相机、PB21、TIMA0 全部绕过。
- 烧录 `Verify OK`。
- 提交：`96d1ad9`。

OLED 此时不显示是正常的。电机应该自动来回运动。请立即确认“动了”还是“仍不动”。

若仍不动，故障就在硬件链路，需要测：

- PA15 是否为低电平；
- PA8 是否有脉冲；
- TMC2208 的 VM/VIO 是否供电；
- MSPM0 与驱动是否共地；
- STEP、DIR、EN 是否实际接到 PA8、PA13、PA15。

::git-stage{cwd="D:\TI\mspm0_sdk_2_02_00_05\TMC2208"}
::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\TMC2208"}

## 用户

<environment_context>
  <cwd>D:\TI\mspm0_sdk_2_02_00_05\电赛备用1\keil</cwd>
  <shell>powershell</shell>
  <current_date>2026-08-01</current_date>
  <timezone>Asia/Shanghai</timezone>
  <filesystem><workspace_roots><root>D:\TI\mspm0_sdk_2_02_00_05\电赛备用1\keil</root><root>C:\Users\16484\.codex\visualizations\2026\08\01\019fbbee-615d-7fc1-9235-1a828d61c697</root></workspace_roots><permission_profile type="disabled"><file_system type="unrestricted" /></permission_profile></filesystem>
</environment_context>

## 用户

8ae5eca回档

## 助手

我按 `8ae5eca` 的代码状态完整回档，并保留后续测试提交的历史，不会动视觉 Python。先按嵌入式硬件安全检查流程确认工作区，再回退、编译并烧录验证。

## 助手

发现 `empty.c` 现在还有一份未提交改动，我先确认它是不是刚才测试版遗留，避免把你手工改的内容覆盖。确认后会把工作树精确恢复到 `8ae5eca` 并新建一次回档提交。

## 助手

回档已完成：当前源码内容与 `8ae5eca` 完全一致，刚才那一字符的未提交改动已单独保存，没有丢失。现在编译这一版，随后按已授权方式烧录。

## 助手

已回档到 `8ae5eca` 的完整代码状态。

- 新提交：`4974246`
- 编译：0 错误、0 警告
- 烧录：成功，`Verify OK`
- 工作区：干净
- PB21：启动连续 `+5 cm / -5 cm` PID
- 未提交的单字符改动已暂存保护，未丢失

::git-stage{cwd="D:\TI\mspm0_sdk_2_02_00_05\TMC2208"}
::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\TMC2208"}

## 用户

96d1ad9改成3200反向

## 用户

96d1ad9改成3200反向，OLED卡屏幕了

## 助手

我按 `96d1ad9` 的直连脉冲测试版恢复：每段改为 3200 步，首次运动方向取反。OLED 卡住是该测试版主动跳过 OLED 初始化造成旧画面残留；我会让它启动时清屏并显示测试状态，但不在高速脉冲期间频繁刷新，避免再次拖死电机控制。先按电机固件安全流程核对后直接修改、提交、编译和烧录。
