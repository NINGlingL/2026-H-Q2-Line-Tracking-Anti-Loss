# 第三问滚球控制（续）

## 助手

确认了：`96d1ad9` 为了隔离故障，确实完全禁止了 OLED 初始化，所以屏上只会残留旧画面，看起来像卡死。我已恢复该测试架构，接下来只在每完成一段 3200 步时刷新一次 OLED，脉冲运行期间不刷新。

## 助手

已编译通过（0 错误、0 警告），代码已提交。现在烧录这版：上电自动以反方向先走 3200 步，随后每 3200 步换向；OLED 每换向一次更新 `DIR` 和 `LEG`。

## 助手

第一次烧录被 Keil 探针接口临时取消（代码和编译没有问题），我现在重新连接并再烧录一次。

## 助手

连续两次都是探针接口被占用/取消，不是固件错误。我按嵌入式调试流程检查当前探针占用；不会继续盲目重复烧录。

## 助手

已完成并提交：

- 基于 `96d1ad9` 测试版
- 每段改为 `3200` 步，首次方向反向，之后往返换向
- OLED 每完成一段才刷新，显示 `DIR` 和 `LEG`，避免高速刷新卡死
- 编译：0 错误、0 警告
- 提交：`3483ced`

暂未烧录：系统检测不到 CMSIS-DAP 探针，Keil 返回 `Internal DLL Error`。重新插拔下载器后即可烧录。

::git-stage{cwd="D:\TI\mspm0_sdk_2_02_00_05\TMC2208"}
::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\TMC2208"}

## 用户

换成4500吧，我自己烧录

## 助手

我把每段脉冲从 3200 改为 4500，并同步 OLED 文案；保持首次反向、随后往返，编译和 Git 提交，但不烧录。按电机固件安全流程核对后直接改。

## 助手

已改为每段 `4500` 步，首次方向仍为反向，OLED同步显示 `PULSE:4500`、`DIST:5.6mm`。

- 编译：0 错误、0 警告
- 提交：`927e9df`
- 未烧录

::git-stage{cwd="D:\TI\mspm0_sdk_2_02_00_05\TMC2208"}
::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\TMC2208"}

## 用户

整体上升5mm，上下速度加快然后运行上述代码

## 助手

我会让上电流程变成：先按当前确认的“反向”方向上升 5 mm（4000 步），完成后自动执行每段 4500 步的往返；脉冲速度由约 1000 步/秒提高到 2000 步/秒。OLED只在升高完成和换向时刷新，避免影响脉冲。按电机安全流程检查后修改、编译并提交，不烧录。

## 助手

代码现在按非阻塞阶段运行：先累计 4000 步上升，期间每个循环仍喂看门狗；完成后继续执行 4500 步换向往返。这样不会因为一次性移动 5 mm 导致看门狗复位。正在编译确认。

## 助手

已完成：

- 上电先上升 `5 mm`（4000步）
- 随后自动运行每段 `4500` 步往返
- 上下速度提升至约 `2000步/秒`
- OLED显示升高阶段、速度、方向和往返次数
- 编译：0错误、0警告
- 提交：`ac1db33`
- 按你的要求未烧录

::git-stage{cwd="D:\TI\mspm0_sdk_2_02_00_05\TMC2208"}
::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\TMC2208"}

## 用户

反了啊

## 助手

明白，是“上升5 mm”的方向写反了。按硬件安全流程修正为：先用另一方向上升4000步，完成后再切回之前确认的反向，执行4500步往返；速度保持2000步/秒，不烧录。

## 助手

已把5 mm升高方向反转修正：

- 先按正确方向上升4000步
- 到位后切回原来的反向，运行4500步往返
- 速度保持2000步/秒
- 编译：0错误、0警告
- 提交：`d2c65fa`
- 未烧录

::git-stage{cwd="D:\TI\mspm0_sdk_2_02_00_05\TMC2208"}
::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\TMC2208"}
