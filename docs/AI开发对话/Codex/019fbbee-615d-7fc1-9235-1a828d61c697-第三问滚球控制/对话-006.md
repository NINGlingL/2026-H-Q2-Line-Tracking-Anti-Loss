# 第三问滚球控制（续）

## 助手

我看到了，问题不是“串口没连上”：屏幕已经是 `UART:OK`，但随后进入了 `SAFE / FAULT:04`。按当前故障码，04 是视觉数据在闭环启动后超过 250 ms 未更新，所以电机被安全关闭；照片里 `A:043ms` 说明拍照瞬间又有新帧，但故障状态是锁存的，不会自行恢复。

这次我会使用你授权的探针烧录调试：先读取探针调试流程并检查现场固件变量，再修正“视觉偶发断帧立即锁死”的策略和任务启动条件，烧录后观察 UART 帧计数、状态机、目标、PID 输出和电机位置。视觉 `main.py` 仍然只读。

## 助手

我重新对照了照片里的故障码：这里 `FAULT:04` 实际是“到达 +5 cm 超时”，不是串口掉线。证据很明确：`UART:OK`、延迟 43 ms、拒收 0；但目标是 `+5.00`，小球却跑到 `-10.76`，电机位置也饱和到 `-4800`。闭环控制方向反了。我先只修正这个方向，保留行程限位和超时保护，然后编译、提交，再用探针烧录。

## 助手

这次不会再改步进电机底层的“上/下”定义，只反转“球的位置误差 → 电机位移”的闭环映射。这样 PB21 零点、软限位 ±10 mm、失联停机都保持不变，改动范围最小。现在开始做探针环境检查；烧录前我还会先确认探针和芯片身份。

## 助手

方向修正已通过 Keil 编译：`0 Error / 0 Warning`。探针检查也通过，当前识别到唯一一只 `CMSIS-DAP_LU (C251:F001)`。我先按你的要求把这一版单独提交，再进行芯片连接与烧录，避免调试过程中代码版本混淆。

## 助手

探针本身已被系统识别，但内置调试通道第一次调用时返回了“连接通道关闭”，还没触碰芯片、更没有烧写。我会先恢复调试服务并再次做只读识别；若目标芯片不能被该通道明确识别，我不会盲写。

## 助手

内置 MCP 通道连续两次都在调用前断开，属于调试服务传输故障；但工程自身已经明确锁定 `MSPM0G3507`、唯一 CMSIS-DAP 探针以及对应的 128 KB 官方烧写算法。新固件上电后仍处于 `WAIT_ZERO` 且电机禁用，所以我改用同一工程的 Keil 下载通道烧录，烧录完成前不会启动电机。

## 助手

已经烧录成功，日志确认 `Erase Done / Programming Done / Verify OK`。现在请把球放在中心 O 附近，然后短按一次 PB21；这一版正误差会驱动电机向正步数方向。请先不要用手扶球，拍下运行过程或至少拍一张最终 OLED，我会依据 `B / V / E / O / St / FAULT` 继续实调。等待你操作时，我继续审查 5 秒状态机和 PID，排除下一处逻辑问题。

## 助手

已修正并烧录：

- 照片中的 `FAULT:04` 是“到达 +5 cm 超时”，串口实际正常。
- 闭环电机方向已反转；目标 `+5 cm` 时，OLED 的 `St` 应先变为正数。
- Keil 编译：`0 Error / 0 Warning`。
- 烧录验证：`Programming Done / Verify OK`。
- 保留 ±10 mm 软限位、视觉失联停机等保护。
- 视觉 [main.py](C:/Users/16484/Documents/xwechat_files/wxid_dd7rk2gb74hv22_5fa6/msg/file/2026-08/main.py) 未修改。
- 已提交：`c4424e2 fix: reverse task 3 closed-loop motor mapping`

现在请将球放在 O 点附近，短按一次 PB21，拍摄完整运动视频或最终 OLED。重点看球是否先到 `+5`、再到 `-5`，以及 `FAULT、B、V、St`，我再根据实测调整 PID。

::git-stage{cwd="D:\TI\mspm0_sdk_2_02_00_05\TMC2208"}
::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\TMC2208"}

## 用户

不是我之前不是有一个设置好的平衡点吗，每次程序重启都恢复到那个平衡点，而且OLED容易卡死

## 用户

不是我之前不是有一个设置好的平衡点吗，每次程序重启都恢复到那个平衡点，而且OLED容易卡死按下按钮是开始PID

## 助手

明白：PB21 不再负责“记录零点”，而只负责启动第三问 PID；上电后机构应先回到之前标定好的平衡位置，OLED 即使总线异常也不能拖死主控制循环。我先从 Git 里找回你之前那版平衡点的准确步数，并检查 OLED 驱动的阻塞位置，再按工程现有配置改。由于这是电机上电自动运动，我会同时保留行程和超时保护。

## 助手

Git 记录确认你最后标定的平衡点是：先到 77 mm，再下降 10 mm 和 5 mm，最终为距离最低端 `62.0 mm`（`49600` 微步）。我会把它恢复为固件固定基准：上电直接把当前位置按 62 mm 平衡位接管并保持，PB21 只发出“开始 PID”命令，不再改零点。  
需要说明一点：系统没有限位开关或编码器，因此冷上电时无法检测真实绝对位置；固件只能以“机械当前仍在已标定的 62 mm 处”为前提接管，不能安全地盲走 62 mm。这个限制我会写进代码并用 ±10 mm 软限位保护。OLED 方面已经找到根因：一次刷新连续发送 3 页，且一次瞬态失败后永久停止刷新；我会改成每次 1 页并允许有界恢复。

## 助手

状态机已经改成：`上电保持平衡位 → READY → 按 PB21 → 等视觉 → 执行 O→+5→-5`。PB21 不再清零，也不会在上电后自动启动 PID。OLED 刷新从每次 3 页降为 1 页，并在瞬态 I²C 故障后按约 1 秒间隔恢复，避免显示故障拖慢闭环。现在进行编译和静态安全检查。

## 助手

第二次编译通过，仍为 `0 Error / 0 Warning`。OLED 硬卡线时的恢复也改成首次超时立即退出，不再连续等待 9 次，因此即使屏幕或接线异常，UART/PID 主循环仍能继续。接下来按你的版本规则先提交这一版，再烧录。

## 助手

已修改、提交并烧录成功：

- 固定平衡基准恢复为 `62.0 mm`。
- 上电立即接管当前位置并使能保持。
- PB21 现在只启动 PID，不再设置零点。
- OLED 每次只刷新一页；总线异常会自动恢复，不再拖死主循环。
- 编译 `0 Error / 0 Warning`，烧录 `Verify OK`。
- 视觉 `main.py` 未修改。
- Git 提交：`c179624`

当前 OLED 应显示：

```text
BALANCE:62.0MM HOLD
M:IDLE Ph:READY
PRESS PB21 START PID
```

确认机构仍处于此前标定的 62 mm 位置后，放球到 O 点并按一次 PB21，即开始 `O → +5 cm → -5 cm`。

注意：硬件没有限位开关或绝对编码器，因此断电时如果手动改变了机构位置，软件无法测出真实的 62 mm；本版不会盲目移动撞击丝杆。

::git-stage{cwd="D:\TI\mspm0_sdk_2_02_00_05\TMC2208"}
::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\TMC2208"}

## 用户

# Files mentioned by the user:

## 微信图片_20260801184807_240_65.jpg: D:/A嵌入式/资料库/电赛资料/微信图片_20260801184807_240_65.jpg

## My request for Codex:
不是来回滚动吗？为什么不动呢？

<image name=[Image #1] path="D:\A嵌入式\资料库\电赛资料\微信图片_20260801184807_240_65.jpg">

</image>

## 助手

照片已经说明原因：它确实开始执行了，但随后触发 `FAULT:03`（视觉数据短时中断），所以安全逻辑立即关闭了电机；`UART:OK` 是相机后来又恢复发送，但故障已经锁存。另一个关键证据是 `St:+04800` 时球到了负坐标，说明上一版闭环方向判断仍然反了。  
我会同时修两点：恢复正确的负向电机映射；把视觉瞬断改为“先停步并等待恢复，持续失联才关机”，避免一次漏检就永久停机。这个调整涉及电机失联保护，我会按嵌入式安全检查约束保留硬限位和最终停机。

## 助手

这一版的失联策略是两级保护：超过 `150 ms` 没有新坐标就立即停止 STEP、保持当前位置；在 `700 ms` 内重新识别会自动继续，超过 `700 ms` 才进入 `FAULT:03` 并断开电机。这样不会因为相机偶发漏掉几帧就锁死，同时真正丢球仍会停机。现在编译验证。

## 助手

编译通过：`0 Error / 0 Warning`。改动只涉及闭环方向和视觉瞬断保护，PID 参数、±6 mm 输出限制、驱动层 ±10 mm 硬限位均未放宽。我先提交，再烧录到当前板子。

## 助手

还有一个必须先处理的实机状态：照片里故障停机时电机停在 `+4800` 步，也就是比原平衡点高了 `6 mm`。刚才重新烧录后位置计数会清零，但机械不会自动回去；如果此时直接按 PB21，PID 的调节范围会偏掉。先不要按按钮。我现在用一次性恢复固件让它下降 6 mm 回到原 62 mm 平衡点，完成后立即换回正常启动固件。
