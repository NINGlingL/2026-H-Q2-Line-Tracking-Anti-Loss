# 第三问滚球控制

> 来源：Codex 019fbbee-615d-7fc1-9235-1a828d61c697；仅保留可见的用户/助手文本。

## 用户

<recommended_plugins>
Here is a list of plugins that are available but not installed.

- Atlassian Rovo (atlassian-rovo@openai-curated-remote)
- Box (box@openai-curated-remote)
- Figma (figma@openai-curated-remote)
- Gmail (gmail@openai-curated-remote)
- Google Calendar (google-calendar@openai-curated-remote)
- Google Drive (google-drive@openai-curated-remote)
- Notion (notion@openai-curated-remote)
- Outlook Calendar (outlook-calendar@openai-curated-remote)
- Outlook Email (outlook-email@openai-curated-remote)
- SharePoint (sharepoint@openai-curated-remote)
- Teams (teams@openai-curated-remote)
</recommended_plugins>

<environment_context>
  <cwd>D:\TI\mspm0_sdk_2_02_00_05\电赛备用1\keil</cwd>
  <shell>powershell</shell>
  <current_date>2026-08-01</current_date>
  <timezone>Asia/Shanghai</timezone>
  <filesystem><workspace_roots><root>D:\TI\mspm0_sdk_2_02_00_05\电赛备用1\keil</root><root>C:\Users\16484\.codex\visualizations\2026\08\01\019fbbee-615d-7fc1-9235-1a828d61c697</root></workspace_roots><permission_profile type="disabled"><file_system type="unrestricted" /></permission_profile></filesystem>
</environment_context>

## 用户

""C:\Users\16484\Documents\xwechat_files\wxid_dd7rk2gb74hv22_5fa6\msg\file\2026-08\main - 副本(1).py""这个是视觉手重新写的代码，优化加入了速度，你仔细阅读，然后就直接做
  PID吧，根据D:\A嵌入式\资料库\电赛资料\H题*车载平衡滚球运动控制系统.pdf D:\\A嵌入式\\资料库\\电赛资料\\H题*车载平衡滚球运
  动控制系统.docx，先把第三问写出来吧，只需要写视觉和步进电机之间调水管小球，然后步进电机的平衡位置是电机端离最近的移动端6.5cm处，项目地址在D:\TI\mspm0_sdk_2_02_00_05\TMC2208\keil下，我之前已经让人写过了，先git一下，每次代码更迭都要git，你仔细阅读并更改，硬件配置如下，现在我准备使用一块MSP M0 G3507来和maixcam
  pro进行串口通讯来解析小球的坐标，，关于步进电机型号是28HS30-0604A-013，丝杆直径6mm 螺距2mm 行程100mm T6 杆端一边是合页另一边和水管直接连接，你先阅读一下视觉手的代码，完成要求

## 助手

我会先把视觉代码、题目第三问、现有工程和 Git 历史对齐，再在现有架构里加入“视觉坐标 → PID → 步进电机”的闭环控制。这个任务涉及 PDF/Word 题面和电机执行器固件，我会同时按 PDF/文档读取规范与嵌入式硬件安全审查规范来做；每一轮实际代码修改后都会单独提交 Git。

## 助手

规范已加载。接下来我会先确认题目文件的真实文件名与页数、视觉串口协议、工程当前分支和已有控制逻辑；在没有确认现有引脚和定时器配置前不会直接覆盖生成代码。安全规范还要求闭环输出限幅、视觉数据超时失能和步数/行程保护，我会把这些作为 PID 实现的一部分。

## 助手

已确认视觉端串口格式是 `P:<位置cm>,V:<带符号速度cm/s>\n`，20 Hz；位置标定为管道中心 0 cm、左右各 12.5 cm。目标工程目前还没有 Git 仓库，而且 Keil 目录下只有工程文件，实际源码需要继续从上级目录定位。我会先做现状基线提交，再开始任何源码更改。

## 助手

基线已提交（`b040f04`）。我也找到现有实现里的关键错位：旧固件仍按 `0~25 cm、中心 12.5 cm` 解析，而新视觉已经改成 `-12.5~+12.5 cm、中心 0 cm`；另外现有代码会超时后继续下一阶段、默认循环运行，并且视觉丢失时仍保持电机使能，这三点都不符合第三问和闭环安全要求。下一步我会先修正协议/状态机/PID，再收紧步进电机的中性位与行程边界。

## 用户

？视觉py代码只能阅读不能修改，继续做题

## 助手

明白，视觉 Python 只作为协议参考，不能改。我刚才把视觉手的新版本同步进工程副本了，这是越界操作；现在会立即用一个新的 Git 提交完整撤销，保留清晰历史，然后只改 MSPM0 的 C 工程。

## 助手

视觉文件已经原样恢复并提交（`9f13c0c`），后面不会再碰。现在只在 MSPM0 端做：按新协议把中心改为 `0 cm`，加入带速度反馈的 PID、到点稳定判据、5 秒总超时、最终停在 `-5 cm`，以及视觉丢失/异常数据时立即停止并禁用步进驱动。中性机械位置会明确按“移动端距电机端 65 mm”设为相对零点。

## 助手

第一版 MSPM0 闭环已经能在 Keil/ArmClang 下通过编译：0 错误、0 警告。电机现在上电默认禁用，只有收到合格视觉帧后才使能；异常帧会被丢弃，连续丢失 250 ms、阶段超时或 PID 数值异常都会进入不可自恢复的安全停机。接下来我会做一次动力学仿真检查 5 秒轨迹，再根据结果调整首组保守 PID 参数并提交这一轮 Git。

## 助手

安全审查还发现一个只能靠硬件保证的点：TMC2208 的 `ENN` 是低有效，软件已改成上电先拉高，但主控复位瞬间仍应有外部上拉。我会核对驱动芯片官方资料确认该引脚内部默认状态，再决定是否必须在接线说明里补一个电阻要求；这不会改视觉代码。

## 助手

我又核对出一处接线不一致：视觉代码写的是 MSPM0 `PA10(TX)/PA11(RX)`，现有工程却配置成了 `PB0/PB1`。按你给出的硬件配置，我会把 MSPM0 串口改到 PA10/PA11；否则 Python 发得对，板子也收不到。TMC2208 的官方资料还确认 `ENN` 没有内部上拉，所以模块若不自带上拉，PA15/ENN 必须补 10k~20k 到 VIO。

## 用户

按照工程的配置来

## 助手

明白，以现有工程的 SysConfig 为准：UART0 保持 `PB0(TX)/PB1(RX)`，不按视觉代码注释改成 PA10/PA11。视觉端只需把线接到工程实际引脚即可；我现在撤回刚才的引脚调整，再完成编译和 Git 提交。

## 助手

MSPM0 端这一轮已经提交（`f09dbea`），工作区干净，视觉 Python 的内容哈希与基线完全一致。Keil 完整构建通过：0 错误、0 警告；UART 保持工程原配置 `PB0/PB1`。我正在做最后一遍安全清单和关键参数定位，随后给你直接可烧录结论与首次上机注意点。

## 助手

第三问 PID 已完成，视觉 Python 保持原样未修改。

- 实现 `0 → +5 cm → -5 cm → 稳定保持`，总超时 5 秒，依据题面要求。:codex-file-citation{path="D:\A嵌入式\资料库\电赛资料\H题_车载平衡滚球运动控制系统.pdf" purpose="source"} :codex-file-citation{path="D:\A嵌入式\资料库\电赛资料\H题_车载平衡滚球运动控制系统.docx" purpose="source" artifact_kind="document" page_number="2"}
- UART 按工程配置：`PB0=TX`、`PB1=RX`、115200 8N1。
- 初始 PID：`Kp=1.20, Ki=0.04, Kd=2.60`，输出限制 ±6 mm。
- 水平零点按移动端距电机端 65 mm 设置。
- 包含积分分离、输出变化率限制、视觉数据量程/跳变检查。
- 视觉中断超过 250 ms、PID 异常或阶段超时会关闭电机。
- 加入 100 mm 物理行程限制、上电默认禁用和 2 秒独立看门狗。
- Keil 构建结果：0 错误、0 警告。

主要代码：[empty.c](D:/TI/mspm0_sdk_2_02_00_05/TMC2208/empty.c:28)、[tmc2208.c](D:/TI/mspm0_sdk_2_02_00_05/TMC2208/tmc2208.c:158)、[empty.syscfg](D:/TI/mspm0_sdk_2_02_00_05/TMC2208/empty.syscfg)。

可烧录文件：[固件 HEX](D:/TI/mspm0_sdk_2_02_00_05/TMC2208/keil/Objects/empty_LP_MSPM0G3507_nortos_keil.hex)。

首次上电前：

- 断电时将移动端手动调到距电机端 65 mm。
- MaixCAM TX 接 PB1，必须共地。
- 先低幅确认电机方向；方向相反时修改 `MOTOR_POSITIVE_LIFTS_END`。
- TMC2208 的 ENN 没有内部上拉；若模块板不自带，需要加 10k～20k 上拉到 VIO。[官方数据手册](https://www.analog.com/media/en/technical-documentation/data-sheets/TMC2202_TMC2208_TMC2224_datasheet_rev1.14.pdf)

Git 提交：`f09dbea`，分支：`codex/pid-ball-balance`。

::git-create-branch{cwd="D:\TI\mspm0_sdk_2_02_00_05\TMC2208" branch="codex/pid-ball-balance"}
::git-stage{cwd="D:\TI\mspm0_sdk_2_02_00_05\TMC2208"}
::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\TMC2208"}

## 用户

你要在OLED显示相关参数啊，串口是否成功，步进电机是否启动，啥的

## 助手

可以。我会把 OLED 改成一屏实时诊断：串口状态/数据年龄、步进电机 OFF/IDLE/MOVE、任务阶段、球位置与目标、速度、PID 输出、当前步数、有效帧/拒绝帧，并让 PID 参数自动轮换显示。视觉 Python 不会修改；这一轮改完会重新编译并单独 Git 提交。嵌入式安全审查会继续约束状态显示，避免为了刷新 OLED 阻塞闭环。

## 助手

我会按现有工程的引脚和外设配置补上 OLED 诊断页：串口接收状态、数据新鲜度、步进电机 OFF/IDLE/MOVE、任务阶段、PID 实时量与参数都会显示；视觉 Python 只读取核对，不做任何修改。这次涉及电机控制，我也会按嵌入式硬件安全检查清单复核，并在修改后编译、核对视觉文件哈希、提交 Git。
