# UART与OLED验证（续）

## 助手

90	    s_hal->delay_ms(ms);
91	}
92	
93	/* ==========================================================================
94	 * 传感器基础功能
95	 * ========================================================================== */
96	
97	int MPU6050_Init(const mpu6050_hal_t *hal, const MPU6050_Config_t *config)
98	{
99	    uint8_t who_am_i;
100	    MPU6050_Config_t cfg;
101	
102	    /* 保存 HAL 指针 */
103	    s_hal = hal;
104	
105	    /* 加载配置 (NULL = 默认值) */
106	    if (config != NULL) {
107	        cfg = *config;
108	    } else {
109	        MPU6050_Config_t def = MPU6050_CONFIG_DEFAULT;
110	        cfg = def;
111	    }
112	    s_i2c_addr  = cfg.i2c_addr;
113	    s_accel_sens = accel_sens_by_range(cfg.accel_range);
114	    s_gyro_sens  = gyro_sens_by_range(cfg.gyro_range);
115	
116	    /* 唤醒 MPU6050 */
117	    MPU6050_WriteReg(MPU6050_REG_PWR_MGMT_1, 0x00);
118	    s_hal->delay_ms(10);
119	
120	    /* 验证设备 ID */
121	    who_am_i = MPU6050_ReadWhoAmI();
122	    if (who_am_i != 0x68 && who_am_i != 0x70) {
123	        return -1;
124	    }
125	
126	    /* 采样率: Fs =...
[/external_agent_tool_result]

## 助手

[external_agent_tool_call: TaskUpdate]
input: {"status":"completed","taskId":"11"}
[/external_agent_tool_call]

## 助手

[external_agent_tool_result]
Updated task #11 status
[/external_agent_tool_result]

## 助手

[external_agent_tool_call: Glob]
input: {"path":"D:\\TI\\mspm0_sdk_2_02_00_05","pattern":"**/mpu6050_hal*"}
[/external_agent_tool_call]

## 助手

[external_agent_tool_result]
No files found
[/external_agent_tool_result]

## 助手

[external_agent_tool_call: Grep]
input: {"pattern":"mpu6050_hal_t","path":"D:\\TI\\mspm0_sdk_2_02_00_05"}
[/external_agent_tool_call]

## 助手

[external_agent_tool_result]
Found 2 files
mpu6050.c
mpu6050.h
[/external_agent_tool_result]

## 助手

好的！MPU6050 驱动使用的是 **平台无关的 HAL 层**，需要我创建 `mpu6050_hal.h`（HAL 接口定义）+ MSPM0 的 I2C 实现。

---

## MPU6050 接线

你的 `.syscfg` 已经配好了 **I2C_1** (PA10=SDA, PA11=SCL)，正好给 MPU6050 用：

| MSPM0G3507 | MPU6050 |
|:-:|:-:|
| **PA10** (I2C1 SDA) | SDA |
| **PA11** (I2C1 SCL) | SCL |
| 3.3V | VCC |
| GND | GND |
| 3.3V | AD0 (地址=0x68) |

> SSD1306 用 I2C_0 (PA0/PA1)，MPU6050 用 I2C_1 (PA10/PA11)，互不冲突。

---

现在我来创建 HAL 层文件和集成代码。需要创建：

1. `mpu6050_hal.h` — HAL 接口结构体定义
2. MSPM0 的 I2C1 读写实现
3. 在 `empty.c` 中初始化 MPU6050 + OLED 显示姿态数据

## 用户

不是，先别动代码，那个HAL层是谁那么一丝

## 用户

不是，先别动代码，那个HAL层是什么意思

## 助手

**HAL = Hardware Abstraction Layer（硬件抽象层）**

这个 MPU6050 驱动 (`mpu6050.c/h`) 是一个**通用驱动**，它不依赖任何特定 MCU。所有跟硬件打交道的操作——I2C 读写、延时、计时——都是通过一个叫 `mpu6050_hal_t` 的结构体来完成的，里面存的是**函数指针**：

```c
typedef struct {
    void     (*i2c_write)(...);       // I2C 写一个寄存器
    uint8_t  (*i2c_read)(...);        // I2C 读一个寄存器
    void     (*i2c_read_burst)(...);  // I2C 连续读多个寄存器
    void     (*delay_ms)(...);        // 毫秒延时
    uint32_t (*millis)(void);         // 获取系统毫秒时间戳
} mpu6050_hal_t;
```

**打个比方**：驱动就像一个万能遥控器，HAL 就是遥控器底部的电池仓——不管你是南孚还是松下（MSPM0 还是 STM32），只要装上对应的电池（实现这 5 个函数），遥控器就能工作。

所以针对 MSPM0G3507，我需要用 DriverLib 来实现这 5 个函数：

| HAL 函数 | MSPM0 实现方式 |
|:-:|:-:|
