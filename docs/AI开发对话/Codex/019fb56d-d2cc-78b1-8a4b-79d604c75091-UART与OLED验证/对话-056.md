# UART与OLED验证（续）

## 助手

43	    while (!(DL_I2C_getControllerStatus(MPU6050_INST) & DL_I2C_CONTROLLER_STATUS_IDLE));
44	
45	    /* 读取1字节数据 */
46	    DL_I2C_startControllerTransfer(MPU6050_INST, MPU6050_ADDR,
47	        DL_I2C_CONTROLLER_DIRECTION_RX, 1);
48	
49	    while (!(DL_I2C_getControllerStatus(MPU6050_INST) & DL_I2C_CONTROLLER_STATUS_BUSY_BUS));
50	    while (!(DL_I2C_getControllerStatus(MPU6050_INST) & DL_I2C_CONTROLLER_STATUS_IDLE));
51	
52	    val = DL_I2C_receiveControllerData(MPU6050_INST);
53	
54	    return val;
55	}
56	
57	/* 读取多个连续寄存器 */
58	static void MPU6050_ReadRegs(uint8_t reg, uint8_t len, uint8_t *buf)
59	{
60	    /* 发送起始寄存器地址 */
61	    while (!(DL_I2C_getControllerStatus(MPU6050_INST) & DL_I2C_CONTROLLER_STATUS_IDLE));
62	
63	    DL_I2C_fillControllerTXFIFO(MPU6050_INST, &reg, 1);
64	    DL_I2C_startControllerTransfer(MPU6050_INST, MPU6050_ADDR,
65	        DL_I2C_CONTROLLER_DIRECTION_TX, 1);
66	
67	    while (!(DL_I2C_getControllerStatus(MPU6050_INST) & DL_I2C_CONTROLLER_STATUS_BUSY_BUS));
68	    while (!(DL_I2C_getControllerStatus(MPU6050_INST) & DL_I2C_CONTROLLER_STATUS_IDLE));
69	
70	    /* 读取 len 字节数据 */
71	    DL_I2C_startControllerTransfer(MPU6050_INST, MPU6050_ADDR,
72	        DL_I2C_CONTROLLER_DIRECTION_RX, len);
73	
74	    while (!(DL_I2C_getControllerStatus(MPU6050_INST) & DL_I2C_CONTROLLER_STATUS_BUSY_BUS));
75	    while (!(DL_I2C_getControllerStatus(MPU6050_INST) & DL_I2C_CONTROLLER_STATUS_IDLE));
76	
77	    for (uint8_t i = 0; i < len; i++) {
78	        buf[i] = DL_I2C_receiveControllerData(MPU6050_INST);
79	    }
80	}
81	
82	/* ================ 功能接口 ================ */
83	
84	int MPU6050_Init(void)
85	{
86	    uint8_t who_am_i;
87	
88	    /* 唤醒 MPU6050, 取消睡眠模式 */
89	    MPU6050_WriteReg(MPU6050_REG_PWR_MGMT_1, 0x00);
90	    delay_ms(10);
91	
92	    /* 验证设备ID */
93	    who_am_i = MPU6050_ReadWhoAmI();
94	    if (who_am_i != 0x68 && who_am_i != 0x70)  /* 兼容 MPU6050 & MPU6500 */
95	        return -1;
96	
97	    /* 采样率分频: 采样率 = 1kHz / (1 + SMPLRT_DIV), 0x00 = 1kHz */
98	    MPU6050_WriteReg(MPU6050_REG_SMPLRT_DIV, 0x00);
99	
100	    /* 低通滤波: DLPF_CFG=3, ~44Hz 带宽 */
101	    MPU6050_WriteReg(MPU6050_REG_CONFIG, 0x03);
102	
103	    /* 陀螺仪量程: ±2000dps，快速直角转弯不会因量程饱和漏积分。 */
104	    MPU6050_WriteReg(MPU6050_REG_GYRO_CONFIG, MPU6050_GYRO_2000DPS);
105	    s_gyro_sens = MPU6050_GYRO_SENS_2000;
106	
107	    /* 加速度计量程: ±2g */
108	    MPU6050_WriteReg(MPU6050_REG_ACCEL_CONFIG, MPU6050_ACCEL_2G);
109	    s_accel_sens = MPU6050_ACCEL_SENS_2G;
110	
111	    /* 电源管理: 使用 X 轴陀螺仪时钟, 更稳定 */
112	    MPU6050_WriteReg(...
[/external_agent_tool_result]

## 助手

[external_agent_tool_call: Read]
file: D:/A嵌入式/资料库/MPU/2026TI_MPU6050_Yaw-main/MYCode_v7/Board/board.h
[/external_agent_tool_call]

## 助手

[external_agent_tool_result]
1	
2	#ifndef __BOARD_H__
3	#define __BOARD_H__
4	
5	#include "ti_msp_dl_config.h"
6	
7	#ifndef u8
8	#define u8 uint8_t
9	#endif
10	
11	#ifndef u16
12	#define u16 uint16_t
13	#endif
14	
15	#ifndef u32
16	#define u32 uint32_t
17	#endif
18	
19	#ifndef u64
20	#define u64 uint64_t
21	#endif
22	
23	int LOG_Debug_Out(const char* __file, const char* __func, int __line, const char* format, ...);
24	
25	#define LOG_D(fmt, ...) \
26	    do { \
27	        LOG_Debug_Out(__FILE__, (const char*)__func__, __LINE__, fmt, ##__VA_ARGS__); \
28	    } while (0)
29	
30	
31	
32	/* 使用可变参数是实现的类printf函数 */
33	int lc_printf(char* format,...);
34	
35	/* UART3 串口 (PB2=TX, 115200, SYSCFG 已配好) */
36	void uart_send_byte(char c);
37	void uart_send_str(const char *s);
38	int  uart_printf(const char *fmt, ...);
39	
40	/* 延时函数 */
41	void delay_us(int __us);
42	void delay_ms(int __ms);
43	
44	void delay_1us(int __us);
45	void delay_1ms(int __ms);
46	
47	/* 1ms SysTick 时间基准：供姿态解算取得真实调用间隔。 */
48	void Board_TimebaseInit(void);
49	uint32_t Board_Millis(void);
50	
51	#endif
52	
[/external_agent_tool_result]
