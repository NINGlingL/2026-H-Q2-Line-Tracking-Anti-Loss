# UART与OLED验证（续）

## 助手

67	    float roll;         // 横滚角 (度)
68	    float pitch;        // 俯仰角 (度)
69	} MPU6050_Attitude_t;
70	
71	/* 欧拉角结构体 (Mahony 四元数输出) */
72	typedef struct {
73	    float roll;         // 横滚角 (度), -180~+180
74	    float pitch;        // 俯仰角 (度), -90~+90
75	    float yaw;          // 航向角 (度), -180~+180
76	} EulerAngles_t;
77	
78	/* 校准参数结构体 */
79	typedef struct {
80	    int16_t accel_x_offset;
81	    int16_t accel_y_offset;
82	    int16_t accel_z_offset;  // Z轴校准后应为1g对应的原始值
83	    int16_t gyro_x_offset;
84	    int16_t gyro_y_offset;
85	    int16_t gyro_z_offset;
86	} MPU6050_Calib_t;
87	
88	/**
89	 * @brief 初始化 MPU6050
90	 * @return 0 成功, -1 失败
91	 */
92	int MPU6050_Init(void);
93	
94	/**
95	 * @brief 读取 WHO_AM_I 寄存器
96	 * @return 设备ID, 正常应为 0x68
97	 */
98	uint8_t MPU6050_ReadWhoAmI(void);
99	
100	/**
101	 * @brief 读取原始加速度数据 (16位有符号)
102	 * @param ax 加速度X原始值
103	 * @param ay 加速度Y原始值
104	 * @param az 加速度Z原始值
105	 */
106	void MPU6050_ReadAccelRaw(int16_t *ax, int16_t *ay, int16_t *az);
107	
108	/**
109	 * @brief 读取原始陀螺仪数据 (16位有符号)
110	 * @param gx 陀螺仪X原始值
111	 * @param gy 陀螺仪Y原始值
112	 * @param gz 陀螺仪Z原始值
113	 */
114	void MPU6050_ReadGyroRaw(int16_t *gx, int16_t *gy, int16_t *gz);
115	
116	/**
117	 * @brief 读取原始温度数据 (16位有符号)
118	 * @return 温度原始值
119	 */
120	int16_t MPU6050_ReadTempRaw(void);
121	
122	/**
123	 * @brief 读取转换后的全部数据 (浮点, 带单位)
124	 * @param data 存放数据的结构体指针
125	 */
126	void MPU6050_ReadData(MPU6050_Data_t *data);
127	
128	/**
129	 * @brief 向 MPU6050 写入单字节寄存器
130	 * @param reg 寄存器地址
131	 * @param val 写入值
132	 */
133	void MPU6050_WriteReg(uint8_t reg, uint8_t val);
134	
135	/**
136	 * @brief 从 MPU6050 读取单字节寄存器
137	 * @param reg 寄存器地址
138	 * @return 读取值
139	 */
140	uint8_t MPU6050_ReadReg(uint8_t reg);
141	
142	/**
143	 * @brief 上电自动校准零偏 (传感器需保持静止水平放置)
144	 * @param samples 校准采样次数, 默认200
145	 * @note 陀螺仪零偏取均值减去, 加速度X/Y零偏取均值减去,
146	 *       Z轴以1g为参考修正
147	 */
148	void MPU6050_Calibrate(uint16_t samples);...
[/external_agent_tool_result]

## 助手

[external_agent_tool_call: Read]
file: D:/A嵌入式/资料库/MPU/2026TI_MPU6050_Yaw-main/MYCode_v7/BSP/src/mpu6050.c
[/external_agent_tool_call]

## 助手

[external_agent_tool_result]
1	#include "mpu6050.h"
2	#include "board.h"
3	#include <math.h>
4	
5	/* 参考 24_H 工程使用 ±2000dps，避免小车快速旋转时 ±250dps 饱和丢角。 */
6	static float s_accel_sens = MPU6050_ACCEL_SENS_2G;
7	static float s_gyro_sens  = MPU6050_GYRO_SENS_2000;
8	
9	/* 校准参数 */
10	static MPU6050_Calib_t s_calib = {0};
11	static float s_gyro_z_bias_raw = 0.0f;
12	
13	/* ================ I2C 底层读写 ================ */
14	
15	void MPU6050_WriteReg(uint8_t reg, uint8_t val)
16	{
17	    uint8_t txData[2];
18	    txData[0] = reg;
19	    txData[1] = val;
20	
21	    while (!(DL_I2C_getControllerStatus(MPU6050_INST) & DL_I2C_CONTROLLER_STATUS_IDLE));
22	
23	    DL_I2C_fillControllerTXFIFO(MPU6050_INST, txData, 2);
24	    DL_I2C_startControllerTransfer(MPU6050_INST, MPU6050_ADDR,
25	        DL_I2C_CONTROLLER_DIRECTION_TX, 2);
26	
27	    while (!(DL_I2C_getControllerStatus(MPU6050_INST) & DL_I2C_CONTROLLER_STATUS_BUSY_BUS));
28	    while (!(DL_I2C_getControllerStatus(MPU6050_INST) & DL_I2C_CONTROLLER_STATUS_IDLE));
29	}
30	
31	uint8_t MPU6050_ReadReg(uint8_t reg)
32	{
33	    uint8_t val;
34	
35	    /* 发送寄存器地址 */
36	    while (!(DL_I2C_getControllerStatus(MPU6050_INST) & DL_I2C_CONTROLLER_STATUS_IDLE));
37	
38	    DL_I2C_fillControllerTXFIFO(MPU6050_INST, &reg, 1);
39	    DL_I2C_startControllerTransfer(MPU6050_INST, MPU6050_ADDR,
40	        DL_I2C_CONTROLLER_DIRECTION_TX, 1);
41	
42	    while (!(DL_I2C_getControllerStatus(MPU6050_INST) & DL_I2C_CONTROLLER_STATUS_BUSY_BUS));
