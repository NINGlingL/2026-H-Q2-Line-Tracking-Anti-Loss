# UART与OLED验证（续）

## 助手

[external_agent_tool_call: Write]
file: D:\TI\mspm0_sdk_2_02_00_05\UART\keil\mpu6050_hal_mspm0.c
[/external_agent_tool_call]

## 助手

[external_agent_tool_result]
File created successfully at: D:\TI\mspm0_sdk_2_02_00_05\UART\keil\mpu6050_hal_mspm0.c (file state is current in your context — no need to Read it back)
[/external_agent_tool_result]

## 助手

现在更新 `empty.c`，集成 MPU6050 + YAW 显示：

## 助手

[external_agent_tool_call: Write]
file: D:\TI\mspm0_sdk_2_02_00_05\UART\empty.c
[/external_agent_tool_call]

## 助手

[external_agent_tool_result: error]
<tool_use_error>File has been modified since read, either by the user or by a linter. Read it again before attempting to write it.</tool_use_error>
[/external_agent_tool_result]

## 助手

[external_agent_tool_call: Read]
file: D:\TI\mspm0_sdk_2_02_00_05\UART\empty.c
[/external_agent_tool_call]

## 助手

[external_agent_tool_result]
1	/*
2	 *  自检 + UART1(PA10/PA11) 轮询接收显示
3	 *  不用中断，纯轮询，避免浮空引脚 ISR 风暴
4	 */
5	#include "ti_msp_dl_config.h"
6	#include "ssd1306.h"
7	#include "moto.h"
8	#include <stdio.h>
9	#include <string.h>
10	
11	/* 显示缓冲: 6行 x 21字符 */
12	#define DL 6
13	#define DC 21
14	static char     g_d[DL][DC + 1];
15	static uint8_t  g_ln = 0;
16	static uint8_t  g_cl = 0;
17	static uint32_t g_rx = 0;
18	
19	/* ========== 自检回调 ========== */
20	static void SelfTest_OLED(uint8_t phase, uint8_t speed, uint8_t sec)
21	{
22	    char s[22];
23	    SSD1306_Clear();
24	    if (phase == 0) {
25	        SSD1306_ShowString(3, 24, "TEST DONE");
26	    } else {
27	        SSD1306_ShowString(1, 24, "SELF TEST");
28	        snprintf(s, sizeof(s), "Phase: %u/3", phase); SSD1306_ShowString(3, 0, s);
29	        snprintf(s, sizeof(s), "Speed: %u%%", speed); SSD1306_ShowString(4, 0, s);
30	        snprintf(s, sizeof(s), "Time:  %us", sec);   SSD1306_ShowString(5, 0, s);
31	        SSD1306_ShowString(7, 0, "Forward ->");
32	    }
33	    SSD1306_Update();
34	}
35	
36	/* ========== 显示字符 ========== */
37	static void DispChar(char c)
38	{
39	    if (c == '\n' || c == '\r') {
40	        g_d[g_ln][g_cl] = '\0';
41	        g_ln = (g_ln + 1) % DL;
42	        g_cl = 0;
43	        memset(g_d[g_ln], ' ', DC); g_d[g_ln][DC] = '\0';
44	        return;
45	    }
46	    if (c < ' ' || c > '~') return;
47	    if (g_cl >= DC) {
48	        g_d[g_ln][DC] = '\0';
49	        g_ln = (g_ln + 1) % DL;
50	        g_cl = 0;
51	        memset(g_d[g_ln], ' ', DC); g_d[g_ln][DC] = '\0';
52	    }
53	    g_d[g_ln][g_cl++] = c;
54	}
55	
56	/* ========== OLED 显示 (手动转数字, 不用 snprintf) ========== */
57	static void OLED_Show(void)
58	{
59	    SSD1306_Clear();
60	
61	    /* 手动显示 RX 计数 */
62	    SSD1306_ShowString(0, 0, "RX:");
63	    SSD1306_ShowNum(0, 24, (int32_t)g_rx, 6);
64	
65	    SSD1306_DrawHLine(0, 8, 128, 1);
66	
67	    for (int i = 0; i < DL; i++) {
68	        uint8_t ln = (g_ln + 1 + i) % DL;
69	        SSD1306_ShowString(2 + i, 0, g_d[ln]);
70	    }
71	    SSD1306_Update();
72	}
73	
74	/* ========== main ========== */
75	int main(void)
76	{
77	    SYSCFG_DL_init();
78	    SSD1306_Init();
79	    Moto_Init();
80	
81	    /* 清缓冲 */
82	    for (int i = 0; i < DL; i++) {
83	        memset(g_d[i], ' ', DC); g_d[i][DC] = '\0';
84	    }
85	
86	    /* 开机自检 */
87	    Moto_SelfTest(SelfTest_OLED);
88	
89	    /*
90	     * 配置 UART1: PA10(TX) PA11(RX), 纯轮询不用中断
91	     */
92	    DL_GPIO_initPeripheralInputFunctionFeatures(
93	        GPIO_UART_1_IOMUX_RX, GPIO_UART_1_IOMUX_RX_FUNC,
94	        DL_GPIO_INVERSION_DISABLE, DL_GPIO_RESISTOR_PULL_UP,
95	        DL_GPIO_HYSTERESIS_DISABLE, DL_GPIO_WAKEUP_DISABLE);
96	
97	    DL_UART_Main_disable(UART_1_INST);
98	    DL_UART_Main_configBaudRate(UART_1_INST, CPUCLK_FREQ, 9600);
99	    DL_UART_Main_enable(UART_1_INST);
100	    /* 不开中断, 不使能 NVIC */
