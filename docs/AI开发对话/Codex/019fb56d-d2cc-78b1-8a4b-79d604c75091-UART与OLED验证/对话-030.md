# UART与OLED验证（续）

## 助手

| 3 | 分隔线 |
| 4 | PWM0/PWM1 占空比值 |
| 5 | GPIOB 引脚状态 (十六进制) |
| 6 | UART 参数 `9600 8N1` |
| 7 | 最后收到的指令 |

## 硬件接线

| MSPM0G3507 | L298N | HC-05 | SSD1306 |
|:-:|:-:|:-:|:-:|
| PA12 (TIMG0 CCP0) | ENA | | |
| PA13 (TIMG0 CCP1) | ENB | | |
| PB16 (AIN1) | IN1 | | |
| PB15 (AIN2) | IN2 | | |
| PB2 (BN1) | IN3 | | |
| PB3 (BIN2) | IN4 | | |
| PA22 (UART2 RX) | | TX | |
| PA21 (UART2 TX) | | RX | |
| PA0 (I2C0 SDA) | | | SDA |
| PA1 (I2C0 SCL) | | | SCL |

编译下载后，开机车轮会转 1 秒确认程序运行，然后 OLED 显示就绪界面。手机通过蓝牙发送 `F`/`B`/`L`/`R` 等指令即可遥控小车。

## 用户

显然，你把串口又改错了，OLED显示RX 4294967295 电机正常启动自检包括T 0s

## 助手

问题很清楚：RX=4294967295 (即 `0xFFFFFFFF`) 是因为 `snprintf` 的 `%lu` 格式在 MSPM0 上有问题，以及 UART 被重复配置造成了寄存器混乱。让我修复这两个问题：

## 助手

[external_agent_tool_call: Read]
file: D:\TI\mspm0_sdk_2_02_00_05\UART\empty.c
[/external_agent_tool_call]

## 助手

[external_agent_tool_result]
1	/*
2	 *  ============ HC-05 Bluetooth Car Control + SSD1306 OLED ============
3	 *  MSPM0G3507 + HC-05 (UART2) + SSD1306 (I2C0) + L298N Motor Driver
4	 *
5	 *  蓝牙控制指令:
6	 *    F/f = 前进    B/b = 后退    L/l = 左转    R/r = 右转
7	 *    S/s = 停止    +/-  = 加/减速
8	 *    0-9  = 速度档位 (0%=停, 1=10%, ..., 9=90%)
9	 *
10	 *  引脚:
11	 *    UART2: RX=PA22, TX=PA21 (HC-05)
12	 *    I2C0:  SDA=PA0,  SCL=PA1  (SSD1306 OLED)
13	 *    PWM:   TIMG0 CCP0=PA12, CCP1=PA13 (L298N ENA/ENB)
14	 *    GPIO:  AIN1=PB16, AIN2=PB15 (Motor A 方向)
15	 *           BN1=PB2,  BIN2=PB3  (Motor B 方向)
16	 */
17	#include "ti_msp_dl_config.h"
18	#include "ssd1306.h"
19	#include <string.h>
20	#include <stdio.h>
21	
22	/* ========== PWM 参数 ========== */
23	#define PWM_PERIOD  1000U
24	
25	/* ========== 全局状态 ========== */
26	static volatile char     g_direction    = 'S';   /* F/B/L/R/S */
27	static volatile uint8_t  g_speed        = 50;    /* 速度 0~100% */
28	static volatile uint32_t g_rx_total     = 0;
29	static volatile uint32_t g_tick_ms      = 0;
30	static          char     g_last_cmd[21] = "None";
31	
32	/* ========== 环形接收缓冲区 ========== */
33	#define RX_BUF_SIZE 64
34	static volatile char    g_rx_buf[RX_BUF_SIZE];
35	static volatile uint8_t g_rx_head = 0;
36	static volatile uint8_t g_rx_tail = 0;
37	
38	/* ========== 电机控制 ========== */
39	
40	static void Motor_SetDirection(char dir)
41	{
42	    /* 先清除所有方向引脚 */
43	    DL_GPIO_clearPins(GPIOB, AIN2_PIN_3_PIN | AIN1_PIN_2_PIN |
44	                              BIN2_PIN_0_PIN | BN1_PIN_1_PIN);
45	
46	    switch (dir) {
47	    case 'F': /* 前进: A正转 + B正转 */
48	        DL_GPIO_setPins(GPIOB, AIN1_PIN_2_PIN | BN1_PIN_1_PIN);
49	        break;
50	    case 'B': /* 后退: A反转 + B反转 */
51	        DL_GPIO_setPins(GPIOB, AIN2_PIN_3_PIN | BIN2_PIN_0_PIN);
52	        break;
53	    case 'L': /* 左转: 右轮正转 */
54	        DL_GPIO_setPins(GPIOB, BN1_PIN_1_PIN);
55	        break;
56	    case 'R': /* 右转: 左轮正转 */
57	        DL_GPIO_setPins(GPIOB, AIN1_PIN_2_PIN);
58	        break;
59	    default:  /* 停止 */
60	        break;
61	    }
62	    g_direction = dir;
63	}
64	
65	static void Motor_SetSpeed(uint8_t pct)
66	{
67	    if (pct > 100) pct = 100;
68	
69	    uint32_t duty = ((uint32_t)pct * PWM_PERIOD) / 100U;
70	
71	    DL_TimerG_setCaptureCompareValue(PWM_0_INST, duty,
72	        GPIO_PWM_0_C0_IDX);
73	    DL_TimerG_setCaptureCompareValue(PWM_0_INST, duty,
74	        GPIO_PWM_0_C1_IDX);
75	
76	    g_speed = pct;
77	}
78	
79	static void Motor_Stop(void)
80	{
81	    DL_TimerG_setCaptureCompareValue(PWM_0_INST, 0,
82	        GPIO_PWM_0_C0_IDX);
83	    DL_TimerG_setCaptureCompareValue(PWM_0_INST, 0,
84	        GPIO_PWM_0_C1_IDX);
85	
86	    DL_GPIO_clearPins(GPIOB, AIN2_PIN_3_PIN | AIN1_PIN_2_PIN |
87	                              BIN2_PIN_0_PIN | BN1_PIN_1_PIN);
88	    g_direction = 'S';
89	    g_speed     = 0;
90	}
91	
92	/* ========== 蓝牙指令处理 ========== */
93	
94	static void Cmd_Process(char cmd)
95	{
