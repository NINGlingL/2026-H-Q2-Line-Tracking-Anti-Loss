# 通用驱动库（续）

## 助手

| 取模方式 | 列行式 |
| 字节序 | 高位在前 |

### 第 2 步：把数据放进 font.c

```c
// 加到 font.c 末尾 (bilibiliImg 后面)

const uint8_t smileData[] = {
    0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
    0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
    0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
    0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
};

const Image smileImg = { 16, 16, smileData };
```

### 第 3 步：在 font.h 中声明

```c
extern const Image smileImg;
```

### 第 4 步：在 main.c 中调用

```c
OLED_NewFrame();
OLED_DrawImage(56, 24, &smileImg, OLED_COLOR_NORMAL);  // 屏幕中央画16×16笑脸
OLED_ShowFrame();
```

---

## 一个工程里可以同时放很多资源

`font.c` 就像一个资源仓库，你可以往里堆任意多的东西：

```
font.c 文件结构：
├── ascii_8x6[]         ← 内置 ASCII 8×6 字模
├── ascii_12x6[]        ← 内置 ASCII 12×6 字模
├── ascii_16x8[]        ← 内置 ASCII 16×8 字模
├── ascii_24x12[]       ← 内置 ASCII 24×12 字模
├── zh16x16[][]         ← 你的中文字库 (自己加)
├── bilibiliData[]      ← 内置示例图片
├── smileData[]         ← 你的图标1
├── heartData[]         ← 你的图标2
├── logoData[]          ← 你的 logo
└── ...
```

---

## API 对比一览

| 你想显示的 | 取模工具选 | 存到 font.c 的类型 | 调用函数 |
|-----------|-----------|-------------------|---------|
| 英文字符 | 不需要（内置） | 不用改 | `OLED_PrintASCIIString()` |
| 中文字符 | 16×16 中文取模 | `zh16x16[][]` + 改 `font16x16` | `OLED_PrintString()` |
| 任意图片/图标 | 图片取模 | `Image` 结构体 | `OLED_DrawImage()` |

---

所以回答你的问题：**是的，取模 → 存 font.c → 调用 OLED_DrawImage / OLED_PrintString → 屏幕上就出来了。** 不管是文字还是图案，流程完全一样。

## 用户

那你现在把之前的内容擦除，在OLED屏幕上画那个示例图案

## 助手

[external_agent_tool_call: Read]
file: D:\A嵌入式\32project\01-IDE\Core\Src\main.c
[/external_agent_tool_call]

## 助手

[external_agent_tool_result]
1	/* USER CODE BEGIN Header */
2	/**
3	  ******************************************************************************
4	  * @file           : main.c
5	  * @brief          : SSD1306 OLED 诗词动画 — 静夜思 (李白)
6	  * @author         : 基于通用驱动库 + STM32CubeIDE
7	  ******************************************************************************
8	  * @attention
9	  *
10	  * 接线: SSD1306 → STM32F103C8T6
11	  *       VCC → 3.3V   GND → GND
12	  *       SCL → PB6    SDA → PB7
13	  *
14	  * 首次使用请将 TEST_WITH_ASCII 设为 1，用拼音验证硬件。
15	  * 硬件验证通过后，参考 font.c 末尾说明生成中文字模，
16	  * 再将 TEST_WITH_ASCII 改为 0，即可显示中文诗句。
17	  *
18	  ******************************************************************************
19	  */
20	/* USER CODE END Header */
21	/* Includes ------------------------------------------------------------------*/
22	#include "main.h"
23	#include "i2c.h"
24	#include "tim.h"
25	#include "usart.h"
26	#include "gpio.h"
27	
28	/* Private includes ----------------------------------------------------------*/
29	/* USER CODE BEGIN Includes */
30	#include "ssd1306.h"
[/external_agent_tool_result]

## 助手

[external_agent_tool_call: Read]
file: D:\A嵌入式\32project\01-IDE\Core\Inc\font.h
[/external_agent_tool_call]

## 助手

[external_agent_tool_result]
66	
67	/**
68	 * @brief 图片结构体
69	 * @note  图片数据可使用波特律动 LED 取模助手生成
70	 */
71	typedef struct {
72	    uint8_t        w;     /**< 图片宽度 (像素) */
73	    uint8_t        h;     /**< 图片高度 (像素) */
74	    const uint8_t *data;  /**< 图片数据 (列行式排列) */
75	} Image;
76	
77	/** bilibili 图标示例 */
78	extern const Image bilibiliImg;
79	
80	#ifdef __cplusplus
