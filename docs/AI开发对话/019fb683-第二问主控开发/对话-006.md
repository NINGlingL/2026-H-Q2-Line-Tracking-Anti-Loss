# 对话记录 第 6 部分

会话 ID：`019fb683-28ab-7eb3-adbd-3c44aa83ff61`

## 100. 助手

- OLED 显示 `YAW:+123.4`，同时保留八路状态
- 上电静止校准约 2 秒，显示 `YAW:CAL 0/100`
- 加入低通滤波、真实采样周期积分、静止零速抑制及温漂零偏微调
- 蓝牙发送 `G` 可重新校准并将当前方向归零
- YAW 为相对航向角，不是磁北绝对角度
- 电机继续硬锁
- Keil 编译：0 错误、0 警告
- 未烧录、未使用 MCP
- Git 提交：`122f481`

::git-stage{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}
::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}

## 101. 用户

启用电机，和蓝牙模块指令控制

## 102. 助手

好，这版开启电机硬件门控，并启用现有蓝牙单字符控制：`F/B/L/R`、`S` 停车、`+/-` 调速。电机仍保留 80% 限幅、低电压保护、30 秒诊断超时和上电默认停止；只编译，不烧录。按嵌入式安全检查，我会先核对这些保护路径再提交。

## 103. 助手

电机门控已开启。现有控制逻辑会在收到运动指令后先检查电池采样有效且高于 9.9V，再拉高 PB14/STBY；上电、`S`、低电压、超时或看门狗复位都会回到停止状态。现在只做 Keil 编译。

## 104. 助手

检查通过：PB14 上电先拉低，PWM 先置零；蓝牙运动指令只有电池检测正常时才生效，驱动层仍有 ±800‰ 硬限幅。Keil 编译为 0 错误、0 警告，准备提交。

## 105. 助手

已启用电机和 HC-05 蓝牙控制。

蓝牙单字符指令：

- `F`：前进
- `B`：后退
- `L`：原地左转
- `R`：原地右转
- `S`：立即停车
- `+/-`：每次增减 5% 功率
- `D`：进入手动诊断模式
- `A`：进入自动循迹
- `G`：陀螺仪重新校准、YAW 归零
- `?`：返回指令帮助

默认功率 25%，最大 80%；低于 9.9V、ADC异常或运行超过30秒会自动停车。

Keil 编译：0 错误、0 警告。未烧录、未使用 MCP。  
Git 提交：`a8b8a1d`

::git-stage{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}
::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}

## 106. 用户

你这个蓝牙模块不对啊，为什么我发G返回G但是OLED屏幕上依旧只有八路寻迹模块和YAW角呢？

## 107. 助手

原因找到了：`G` 在当前程序里是“陀螺仪校准/YAW 归零”，不是电机指令；手机端显示回来的 `G` 很可能只是发送框本地回显，因为固件此前没有发送确认信息。OLED 也只画了 4 行传感器数据，根本没有画蓝牙状态。

我现在补上两件事：蓝牙返回明确的 `ACK + 实际执行结果`，OLED 增加最近指令、模式和左右轮输出。仍然不烧录。因为会涉及电机命令路径，我会保留安全检查和失败时的 `LOCK` 回执。

## 108. 助手

现在 `G` 会由固件明确回复类似 `ACK G GYRO CAL MODE:SAFE`；如果电池/硬件条件不允许运动，`F/B/L/R` 会回复 `LOCK:HW/BAT`，不会假装执行。OLED 新增第 5、6 行显示最近蓝牙命令和实际左右轮输出。正在编译验证。

## 109. 助手

编译通过：0 错误、0 警告。现在 OLED 会显示 `BT:GYRO CAL/FWD/...` 和 `M:DIAG L:25 R:25`；蓝牙端收到的是带 `ACK` 的整行，能和手机本地回显区分开。准备只提交本次两个源码文件，不碰你当前 Keil/SysConfig 生成的改动。

## 110. 助手

你说得对，之前的现象不能证明蓝牙回传正常：

- `G` 原本是陀螺仪校准，不是电机命令。
- 单独显示的 `G` 很可能是手机软件的本地回显。
- 旧版 OLED 没有蓝牙状态区域。

现已修正：

- 发送 `G` 应收到：`ACK G GYRO CAL MODE:SAFE`
- 发送 `F` 应收到：`ACK F FWD MODE:DIAG`
- 若电池或硬件条件不满足，会返回 `LOCK:HW/BAT`
- OLED 第5行显示最近蓝牙指令
- OLED 第6行显示模式及左右轮实际输出
- `+/-` 会立即修改当前运动功率

Keil 编译：0 错误、0 警告。未烧录。  
Git 提交：`08df161`

你手动下载新版后，如果仍然只有单独的 `G`、没有 `ACK`，就检查：

- HC-05 TX → PB16（主控RX）
- HC-05 RX → PB15（主控TX）
- 手机串口软件关闭“本地回显”

::git-stage{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}
::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}

## 111. 用户

蓝牙指令还是没有作用没有办法控制电机，OLED能够显示蓝牙状态：BT:BOOT  SAFE M:SAFE L:0 R:0，发送G没有陀螺仪校准，发送f电机不转

## 112. 助手

这个现象已经能定位：OLED 一直是 `BT:BOOT SAFE`，说明主控根本没有收到任何蓝牙字节；手机显示的字符仍是本地回显，所以不是电机或 `G` 指令逻辑的问题。

我会先把蓝牙接收从中断方式简化成主循环轮询，绕开 UART2 中断可能未触发的问题，并在 OLED 上增加实际接收计数 `RX`。这样新版里只要字节进了 PB16，计数一定会增加。仍然不烧录。

## 113. 助手

新版判断标准会很直观：

- `RX` 从 0 增加：PB16 已收到数据，软件会处理指令。
- `RX` 始终为 0：就是接线方向或 HC-05 波特率/工作模式问题，不再是电机逻辑问题。
- 收到 `f` 后应显示 `BT:FWD`；如果显示 `LOCK:HW/BAT`，再看同一行的电池电压。

现在进行编译。

## 114. 助手

编译已通过：0 错误、0 警告。接收改为轮询后，不再依赖 UART2 中断向量；电机保护路径没有放宽。现在提交这版诊断修复。

## 115. 助手

问题根因是主控没有收到 PB16 上的蓝牙数据，不是电机指令失效。现已改成主循环直接轮询 UART2，并增加 OLED 诊断：

- `RX:0`：没有收到字节
- 发送 `G` 后：`RX` 增加，显示 `BT:GYRO CAL`
- 发送 `f` 后：显示 `BT:FWD`、`M:DIAG`
- 同时显示电池电压，便于判断电机是否被低压保护锁定

请手动下载新版后测试。若 `RX` 仍为0，检查：


