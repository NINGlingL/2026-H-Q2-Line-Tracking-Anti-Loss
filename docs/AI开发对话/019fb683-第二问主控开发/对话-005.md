# 对话记录 第 5 部分

会话 ID：`019fb683-28ab-7eb3-adbd-3c44aa83ff61`

## 76. 助手

这轮我会按嵌入式硬件安全检查和调试流程修改、编译、自动 Git、烧录，再直接读取运行状态。

## 77. 助手

我找到了显示形态异常的直接软件原因：当前初始化选择了“水平寻址模式”，刷新代码却按“页寻址模式”写屏，两种模式不匹配，确实会出现整条、整块或错位显示。项目里那份备用驱动也明确使用页寻址模式。

我现在会统一为页寻址，并用软件配置内部弱上拉；同时把整屏刷新拆成逐页刷新，避免 OLED 长时间占住主循环。

## 78. 助手

软件版已经编译通过：0 错误、0 警告。内部弱上拉是通过运行时重新配置 IOMUX 实现的，避开了 SysConfig 对开漏上拉的界面限制；寻址模式已改为页模式，刷新也改成每次只发一页。现在提交这一版并烧录验证。

## 79. 助手

运行态发现了一个新的软件时序问题：内部弱上拉启用后，传感器失败重试时间变长，初始化阶段超过了当前 500 ms 看门狗周期，程序在到达 OLED 初始化前反复复位。没有硬件故障异常，也没有发生 HardFault。

我会把独立看门狗改为 4 秒，主循环仍在末尾喂狗；这样保留失控保护，同时允许上电完成 I²C 重试。

## 80. 助手

还有一个关键软件问题已经抓到：Keil 启动文件只分配了 256 字节栈，而当前 I²C 恢复、格式化 OLED 文本和控制调用链已经把栈压进 OLED 状态与显存区域；这正会造成“矩形亮块/一条线/状态莫名变化”。

我会把栈扩大到 2 KB。MSPM0G3507 有足够 SRAM，这不会影响电机安全门，也比继续调字体更根本。

## 81. 助手

栈修复后诊断值已经稳定：OLED 状态没有再被覆盖，但硬件 I²C 仍停在 `IDLE + BUS BUSY`，说明 MSPM0 在 I²C 外设复用模式下确实没有启用内部上拉。

既然限定只能软件处理，我会把 OLED 改为 GPIO 模拟开漏 I²C：拉低时输出 0，拉高时切回带内部上拉的输入，绝不主动输出高电平。这样无需外部电阻，也绕开硬件 I²C 对内部上拉的限制。ICM 暂时仍保持安全失效，不影响先把 OLED 调正常。

## 82. 用户

你能不能不要这么严谨，你让我先跑起来啊别用MCP了，你给我简化

## 83. 助手

可以。停止用 MCP，也不再卡在 OLED/IMU 上。

我现在给你做“先跑版”：OLED 和陀螺仪都改成可选，失败也不阻塞；按键直接进入八路巡线 + 右轮编码器速度环；开放 STBY，但保留 80% PWM、低电压和失线停车保护。只用 Keil 编译、烧录、自动 Git。

## 84. 助手

“先跑版”已编译成功，0 错误、0 警告。现在 OLED/IMU 不参与启动和循迹，也不会再拖慢主循环；按键循迹只依赖八路模块、右轮编码器和电池保护。接下来直接提交并用 Keil 烧录，不再做在线调试。

## 85. 用户

不允许烧录

## 86. 助手

收到，立即停止烧录；后续只修改和编译，不再向单片机写入。刚才的烧录进程已经启动，我现在马上终止并核对它是否已经结束。

## 87. 助手

已停止我启动的 Keil 烧录进程；原本你已打开的另一个 Keil 进程没有动。日志显示本次在进入下载前就发生 `Debug access failed`，随后 `Target DLL has been cancelled`，所以这次没有成功写入单片机。

## 88. 助手

已停止，之后除非你明确要求，否则绝不烧录，也不再使用 MCP。

- 本次下载失败于建立调试连接之前，没有写入单片机。
- 板上仍是上一版 `091be36`，电机保持锁定。
- 简化循迹版仅完成编译和 Git 提交：`ace4bbb`。
- 用户自己的未跟踪文件均未修改。

::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}

## 89. 用户

你先让改屏幕实时显示八路寻迹状态

## 90. 助手

好，先只做屏幕：实时显示 `x1~x8`、原始位图、偏差位置、有效通道数和通信帧计数。不烧录、不用 MCP。

我会按嵌入式安全检查把这版电机重新锁住，避免你手动下载后按键误启动；OLED 和八路串口保留运行。

## 91. 助手

我会把这版做成“纯监视器”：电机强制锁定，屏幕只显示 8 路状态；同时把 OLED 改为软件 I²C，直接用芯片内部上拉，适配你现在没有外接上拉的接法。只编译检查，不下载。

## 92. 助手

界面已经收敛为 4 行：通道号、8 位原始状态、黑线命中图、位置/帧统计；电机编译期锁死。现在只做 Keil 编译检查。

## 93. 助手

编译已通过：0 错误、0 警告。安全锁也核对了：`PB14/STBY` 始终保持关闭，按键和蓝牙命令都不能启动电机。接下来只提交这 3 个改动文件。

## 94. 助手

已完成并提交。

OLED 实时显示：

- `CH:12345678`：通道编号
- `ST:01010101`：八路原始状态
- `LN:#..##...`：`#` 表示检测到黑线
- 位置、有效探头数量、通信帧与错误统计交替显示

同时：

- OLED 改用软件 I²C + 内部上拉
- 电机硬锁，PB14/STBY 不会开启
- Keil 编译：0 错误、0 警告
- 未烧录、未使用 MCP
- Git 提交：`d4f46a4`

::git-stage{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}
::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}

## 95. 用户

好的，现在引入陀螺仪模块，显示YAW角，优化滤波算法

## 96. 助手

好，这一版我会启用 ICM20948，在 OLED 上显示实时 YAW，并把静止零偏校准、低通滤波和漂移抑制合并进现有读取链路。仍然只编译、不烧录；电机继续锁住。由于这是带执行器的嵌入式项目，我会按硬件安全检查保留现有电机隔离。

## 97. 助手

现有资料确认模块能提供陀螺仪数据，但 6 轴算法无法用重力修正 YAW，所以我会把它做成“相对航向角”：上电静止约 2 秒自动定零，Z 轴低通、静止零速抑制、在线零偏微调，再按真实采样间隔积分。这样更适合小车转弯控制，也不会伪装成磁北绝对航向。

## 98. 助手

滤波和相对 YAW 已接入；另外发现主程序此前没有直接包含功能开关头文件，导致 OLED/IMU 初始化开关可能被当成未定义。我一并修正了这个入口问题。现在只做编译验证。

## 99. 助手

最终编译仍是 0 错误、0 警告。YAW 显示精度为 0.1°，蓝牙发送 `G` 会重新静止校准并把当前方向归零；通信超时、跳变和量程检查仍保留，电机继续硬锁。现在做本版 Git 提交。

## 100. 助手

已完成并提交。

- 启用 ICM20948，PB2/PB3 使用软件 I²C + 内部上拉

