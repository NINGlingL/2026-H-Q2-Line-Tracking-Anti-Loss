# 对话记录 第 22 部分

会话 ID：`019fb683-28ab-7eb3-adbd-3c44aa83ff61`

## 331. 助手

这是我上一版把“连续静止”条件设得过严造成的，校准样本被反复清零，所以卡在 `YAW:CAL`。我现在撤掉这个严格门槛，恢复为对100个合法陀螺仪样本直接平均；约2秒必然完成，同时保留按键启动时 `YAW=0`。只编译，不烧录。

## 332. 助手

修正后编译通过：0 错误、0 警告。现在校准计数只会从 `0` 单向增加到 `100`，不会因一帧振动又退回 `0`；正常约2秒结束。其他刚完成的终点防误判和1.2秒丢线恢复逻辑保持不变。正在提交这一版。

## 333. 助手

已修复 `YAW:CAL` 卡死：

- 校准改回100个合法样本直接平均。
- 计数只会从 `0` 增加到 `100`，不再因振动清零。
- 正常约2秒完成并显示实际 `YAW`。
- 终点防误判、停车优化和1.2秒丢线恢复保持不变。

Keil 编译：`0 Error(s), 0 Warning(s)`，未烧录。

提交：`7903edc fix: prevent gyro calibration from stalling`

::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}

## 334. 用户

直接IMU ERR，D:\A嵌入式\资料库\20948\ICM20948_WE资料在这里

## 335. 助手

我会直接对照这套 `ICM20948_WE` 资料检查寄存器配置、量程和 I²C时序，重点确认为什么驱动从 `CAL` 变成 `IMU ERR`。先只读资料和源码诊断，找到原因后修改、编译，不烧录。由于涉及 IMU辅助电机控制，我会同时保留失效降级和电机限幅。

## 336. 助手

我已经定位到这里不是“校准卡住”，而是 IMU 初始化或连续读取阶段直接失败。接下来按你给的 ICM20948_WE 源码核对底层读时序，重点修复无外部上拉时最容易出问题的 I²C 速度、重复起始和读取重试；同时把 OLED 的 `IMU ERR` 改成能区分地址/芯片 ID/失败次数的提示。只编译验证，不烧录。

## 337. 助手

对照参考库后发现两个实际差异：参考库读取寄存器使用“重复起始”，现有代码却在寄存器地址和读取数据之间先发了 STOP；而且当前板上没有外部上拉，原来的软件 I²C 仍按约 100 kHz 跑，容错偏低。另一个程序问题是 IMU 初始化结果随后被 `Control_Init` 清零，所以 OLED 丢失了真实芯片 ID。我现在一起修正，并加入自动重新识别。

## 338. 助手

底层修复已经写入；现在做一次 Keil 全量编译和静态检查。这里不会调用下载按钮，也不会连接或复位单片机。编译完成后我还会只提交本次两个源文件，保留你现有的 SysConfig/Keil 本地改动。

## 339. 助手

已修复并提交，提交号 `faa2fe1`。

主要修改：

- 软件 I²C 降至约 50 kHz，适应无外部上拉的情况。
- 按 ICM20948_WE 改用重复起始读取时序。
- 每次读取最多重试 3 次，启动后重新识别 5 次。
- IMU 晚上电或短暂断线后会自动重新初始化。
- 不再清除初始化得到的芯片 ID。
- OLED 错误改为 `IMU ERR ID:xx E:x`。
- Keil 编译通过：`0 Error(s), 0 Warning(s)`。
- 未烧录，电机限幅和失控保护没有改变。

烧录后正常流程应该是：

`YAW:CAL 0/100` → `YAW:CAL 100/100` → 显示 YAW。

如果仍报错：

- `ID:EA`：已经识别到 ICM20948，属于后续通信不稳定。
- `ID:00` 或 `ID:FF`：主要是 SDA/SCL、电源、共地或者上拉问题。
- `E:x`：累计通信失败次数。

相关修改在 [icm20948.c](D:/TI/mspm0_sdk_2_02_00_05/电赛备用2/icm20948.c:32) 和 [control.c](D:/TI/mspm0_sdk_2_02_00_05/电赛备用2/control.c:814)。

::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}

