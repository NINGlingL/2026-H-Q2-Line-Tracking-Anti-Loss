# 对话记录 第 3 部分

会话 ID：`019fb683-28ab-7eb3-adbd-3c44aa83ff61`

## 37. 助手


- PB14可以直接接STBY，TB6612FNG内部有约200kΩ下拉。:codex-file-citation{path="D:/A嵌入式/资料库/【WHEELTEC】TB6612电机驱动附送资料/【WHEELTEC】TB6612电机驱动附送资料_V3.2_2026.07.25/6.芯片手册/TB6612FNG芯片手册.pdf" purpose="source"}
- 右轮为A通道时，编码器应使用模块 `E1A/E1B → PB10/PB11`
- 模块、MSPM0和传感器必须共地
- 20kHz PWM位于模块允许的0–100kHz范围内
- 模块输入允许4.5–15V，单路连续电流不超过1.2A，禁止堵转。:codex-file-citation{path="D:/A嵌入式/资料库/【WHEELTEC】TB6612电机驱动附送资料/【WHEELTEC】TB6612电机驱动附送资料_V3.2_2026.07.25/1.用户手册与使用教程/电机驱动模块使用手册—TB6612(2025.08.27).pdf" purpose="source"} :codex-file-citation{path="D:/A嵌入式/资料库/【WHEELTEC】TB6612电机驱动附送资料/【WHEELTEC】TB6612电机驱动附送资料_V3.2_2026.07.25/TB6612稳压版随货手册.pdf" purpose="source"}

固件电压换算已改为1/11，并修复了原先可能出现的32位计算溢出。Keil构建为 **0 errors、0 warnings**，Git提交为：

`ca9004d fix: use D153C onboard battery divider`

请再确认两点：你的板子丝印是否为 `D153C`，以及电池标签是几串或充满电压是否为 **12.6V**。确认3S后，我就设置低压阈值并解除电机软件锁。

::git-stage{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}
::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}

## 38. 用户

D:\A嵌入式\资料库\20948\ICM20948_WE陀螺仪的相关资料

## 39. 助手

我会用这套 ICM20948_WE 资料核对当前驱动的设备地址、寄存器分组、量程、轴向符号、启动校准和异常恢复，并重点确认“正常安装”时车体转向对应哪个陀螺仪轴。若发现不符，我会修改驱动、重新构建并自动提交。这里继续按嵌入式安全审查与 PDF 核对流程进行。

## 40. 助手

你给出的文字看起来是“目录＋说明”，实际没有同名子文件夹；我会从 `D:\A嵌入式\资料库\20948` 开始定位资料，不需要你重新发路径。

## 41. 助手

初步核对表明现有驱动的寄存器地址、WHO_AM_I=0xEA、±500°/s与±4g换算是自洽的，但还缺两项库示例强调的内容：上电软件复位和静止零偏校准。直接阻塞校准会超过现有500ms看门狗窗口，因此我会改成后台非阻塞校准，保持主循环和急停逻辑持续运行。还会兼容0x68/0x69两种地址。

## 42. 助手

我已把 ICM20948 的本地函数说明、示例和芯片资料对上了。接下来会把驱动改成 0x68/0x69 自动识别、非阻塞静止校准和更适合小车转向的滤波配置，并把自动循迹入口与校准状态联锁；硬件安全审查要求电机仍保持锁定，先不烧录带动力版本。

## 43. 助手

驱动层已完成第一轮调整：上电软件复位、WHO_AM_I 校验、两种 I²C 地址探测、约 100 Hz 采样、±250°/s 量程，以及静止约 2 秒的非阻塞零偏校准。现在把它接入按键/蓝牙/OLED 的安全状态机，避免 IMU 未就绪时误进入自动循迹。

## 44. 助手

Keil + SysConfig 已通过：0 errors、0 warnings。现在我在做最后的安全审查和版本整理，重点确认校准不会卡住看门狗、IMU 失联会停机、以及自动循迹仍不会在硬件锁未解除时驱动电机。

## 45. 助手

已完成 ICM20948 驱动版本，并自动提交 Git：`46e97d5`。

主要改动：

- 自动探测 `0x68/0x69`，校验 `WHO_AM_I == 0xEA`。
- 配置约 100 Hz、±250 dps、51.2 Hz 数字低通。
- 上电静止约 2 秒自动校准，移动会重新计数。
- 蓝牙新增 `G`：停车并重新校准陀螺仪。
- OLED 显示 `IMU:CAL / OK / STALE / ERR`。
- IMU 未校准、数据超时或通信失败时，禁止进入/继续自动循迹。
- I²C 带超时和总线恢复，校准为非阻塞，不会饿死 500 ms 看门狗。
- 转向阻尼增益仍保持为零，待实车确认 Z 轴正负方向后再启用，避免正反馈。

实现位于 [icm20948.c](D:/TI/mspm0_sdk_2_02_00_05/电赛备用2/icm20948.c)、[control.c](D:/TI/mspm0_sdk_2_02_00_05/电赛备用2/control.c) 和 [README.md](D:/TI/mspm0_sdk_2_02_00_05/电赛备用2/README.md)。

参数已与本地函数说明及 TDK 官方的[芯片数据手册](https://invensense.tdk.com/wp-content/uploads/2024/03/DS-000189-ICM-20948-v1.6.pdf)、[I²C 地址说明](https://adm.invensense.tdk.com/what-are-i2c-addresses-supported-smartmotion-devices-0)核对。:codex-file-citation{path="D:/A嵌入式/资料库/20948/ICM20948_WE/List of public functions.pdf" purpose="source"}

Keil + SysConfig 构建结果：`0 errors，0 warnings`。本版未烧录；`APP_MOTOR_HW_READY` 仍为 `0`，PB14/STBY 无法被软件拉高。

继续解除动力锁和启用速度 PID 前，还需要你确认：

- 电池是几串，例如是否为 3S、满电 12.6 V。
- ICM20948 模块具体型号或正反面照片，用于确认 PB2/PB3 的 3.3 V 电平兼容性。
- 电机型号、减速比或右轮编码器每圈计数。

项目里原有的未跟踪文件均未修改、未提交。

::git-stage{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}
::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}

## 46. 用户

不是3S就是三节锂电池，满电11.8V，ICM再具体我也不知道了，文档里没有吗，电机型号MG513XP28_12V减速比你自己查吧，编码器我不知道啊，你自己设置吧

## 47. 助手

我会按“三节锂电、实测满电约 11.8 V”配置，不再等你提供编码器参数；我会从本地电机/IMU资料和厂商资料里查 MG513XP28_12V 的减速比与编码器线数，并采用可现场标定的默认值。因为这一步会解除部分电源与反馈参数，我先按嵌入式硬件安全审查规则核对欠压阈值、编码器溢出和失效停机条件。

## 48. 助手

本地 TB6612 资料包只包含驱动板和通用编码器教程，没有按电机型号命名的参数表；我会继续读取其中编码器说明和手册，再用网络检索 MG513XP28_12V。这里需要用 PDF 检查流程来提取手册参数，避免只凭商品标题猜减速比。

## 49. 用户

继续

## 50. 助手

继续。我已经确认本地资料的编码器测试方式，但还没找到 MG513XP28 的专用参数；刚才 PDF 说明文件路径写错了，没有改动项目。我现在从正确位置读取手册检查规则，再完成参数检索、固件配置、构建和自动 Git 提交。

## 51. 助手

查到了：MG513XP28_12V 中的 `P28` 对应 1:28。轮趣官方 MG513X 页面同时说明该型号可选两种编码器，并不能仅靠电机型号区分：裸露绿色小板是 13 PPR 霍尔，黑色塑料后盖是 500 PPR GMR。为避免猜错导致速度 PID 饱和，我会让固件自动识别两种脉冲密度，并统一换算到输出轮速度。官方参数还给出 MG513X 12 V、减速后空载约 370 rpm、额定约 300 rpm。

## 52. 用户

不用猜了，是霍尔编码器

## 53. 助手

明白，确定是 13 PPR 霍尔编码器。这样不再做类型自动识别：按 13 PPR × 1:28 × 硬件正交四倍频计算，每个车轮输出轴一圈为 1456 个计数；65 mm 轮径下我会把首版目标速度设为保守的约 0.25 m/s，并把 PID 反馈改成实际 mm/s，而不是不可解释的“每周期计数”。

## 54. 助手

构建已经通过，但安全审查发现一个必须先核对的硬件点：MG513 霍尔编码器输出上拉到它的供电 VCC。若 D153C 的 6 针接口给编码器供 5 V，那么 E1A/E1B 也会输出约 5 V，不能直接假定 PB10/PB11 可承受。我正在核对 D153C 原理图；这不会影响当前参数提交，但在确认前仍不会解除电机硬件锁。

## 55. 助手

D153C 原理图已确认：V1.1 的两个 6 针电机接口给编码器的是板载 `3V3`，E1A/E1B 只是经过 22 Ω 串联后引出，因此霍尔编码器接在该接口时，PB10/PB11 接收到的是 3.3 V 逻辑，直接连接是合适的。这个疑点已排除；硬件锁现在仅剩“ADC 实际接到 PA27并与万用表读数一致”和悬空方向测试。

## 56. 助手

已按霍尔编码器完成配置并提交 Git：`a0fbdd9`。

- MG513XP28_12V：减速比 `1:28`。
- 霍尔编码器：`13 PPR`。
- 硬件正交四倍频：`13 × 28 × 4 = 1456` 计数/轮。
- 65 mm 轮径，首版速度目标设为 `250 mm/s`。
- OLED 现在显示编码器增量和实际速度。
- 编码器增量超出 MG513X 的合理转速范围、采样超时或反馈失效时，自动循迹立即停机。
- 三节电池标称值按你实测的 `11.8 V` 设置，欠压锁存阈值为保守的 `9.9 V`。
- ADC 合理范围设为 `7.0–13.5 V`，异常读数禁止启动。
- 电机硬件锁仍保持开启，未烧录动力版本。

参数与轮趣[MG513X 官方产品资料](https://wheeltec.net/product/html/?223.html)一致。TB6612 手册给出的速度换算关系也已核对：:codex-file-citation{path="D:/A嵌入式/资料库/【WHEELTEC】TB6612电机驱动附送资料/【WHEELTEC】TB6612电机驱动附送资料_V3.2_2026.07.25/1.用户手册与使用教程/电机驱动模块使用手册—TB6612(2025.08.27).pdf" purpose="source"}

D153C V1.1 原理图确认电机接口给编码器供电的是 `3V3`，E1A/E1B 经 22 Ω 后引出，因此可以直接接 PB10/PB11：:codex-file-citation{path="D:/A嵌入式/资料库/【WHEELTEC】TB6612电机驱动附送资料/【WHEELTEC】TB6612电机驱动附送资料_V3.2_2026.07.25/5.原理图/2.D153C双路驱动稳压模块/TB6612双路驱动稳压模块原理图_V1.1_2026.04.20（D153C）.pdf" purpose="source"}

ICM 文件夹里是软件库和通用接线资料，并没有注明你手中模块的具体转接板型号，所以之前无法仅靠该目录确定模块版本。不过固件已经通过 `WHO_AM_I`、`0x68/0x69` 自动探测进行运行时确认。


