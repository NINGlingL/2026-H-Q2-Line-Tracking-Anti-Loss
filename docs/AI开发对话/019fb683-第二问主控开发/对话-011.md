# 对话记录 第 11 部分

会话 ID：`019fb683-28ab-7eb3-adbd-3c44aa83ff61`

## 181. 助手

这版仍保留电机80%硬限幅、STBY默认关闭、巡线/编码器失效停车和看门狗；只暂时取消ADC对启动与运行的阻断。

## 182. 助手

现有循迹框架已经有八路位置PID和右轮速度PID，但被ADC许可、编码器启动检查、按住即松停等多层条件卡住。我会改成第一阶段最简闭环：按键单击切换AUTO，线路有效就直接输出65%基础PWM；编码器有效时加入右轮速度PID，无效时先按固定PWM继续，不再阻止电机启动。

## 183. 助手

循迹控制路径已简化完成：线路PID直接形成左右差速，右轮编码器有效时才叠加速度PID；编码器失效不再卡住启动，只回退到固定基础PWM。OLED第7行改为实时显示 `ENC速度/增量/有效标志`，方便确认霍尔编码器。现在编译检查。

## 184. 助手

最终检查结果：单击PB21后应显示 `KEY:AUTO START`、`M:AUTO E:1`，中心线上初始左右输出会接近80%以确保起步；第二次按PB21、巡线数据超时、丢线50ms或运行35秒都会停车。右轮编码器实时显示为 `ENC:速度 D:增量 V:有效`。现在只提交本次3个文件。

## 185. 助手

已完成无ADC门控的循迹PID版本。

运行方式：

- 单击PB21：启动循迹
- 再次单击PB21：停车
- 必须先把八路模块放在线上，否则显示 `AUTO:NO LINE`

控制参数：

- 基础PWM：65%
- 起步时速度PID会提升至接近80%
- 八路位置PID：`Kp=38、Ki=1、Kd=0.8`
- 右轮编码器速度PID：`Kp=0.6、Ki=0.25`
- 目标速度：350mm/s
- 编码器：65mm轮径、13PPR、28:1、四倍频，共1456计数/圈
- 编码器无效时不再阻止启动，自动退回固定PWM

OLED应显示：

- `KEY:AUTO START`
- `M:AUTO E:1 L:xx R:xx`
- `ENC:速度 D:增量 V:1`

ADC采样服务和电压门控已经关闭，所以6V阈值不再参与电机控制。编译 `0 Error / 0 Warning`，未烧录。

Git提交：`bee06d9 feat: start line PID tracking from PB21`

::git-stage{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}
::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}

## 186. 用户

先限幅10%，所有情况都是，我的陀螺仪可以辅助啊

## 187. 助手

我会把最终电机写寄存器前的硬限幅统一改为10%，确保手动、循迹、PID异常等所有路径都不可能超过10%。同时把IMU加入转向环：八路模块给出目标转向，陀螺仪YAW角速度负责抑制摆动；IMU失效时自动退回纯八路PID。只编译、不烧录并自动提交。

## 188. 助手

这次10%限幅会放在电机驱动最底层，并保留STBY默认关闭、丢线停车和按键停车。需要说明，12V减速电机在10%时可能只能轻微抖动，这是限幅后的物理结果。

## 189. 助手

限幅和陀螺仪辅助已经写入；我现在只做编译与安全路径检查，不烧录。重点确认所有电机入口都不会超过 10%，以及陀螺仪异常时能自动退回纯循迹控制。

## 190. 助手

检查结果：自动循迹、诊断/手动入口最终都经过电机底层的 ±10% 硬限幅；即使上层参数填错，也越不过这个限制。现在开始完整 Rebuild，仍然不会执行 Download。

## 191. 助手

Rebuild 已通过：0 错误、0 警告。安全路径也确认保留：上电 STBY 关闭、按键可随时停止、丢线立即停、传感器超时停、自动模式超时停、看门狗继续工作；ADC 仍按你要求不参与本版控制。接下来只提交这三个改动文件。

## 192. 助手

已完成并自动提交 Git，本版不烧录。

- 所有模式统一硬限幅为 10%，底层电机驱动也锁死在 ±100‰。
- 循迹基础输出调整为 10%，目标速度 80 mm/s。
- 加入陀螺仪 Z 轴角速度 PID 辅助转向，用于抑制摆动。
- IMU 未校准、失效或数据超时时，自动退回纯八路循迹 PID。
- 编译结果：0 Error、0 Warning。
- 提交：`b62d148 feat: cap motors at 10 percent with yaw assist`

OLED 上 L/R 最大显示 `100`，代表 10%。如果电机只抖动或无法克服静摩擦，这是 10% 输出较低造成的；先按你的要求保持这个上限。

修改文件：[control.c](D:/TI/mspm0_sdk_2_02_00_05/电赛备用2/control.c)、[moto.c](D:/TI/mspm0_sdk_2_02_00_05/电赛备用2/moto.c)、[app_config.h](D:/TI/mspm0_sdk_2_02_00_05/电赛备用2/app_config.h)。

::git-stage{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}
::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}

## 193. 用户

按下按键却是显示的是AUTOSTART但是电机不动，V:1还有一个问题是YAW的初始化一直卡在0/100

## 194. 助手

我先处理两个直接原因：10% 很可能低于 MG513 的起转占空比；而 `YAW 0/100` 说明陀螺仪校准采样条件一次都没通过，不是还在缓慢校准。这个改动涉及电机和 IMU，我会按嵌入式硬件安全检查流程核对，但仍然只改代码、编译和提交，不烧录。

## 195. 助手

我已确认：电机程序实际会给到固定 10%（OLED 第 6 行应为 `M:AUTO E:1 L:10 R:10`，转弯时一侧会更低）。因此 `E:1` 且 L/R 有数仍不转，就是 10% 低于电机静摩擦起转点，软件在保持“任何情况不超过 10%”时无法再提高扭矩。

我先修复确定的软件问题：IMU 改为对前 100 个有效数据直接求平均，约 2 秒完成，不再因为某一帧偏置稍大而整段清零回到 0。

## 196. 助手

IMU 修复版已完整编译通过，0 错误、0 警告。电机的 10% 底层硬限幅、安全停机和 IMU 数据范围检查都没有被绕过；现在自动提交这一版。

## 197. 助手

两个问题已经确认：

- `V:1` 只表示编码器接口有效，不表示电机已经旋转。
- 电机保持 10% 硬限幅；若 OLED 显示 `E:1 L:10 R:10` 仍不转，就是 10% 低于电机起转占空比，软件无法在不突破限幅的情况下增加扭矩。
- `YAW CAL 0/100` 已修复。现在前 100 帧有效数据直接求平均，静置约 2 秒应从 `0` 连续增长到 `100`，随后显示 YAW 角。
- 编译：0 错误、0 警告。
- 未烧录。
- Git 提交：`ba43c86`

请烧录后观察 `M:AUTO E:1 L:x R:x`。如果 L/R 已到 `10` 仍不转，我建议下一版把全局限幅提高到 20%。

::git-stage{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}
::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}

## 198. 用户

我的意思是速度改到比较低就行，然后编码器你说有效我手动转不变啊陀螺仪好了

## 199. 助手

明白了：你要的是“低车速”，不是把 PWM 锁死在 10%。我会改成较低的编码器目标速度，同时给电机足够的起转 PWM；另外 `V:1` 目前只是驱动已初始化，不代表编码器真的有脉冲，我会把这个假有效状态修掉并核对 PB10/PB11 的 QEI 读取方式。陀螺仪保持现状。仍然不烧录。

## 200. 助手

编码器这里找到了两个软件问题：

- 代码每 10 ms 在计数器运行时强行把计数值改回 32768，而 TI 驱动明确说明运行中这样写会产生不可预测结果。

