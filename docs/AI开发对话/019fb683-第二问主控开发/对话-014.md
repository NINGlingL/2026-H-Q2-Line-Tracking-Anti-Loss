# 对话记录 第 14 部分

会话 ID：`019fb683-28ab-7eb3-adbd-3c44aa83ff61`

## 238. 助手

参数区已经放到 [empty.c](D:/TI/mspm0_sdk_2_02_00_05/电赛备用2/empty.c) 的头部、所有 `include` 后面。默认起步改为 22%，再用 2 秒平滑爬升；你以后主要改 `start_pwm_permille` 和 `start_ramp_ms`：前者越小起步越柔，后者越大加速越慢。现在进行编译检查。

## 239. 助手

编译通过：`0 Error(s), 0 Warning(s)`。这次安全检查重点确认了软启动只限制自动循迹的正向输出，不会延迟按键停车、失线停车或到达起点停车；所有停车路径仍直接清零 PWM 并拉低 STBY。现在按你的要求自动提交 Git，其他工程配置和旧文件不会提交。

## 240. 助手

已完成：

- 起步输出降为 22%。
- 用 2 秒从 22% 平滑爬升到正常速度，减少突然窜车。
- 所有常用调车参数集中到 [empty.c](D:/TI/mspm0_sdk_2_02_00_05/电赛备用2/empty.c:45)，每项后面都有中文功能注释。
- 70% 电机硬限幅仍保留在 [moto.c](D:/TI/mspm0_sdk_2_02_00_05/电赛备用2/moto.c:71)。

以后主要调整：

```c
.start_pwm_permille = 220,   /* 起步速度 */
.start_ramp_ms      = 2000U, /* 加速时间，越大越柔和 */
```

如果仍然起步过猛，可改成 `200` 和 `3000U`；如果电机起步不动，则把 `220` 稍微提高。

Keil 编译：`0 Error(s), 0 Warning(s)`。未烧录。

Git 提交：`e97b21a`

::git-stage{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}
::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}

## 241. 用户

6880a79,你把这个项目的调参参数给我

## 242. 助手

我会直接读取 `6880a79` 当时的源码，把那一版实际生效的速度、PID、循迹、超时和编码器参数整理出来，不改当前代码。

## 243. 助手

`6880a79` 版本当时实际使用的调参如下。

### 电机与速度

```c
最大PWM        = 300;   // 30%
自动巡线基础PWM = 220;   // 22%
诊断模式PWM     = 200;   // 20%
目标速度         = 120;   // mm/s
```

该版本没有软启动、直线加速和弯道降速，按键后直接输出 22%。

### 循迹PID

```c
Kp              = 8.0f;
Ki              = 0.3f;
Kd              = 0.12f;
积分限幅         = 20.0f;
输出限幅         = 70.0f;
积分分离误差     = 4.0f;
```

### 编码器速度PID

```c
Kp              = 0.45f;
Ki              = 0.40f;
Kd              = 0.0f;
积分限幅         = 150.0f;
输出限幅         = 140.0f;
积分分离误差     = 200.0f;
```

编码器无效时，这个版本会放弃速度PID，直接使用基础 PWM `220`，但不会停车。

### 陀螺仪辅助PID

```c
Kp              = 0.8f;
Ki              = 0.02f;
Kd              = 0.0f;
积分限幅         = 30.0f;
输出限幅         = 30.0f;
积分分离误差     = 60.0f;

每单位循迹偏差目标角速度 = 5.0f; // deg/s
```

### 时间参数

```c
控制周期          = 10ms;
IMU读取周期       = 20ms;
OLED刷新周期      = 125ms;
循迹最长时间      = 35000ms;
诊断最长时间      = 30000ms;
连续丢线停车时间   = 50ms;
八路模块预热时间   = 20000ms;
八路数据超时       = 150ms;
I2C超时           = 10ms;
```

### 编码器与车轮

```c
减速比             = 28;
编码器PPR          = 13;
正交倍频           = 4;
每圈计数           = 1456;
轮子直径           = 65mm;
轮子周长           = 204.204mm;
最大有效电机转速    = 500RPM;
```

### 功能开关

```c
电机              = 开启;
OLED              = 开启;
IMU20948           = 开启;
蓝牙              = 关闭;
电池ADC           = 关闭;
```

如果要在当前 `empty.c` 参数区尽量复现 `6880a79` 的运行手感，核心设置是：

```c
.pwm_limit_permille       = 300,
.start_pwm_permille       = 220,
.start_ramp_ms            = 0U,
.curve_pwm_permille       = 220,
.straight_pwm_permille    = 220,
.diagnostic_pwm_permille  = 200,

