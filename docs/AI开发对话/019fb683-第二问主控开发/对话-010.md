# 对话记录 第 10 部分

会话 ID：`019fb683-28ab-7eb3-adbd-3c44aa83ff61`

## 162. 助手

::git-stage{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}
::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}

## 163. 用户

我现在是按下按键他就转动一下正常吗

## 164. 助手

如果你只是“按一下就松开”，转一下是正常的。当前逻辑是：

- 按住 PB21：持续前进，最长30秒
- 松开 PB21：立即停车

如果一直按住也只转一下，就不正常。看OLED：

- 出现 `BAT FAULT/LOCK:BAT LOW`：电机启动导致电压瞬间下跌
- 屏幕重新出现 `BOOT SAFE`：电机干扰导致主控复位
- 显示 `KEY STOP`：PB21被检测为松开

请按住不放测试，并告诉我OLED上的 `KEY`、`M`、`BAT V/L` 三行内容。

## 165. 用户

显示LOCK:BAT ADC

## 166. 助手

`LOCK:BAT ADC` 已经定位到ADC采样流程：不是低压，而是采样在电机启动后失效。当前代码成功读取后没有执行 TI 单次转换要求的“停止转换→重新使能”，很可能第一帧正常、后续超时，于是电机只动一下。我会修正ADC重启顺序，并保留8V保护；不烧录。

## 167. 助手

修复只针对采样重启，不会绕过电池保护或提高电机上限。

## 168. 助手

ADC单次转换现在按TI推荐顺序在每次成功采样后执行“停止→重新使能”，避免下一帧一直超时形成 `LOCK:BAT ADC`。编译与下载段检查通过，现在提交。

## 169. 助手

已修复 `LOCK:BAT ADC` 的采样流程。

原因是ADC成功采样后没有按单次转换流程重新停止并使能，导致后续采样可能超时。现在每次采样都会正确重新装载。

测试时：

1. 等OLED显示 `BAT:xxxxx V:1 L:0`
2. 按住PB21
3. 应持续显示 `M:DIAG E:1` 并转动

编译 `0 Error / 0 Warning`，未烧录。

Git提交：`fd6e98a fix: rearm battery ADC after each sample`

::git-stage{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}
::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}

## 170. 用户

不是你改了吗，确定是电赛备用2项目项目?为什么还是之前的现象

## 171. 助手

我改的确实是 `D:\TI\mspm0_sdk_2_02_00_05\电赛备用2`，但你当前 Codex 工作区显示的是“电赛备用1”，Keil 也可能仍打开了备用1或加载旧AXF。我马上核对两个目录的提交、生成时间以及当前Keil进程实际打开的工程，先不改代码、不烧录。

## 172. 助手

确定了：我修改的是“电赛备用2”，但你刚才编译/下载的是“电赛备用1”。

证据：

- 备用1的 AXF 刚在 `17:52:47` 重新生成
- 备用1当前提交：`d857538`
- 备用2最新提交：`fd6e98a`
- 备用2 AXF 生成时间：`17:50:52`

请关闭当前Keil工程，重新打开：

[备用2 Keil工程](D:/TI/mspm0_sdk_2_02_00_05/电赛备用2/keil/empty_LP_MSPM0G3507_nortos_keil.uvprojx)

重新编译时，输出路径必须包含：

`D:\TI\mspm0_sdk_2_02_00_05\电赛备用2\...`

之前现象不变，是因为下载的一直是备用1的旧程序。本次没有修改代码。

## 173. 用户

按下按键还是LOCK:BAT ADC啊

## 174. 助手

如果OLED电压数值正常、但显示 `V:0`，说明只是ADC后续转换超时，最近一次有效电压仍然存在。为了先让按键测试跑起来，我会让“PB21点动模式”允许使用最近一次处于7–13.5V范围内的电压；仍要求电压>8V。自动循迹模式仍必须使用实时有效ADC，不会放宽。

## 175. 助手

这个放宽只作用于人工按住按键的检测模式，松手停车、80%限幅和8V门槛不变。

## 176. 助手

修改已通过编译：PB21检测模式现在可在 `V:0` 时使用最近一次正常电压继续运行，只要显示电压仍在7–13.5V且高于8V；自动循迹仍要求 `V:1`。下载段也保持8字节对齐。现在提交。

## 177. 助手

已修改正确的“电赛备用2”项目：

`D:\TI\mspm0_sdk_2_02_00_05\电赛备用2`

现在PB21检测模式下：

- 即使显示 `V:0`
- 只要最近显示电压在7–13.5V之间且高于8V
- 就允许电机运行

自动循迹仍要求实时ADC有效。编译 `0 Error / 0 Warning`，未烧录。

Git提交：`e1d539c fix: allow last battery sample in PB21 bench mode`

如果下载这版后电压显示约11.4V却仍出现 `LOCK:BAT ADC`，就说明Keil下载的仍不是备用2的新AXF；因为这版代码在该条件下不会产生这个锁定提示。

::git-stage{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}
::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}

## 178. 用户

按下按键显示KEY FWD，但是电机不转，我现在再次要求把电压降到6V，由于循迹和YAW角正常，然后这一次直接写循迹的PID算法，先让电机转是首要目的，保护后面再说，注意编码器也要弄好

## 179. 用户

不采用ADC先，先实现循迹再说

## 180. 助手

收到，先切成“无ADC门控的循迹调试版”：PB21按一下启动/再按一下停止，八路巡线位置做差速PID，右轮霍尔编码器做速度PID；启动功率提高到能带动车辆。ADC可以继续显示，但不参与电机许可和PWM补偿。只编译、不烧录，完成自动提交Git。

