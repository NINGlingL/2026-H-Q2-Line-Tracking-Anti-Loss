# 对话记录 第 9 部分

会话 ID：`019fb683-28ab-7eb3-adbd-3c44aa83ff61`

## 144. 助手

按键逻辑已改为低电平按住前进，并把松手停车放在去抖之前，确保释放 PB21 时立即拉低 STBY；蓝牙初始化和蓝牙服务均已关闭。现在进行编译与 Flash 对齐检查。

## 145. 助手

验证通过：编译 `0 Error / 0 Warning`，下载段 `0x4E40` 满足8字节对齐。PB21按下需稳定30ms才启动，松开不等待去抖、立即停车；电池低于保护值或采样异常时仍不会启动。现在只提交本次按键模式的3个文件。

## 146. 助手

已完成：

- 按住 `PB21`：左右电机以25%功率前进
- 松开 `PB21`：立即停车并拉低 STBY
- 蓝牙初始化和指令处理已关闭
- 电池异常时禁止启动
- OLED 显示 `KEY FWD / KEY STOP` 和电机输出
- 编译 `0 Error / 0 Warning`
- 未烧录

Git 提交：`d2818d8 feat: run motors while PB21 is held`

::git-stage{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}
::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}

## 147. 用户

按键有响应但是电机不转

## 148. 助手

按键能响应说明 PB21 和主循环正常；现在只剩“控制状态是否允许输出、STBY/方向脚、PWM极性”这三段。我先核对 OLED 对应的实际命令值和生成的 PWM 波形配置，重点检查当前 `25%` 是否被代码算成了错误极性；不烧录。

## 149. 助手

这次会触碰 TB6612 的实际输出路径，我会继续保留80%硬限幅、电池保护和松手立即停车。

## 150. 助手

PWM配置本身是正确的：PB0/PB1输出20kHz，SysConfig里比较值200确实代表0%，所以不是极性写反。当前按键只给25%占空比，12V减速电机在静摩擦下很可能起不来；我先把点动启动功率提高到60%，仍受80%硬限幅，并在OLED增加STBY实际状态，方便一次判断软件是否真的把驱动使能。

## 151. 助手

新版本已通过编译，下载段仍正确对齐。按住PB21时OLED应显示类似 `M:DIAG E:1 L:62 R:62`（电池前馈后约60%上下）；其中 `E:1` 代表软件已经拉高PB14/STBY。现在提交这一版。

## 152. 助手

已把按键启动功率从25%提高到60%，最高仍限制80%。

按住PB21时，OLED应显示类似：

`M:DIAG E:1 L:62 R:62`

- `E:1`：PB14/STBY已拉高
- `L/R≈60`：PWM已经输出

编译：`0 Error / 0 Warning`，未烧录。

Git提交：`285e919 fix: raise PB21 motor start power`

如果更新后显示 `E:1`、`L/R≈60` 但电机仍不转，就只剩硬件链路需要检查：`PB0→PWMA`、`PB1→PWMB`、TB6612逻辑VCC、12V VM以及共地。

::git-stage{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}
::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}

## 153. 用户

我按下按键显示LOCK HW/BAT

## 154. 助手

`LOCK:HW/BAT` 是软件保护拦截，不是PWM问题。当前硬件门已经开启，所以大概率是电池模块虽然显示电压，但 `Battery_IsSafe()` 的有效/锁存条件没有通过。我先核对电池状态判定，把OLED改成能明确区分 `HW LOCK` 和 `BAT LOCK`，并让正常电压能够解除启动锁；不烧录。

## 155. 助手

这次只调整启动许可判断，电池低压保护和80%电机限幅仍保留。

## 156. 助手

当前保护提示把两种原因合并了，确实不便判断。我会做两项修正：电池偶发一次ADC超时不再立刻锁死；若电压连续1秒高于10.2V，则清除先前的低压误锁。同时OLED改为显示 `V`（ADC有效）和 `L`（低压锁存），提示也拆成 `LOCK:HW / BAT ADC / BAT LOW`。低于9.9V仍禁止启动。

## 157. 助手

编译通过，新的OLED电池行会直接显示 `BAT:xxxxx V:1 L:0`。只有 `V:1、L:0` 才允许按键启动；这样下一次测试不会再出现含糊的 `HW/BAT`。正常11.44V连续约1秒会自动清除先前的低压误锁。现在提交。

## 158. 助手

已修复电池许可误锁并拆分提示。

更新后等待约1秒，OLED应显示：

`BAT:11440 V:1 L:0`

含义：

- `V:1`：ADC数据有效
- `L:0`：没有低压锁
- 此时按住PB21应显示 `M:DIAG E:1` 并驱动电机

新的锁定提示会明确显示：

- `LOCK:HW`：电机硬件门未开启
- `LOCK:BAT ADC`：ADC采样无效
- `LOCK:BAT LOW`：电池低压

正常电压持续1秒会自动清除之前的低压误锁。编译 `0 Error / 0 Warning`，未烧录。

Git提交：`1693c06 fix: recover valid battery motor permit`

::git-stage{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}
::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}

## 159. 用户

我觉得你的电压保护过于高了，你调一下高于8V就允许启动电机

## 160. 助手

可以，我会把3S电池的电机启动门槛从9.9V降到8.0V：只有测得电压严格高于8.0V、`V:1`且无锁存时才允许启动；≤8.0V立即禁止电机。需要提醒：8.0V相当于平均约2.67V/节，已经非常低，建议电池组本身必须有可靠BMS。完成后只编译、不烧录并提交Git。

## 161. 助手

阈值已改好并验证：>8.0V允许启动，≤8.0V禁止；电压恢复到8.3V以上并持续约1秒会清除低压锁。编译与Flash对齐均正常，现在提交。

## 162. 助手

已调整电池门槛：

- `> 8.0V`：允许启动电机
- `≤ 8.0V`：禁止启动
- 恢复至 `≥ 8.3V` 持续约1秒：清除低压锁
- 编译 `0 Error / 0 Warning`
- 未烧录

Git提交：`5791df1 tune: lower motor battery permit to 8V`

注意：3S电池8V约为每节2.67V，必须确保电池组带可靠BMS，避免过放。


