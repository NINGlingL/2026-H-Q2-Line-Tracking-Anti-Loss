# 对话记录 第 13 部分

会话 ID：`019fb683-28ab-7eb3-adbd-3c44aa83ff61`

## 218. 用户

现在小车只能运动一小段也可以转弯，但是电机大概只能转3s就停了，然后显示ENCODE LOST

## 219. 助手

原因已经明确：不是循迹停了，而是右轮编码器一直没有产生有效脉冲，代码里的 `ENC LOST` 超时保护主动停机。先按你的优先级处理：取消“编码器丢失就停机”，编码器无信号时自动退化为固定低速 PWM，八路循迹和陀螺仪继续控制转向；编码器之后单独排查。仍然不烧录。

## 220. 助手

`ENC LOST` 停机条件已经移除；编码器 `V:0` 时速度 PID 自动清零，但循迹转向仍继续运行。丢线、按键停止、35 秒自动停止和 30% PWM 限幅仍保留。正在等待编译完成。

## 221. 助手

已取消 `ENC LOST` 停机。

现在编码器没有信号时：

- 不再停止电机。
- 自动退化为固定低速 PWM。
- 八路循迹 PID 和陀螺仪辅助继续工作。
- OLED 仍显示 `V:0`，方便以后排查编码器。
- 丢线、PB21 停止、35 秒超时和 30% PWM 限幅仍保留。

编译 0 错误、0 警告，未烧录。Git 提交：`6880a79`。

::git-stage{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}
::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}

## 222. 用户

# Files mentioned by the user:

## H题_车载平衡滚球运动控制系统.pdf: D:/A嵌入式/资料库/电赛资料/H题_车载平衡滚球运动控制系统.pdf

## My request for Codex:
目前效果挺好，可以完全跑圈，但是你再看一下题目，有一道题目不是从起点跑一圈回到起点停止嘛，起点那里的是中间六个循迹亮，然后速度可以提升一点，你改一下代码

## 223. 助手

我会先按题面确认“完成一圈”的判定要求，再改控制逻辑。这里会用 PDF 阅读流程核对题目，并用嵌入式安全检查流程处理提速与自动停车：起点标志按你确认的“中间六路同时触发”，启动离开起点后重新遇到该标志即停车，避免上电时直接误判完成。

## 224. 助手

我继续按“跑起来优先”的方式改：先核对题目中的一圈停止要求，再在现有能稳定跑圈的版本上加入“离开起点后，第二次识别中间六路标志才停车”，并只小幅提速；不会烧录。这里会按嵌入式安全检查保留现有的失线停车、超时和 30% 硬限幅。

## 225. 用户

50限幅吧速度有点慢了

## 226. 助手

收到，改为 50% 总限幅。为了不突然从 22% 直接顶到 50%，正常巡线基准先提高到约 35%，PID 转向时允许单轮最高到 50%；同时实现一圈回起点自动停车。仍然只编译，不烧录。

## 227. 助手

题目已核对：第 2 项明确要求从 A 点顺时针一圈后停在 A 点、显示总时间，且总时间不超过 20 秒；A 点是横跨轨迹的 5 cm 居中黑色启停线。代码里我会用“中间六路中至少五路同时压黑线”做容错识别，必须先离开起点且运行超过 5 秒，才允许再次识别停车，避免一按键就在起点停下。50% 只作为硬上限，常速先设 35%。

## 228. 助手

代码已经改好，正在做纯编译验证。除了 50% 执行层硬限幅，还保留按键急停、失线 50 ms 停车、串口循迹数据超时停车和 35 秒兜底超时；OLED 最后一行会显示本圈计时与 `LAP` 状态，识别起点后显示 `LAP DONE` 并关断 STBY。

## 229. 助手

编译已通过：`0 Error(s), 0 Warning(s)`，生成的新程序大小约 18.9 KB。现在只会提交本次改动的 3 个源码文件；SysConfig、Keil 工程设置和你原有的未跟踪文件都不会带进提交。

## 230. 助手

已完成并提交：

- 巡线基准速度：35%
- 电机/PID 总限幅：50%
- 识别起点中间六路，允许其中一路瞬时漏检
- 必须先离开起点、运行超过 5 秒，再次检测启停线才停车
- 停车时显示 `LAP DONE`
- OLED 第 8 行实时显示总时间和圈数
- 保留失线、通信超时、按键急停等保护

关键修改：[control.c](D:/TI/mspm0_sdk_2_02_00_05/电赛备用2/control.c:63)、[app_config.h](D:/TI/mspm0_sdk_2_02_00_05/电赛备用2/app_config.h:29)、[moto.c](D:/TI/mspm0_sdk_2_02_00_05/电赛备用2/moto.c:8)。

Keil 编译结果：`0 Error(s), 0 Warning(s)`。未烧录。

Git 提交：`d1717f6 feat: stop after one lap and raise tracking limit`

:codex-file-citation{path="D:/A嵌入式/资料库/电赛资料/H题_车载平衡滚球运动控制系统.pdf" purpose="source"}

::git-stage{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}
::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}

## 231. 用户

你改的代码为什么还是没有停在原点而是一直跑，而且速度可以再提升可以提高上限，然后再直线行进的时候车头左右摇摆，我确定在原点时，是中间六个传感器能够感应到黑线，改吧

## 232. 助手

我来改，这次把“不停车”的判定改成更直接的方式：跑完起点离开阶段后，只要收到一帧“至少 5 路同时压黑线”（你的中间六路会满足）就立即停车，不再要求持续 20 ms，避免高速跨过 1.8 cm 启停线时漏判。

同时我会把直线和弯道速度分开：直线提速、弯道略降速，并给循迹误差加中心死区和滤波来压住左右摇摆。因为要把电机上限继续提高，我会按嵌入式硬件安全检查保留执行层硬限幅和所有急停条件；仍然只编译、不烧录。

## 233. 助手

这版的停点逻辑已经改成“单帧即停”，OLED 最后一行新增 `A`：离开起点后应从 `A:0` 变成 `A:1`；回到起点检测到六路黑线后，应立即显示 `LAP DONE`、`L:1` 并拉低 STBY。这样如果实车仍有问题，也能直接从屏幕判断是“没有完成启用”还是“没有收到六路帧”。现在开始纯编译验证。

## 234. 助手

编译通过，`0 Error(s), 0 Warning(s)`。安全检查确认：最高输出在电机寄存器写入前再次硬限制为 70%，PID 输出另有限幅；按键停机、失线停机、IR 超时停机、自动模式 35 秒超时均保留。按你之前的要求，电池 ADC 仍未参与电机许可。接下来只提交这 3 个源码文件。

## 235. 助手

已修改并提交：

- 停点识别改为：运行超过 5 秒且已离开起点后，任意一帧检测到至少 5 路黑线便立即停车。
- 不再要求启停线保持 20 ms，避免高速跨线漏判。
- 直线速度提高到 55%，弯道基准 45%，总上限提高到 70%。
- 增加循迹低通滤波和中心死区，降低直线左右摇摆。
- 直线居中时暂停陀螺仪转向修正，避免陀螺仪噪声反复差速。
- OLED 最后一行显示 `A`：离开起点后应为 `A:1`；返回时检测到六路黑线应显示 `LAP DONE`、`L:1`。

修改位置：[control.c](D:/TI/mspm0_sdk_2_02_00_05/电赛备用2/control.c:65)、[app_config.h](D:/TI/mspm0_sdk_2_02_00_05/电赛备用2/app_config.h:29)、[moto.c](D:/TI/mspm0_sdk_2_02_00_05/电赛备用2/moto.c:76)。

Keil 编译：`0 Error(s), 0 Warning(s)`。未烧录。

Git 提交：`ff3fb67`

::git-stage{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}
::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}

## 236. 用户

起步加速度太大了，你这回改程序的时候，把可以改的参数放到empty.c开头方便我后面自己调，还有他们的功能注释在后面，你把起步速度改小一下，

## 237. 助手

我会把常用调车参数集中到 `empty.c` 文件开头，每个参数后面直接写中文功能注释；控制代码只读取这些参数。起步会改为“低占空比起步 + 约 2 秒平滑爬升”，不会一按键就跳到直线高速。

由于这是电机输出和加速曲线修改，我会按嵌入式安全检查保留 `moto.c` 内不可绕过的 70% 硬限幅；本次仍只编译、不烧录。

