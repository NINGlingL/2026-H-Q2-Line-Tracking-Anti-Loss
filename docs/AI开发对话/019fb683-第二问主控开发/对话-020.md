# 对话记录 第 20 部分

会话 ID：`019fb683-28ab-7eb3-adbd-3c44aa83ff61`

## 297. 助手

如果自制地图的起点只是普通单线，没有独特横线，同样的八路状态可能沿途重复；目前用YAW门槛降低误停概率，但最可靠的方法仍是在每张地图起点设置独特横线。

Keil编译：`0 Error(s), 0 Warning(s)`，未烧录。

::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}

## 298. 用户

我现在在一张小地图上跑但是起步还是报丢线，实际上是只能识别1-2路或者卡在中间导致没有先线，之前的代码在大地图时候没有问题但是小地图就报丢线，你做一个通用化的代码

## 299. 助手

原因找到了：当前版本虽然把丢线超时放宽到180ms，但“启动瞬间没有探头看到线”仍然会被 `AUTO:NO LINE` 拒绝；而且没有历史转向时，丢线恢复只能停住。通用版我会改成两级寻线：短暂丢线先保持上一次转向，仍找不到就以低速左右扫线；启动时即使8路暂时全白也允许进入低速寻线，超过有限时间才停车。终点状态学习和YAW保底继续保留。我会按电机安全检查流程复核，不烧录。

## 300. 助手

我已经把“小图起点可能只有 0～2 路命中”作为正常启动场景处理，正在确认改动是否完整落盘，然后只编译验证，不会烧录。软件会短时间低速找线，找回后自动进入循迹，超时才真正报丢线。

## 301. 助手

改动已经落盘：启动时“0 路命中”不再被直接拒绝；运行中短暂看不到线时，会先沿用原方向，再朝最后一次看到黑线的方向低速扫线。现在我检查停车判定是否会被这套逻辑误触发，并做一次 Keil 全量编译。

## 302. 助手

全量编译已通过（0 错误、0 警告）。安全边界也保留了：找线只用 20% 低速，最多持续 500 ms；串口数据真的中断仍会立即停车，电机硬限幅没有提高。接下来我只提交这两个源码文件，不会把工程配置或编译产物带进 Git。

## 303. 助手

已完成小地图通用化循迹处理：

- 起步只有 1～2 路：直接正常进入 PID 循迹。
- 起步 0 路：不再立即报丢线，使用 20% 低速左右交替扫线。
- 运行中丢线：先保持上次转向约 166 ms，再朝最后看到黑线的方向寻找。
- 重新识别到任意一路后，立即恢复正常 PID。
- 连续 500 ms 仍未找到线，才显示 `LINE LOST` 并停车。
- 停车线、陀螺仪一圈判断和电机硬限幅保持不变。

修改文件：[control.c](D:/TI/mspm0_sdk_2_02_00_05/电赛备用2/control.c:510)、[empty.c](D:/TI/mspm0_sdk_2_02_00_05/电赛备用2/empty.c:73)

Keil 全量编译通过：`0 Error(s), 0 Warning(s)`。没有烧录。

Git 提交：`bc0f445 feat: add bounded line reacquisition search`

::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}

## 304. 用户

0af5e50回档

## 305. 用户

0af5e50回档

## 306. 助手

我会把“电赛备用2”恢复到 `0af5e50` 的源码状态，但保留 Git 历史，并且不碰现有未提交的工程配置和个人文件。由于这是电机控制固件回档，我会按嵌入式安全检查流程确认限幅与停车逻辑，再编译、不烧录、自动提交。

## 307. 助手

检查结果很明确：`0af5e50` 之后只有 [control.c](D:/TI/mspm0_sdk_2_02_00_05/电赛备用2/control.c) 和 [empty.c](D:/TI/mspm0_sdk_2_02_00_05/电赛备用2/empty.c) 两个源码发生变化。因此这次只恢复这两个文件，其余未提交内容全部保留。

## 308. 助手

源码已与 `0af5e50` 逐字匹配。该版本会同时撤销后续的“起点状态学习”和“500 ms 扫线”，恢复为原版六路停车线判定与 50 ms 丢线停车；陀螺仪辅助停车仍在，因为它本来就是 `0af5e50` 的内容。现在进行限幅检查和全量编译。

## 309. 助手

回档版本编译通过：0 错误、0 警告，电机入口仍保留 ±55% 硬限幅、急停和自动超时。接下来生成一个“恢复到 `0af5e50`”的新提交，这样以后仍可追溯和再次切换版本。

## 310. 助手

已回档到 `0af5e50` 的源码状态：

- [control.c](D:/TI/mspm0_sdk_2_02_00_05/电赛备用2/control.c) 和 [empty.c](D:/TI/mspm0_sdk_2_02_00_05/电赛备用2/empty.c) 已逐字匹配。
- Keil 编译：`0 Error(s), 0 Warning(s)`。
- 未烧录，其他未提交文件未改动。
- 新回档提交：`70588be revert: restore 0af5e50 tracking behavior`

::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}

## 311. 用户

bc0f445回档，但是停车有误差

## 312. 用户

bc0f445回档，但是停车误差有点大

## 313. 助手

我会恢复 `bc0f445` 的通用找线版本，同时针对你之前描述的“越过停车基准线”做小幅提前制动，不改循迹速度和弯道参数。因为涉及电机停车，我会先按嵌入式安全检查确认制动、限幅和超时，再编译、不烧录、自动 Git。

## 314. 助手

停车误差的主要来源不是制动保持时间，而是最后一段仍以约 27% PWM 接近停车线；传感器识别后才制动，惯性会继续带动车体。我会保留 `bc0f445` 的停车线/陀螺仪双判定，只把最后 30°改为渐进降速，终点附近降到约 18.5% PWM，再执行原有短刹车。这样不会提前判定停车，只会缩短越线距离。

## 315. 助手

现在的源码结构是 `bc0f445`，与原版本相比只多了两处停车调节：减速提前到累计转角 330°，终点附近基础输出从约 27% 降到约 18.5%。停车线命中后仍立即使用 TB6612 主动短刹，不会用倒车纠偏。开始全量编译验证。

## 316. 助手

编译已通过：0 错误、0 警告。安全检查确认电机入口仍为 ±55% 硬限幅，找线最多 500 ms，IR 失联和自动任务超时仍进入安全停车。现在只提交 `control.c` 与 `empty.c`。

## 317. 助手

已恢复到 `bc0f445`，并缩小停车越线误差：

- 保留该版本的通用找线、起点状态学习和陀螺仪保底停车。
- 最后一圈累计转角到 `330°` 开始渐进减速。
- 停车线附近输出由约 `27%` 降至约 `18.5%`。
- 识别停车线后立即主动短刹，不使用倒车修正。
- Keil 编译：`0 Error(s), 0 Warning(s)`。
- 未烧录。

提交：`c5092a5 feat: restore bc0 tracking and tighten lap stop`

::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}

## 318. 用户

bc0f445回档，不该

## 319. 助手

明白，上次不该额外修改停车参数。这次恢复为**完全一致的 `bc0f445`**，不添加任何改动；只编译验证、不烧录，并自动 Git。由于是电机固件回档，我会照例核对限幅和停车安全。

## 320. 助手

现在 `control.c` 和 `empty.c` 已与 `bc0f445` 完全一致，包括原来的 `340°` 末段减速和 `65%` 终点速度比例；上一版新增的停车调节已经全部撤销。正在做最后一次 Keil 编译。

## 321. 助手

已纠正：现在源码与 `bc0f445` **完全一致**，没有附加停车参数修改。

- Keil 编译：`0 Error(s), 0 Warning(s)`

