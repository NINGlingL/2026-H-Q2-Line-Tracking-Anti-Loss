# 对话记录 第 12 部分

会话 ID：`019fb683-28ab-7eb3-adbd-3c44aa83ff61`

## 200. 助手

- `V` 上电就被固定置 1，所以之前的 `V:1` 是假有效；OLED 又只显示瞬时 `D`，手转后很容易看不到。

我会改成连续读取 16 位计数差、不再运行中清零，并让 OLED 显示累计脉冲。PB10/PB11 同时开启内部上拉，适配霍尔编码器常见的开漏输出。电机改为低速闭环目标 120 mm/s，PWM 只是保守限制在 30% 以内，不再锁死 10%。

## 201. 助手

编译通过了，但 SysConfig 对 QEI 的内部上拉选项没有实际生成到代码里。我会把上拉明确放进编码器初始化函数，确保 PB10/PB11 真正生效；图形配置仍保留原来的 TIMG8 QEI 引脚映射。这个检查避免出现“界面看着设置了、固件里其实没设置”的情况。

## 202. 助手

最终行为已经明确：按键后先用约 22% 起步，右轮出现真实脉冲后进入 120 mm/s 低速 PID；若 1.5 秒仍收不到编码器脉冲，会显示 `ENC LOST` 并停机，避免把“编码器断线”误当成“速度为零”持续加力。OLED 的 `ENC` 现在是累计脉冲，手转轮子后数值会保留变化。现在做最后编译确认并提交。

## 203. 助手

已按你的真实意思修改：限制低车速，不再把 PWM 锁死在 10%。

- 目标速度：120 mm/s。
- 起步基础 PWM：22%，最大硬限幅：30%。
- 编码器不再运行中反复清零计数器。
- PB10/PB11 启用内部上拉。
- `V:1` 现在表示确实检测到编码器脉冲。
- OLED 的 `ENC` 改为累计脉冲，手转右轮后数值应保持变化。
- 启动后 1.5 秒仍没有脉冲，会显示 `ENC LOST` 并停机。
- 陀螺仪代码保持正常版本。
- 编译：0 错误、0 警告；未烧录。
- Git：`bffff89`

烧录后先不按按键，手转右轮观察：

```text
ENC:累计值 S:速度 V:状态
```

只要 `ENC` 发生变化，编码器就通了。随后按 PB21，电机应该能够起转并进入低速控制。

::git-stage{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}
::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}

## 204. 用户

ENC LOST，你能先让电机动起来嘛，其他的后面面再调都可以

## 205. 助手

可以。这一版先完全绕开循迹、编码器和陀螺仪闭环：PB21 第一次按下直接让左右电机以 35% 正转，第二次按下立即停止。这样不会再出现 `ENC LOST`，也能单独确认 TB6612、电机和 PWM 是否真正工作。这个版本仍保留底层 40% 硬限幅，不烧录。

## 206. 助手

直驱测试版已经写好：按键后 OLED 应显示 `KEY FWD TEST`、`M:DIAG E:1 L:35 R:35`，不会读取循迹、编码器或 YAW 来决定是否转动。第二次按键停止，连续运行 30 秒也会自动停止。现在只确认编译并提交。

## 207. 助手

已改成最简单的电机直驱测试版：

- 第一次按 PB21：左右电机直接以 35% PWM 正转。
- 第二次按 PB21：立即停止。
- 不检查编码器、循迹或陀螺仪，不会再出现 `ENC LOST`。
- 30 秒后自动停止。
- OLED 正常应显示：

```text
KEY:KEY FWD TEST
M:DIAG E:1 L:35 R:35
```

编译 0 错误、0 警告，未烧录。Git 提交：`a3c1a7a`。

如果显示完全一致但两个电机仍不转，就可以确定问题不在 PID 或编码器，而在 PWM/STBY/TB6612接线或电机供电路径。

::git-stage{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}
::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}

## 208. 用户

电机就是不转，你检查问题

## 209. 助手

我现在按“代码 → SysConfig 实际引脚 → TB6612 文档 → 开发板引脚复用”逐层核对。先不再改参数，也不烧录；因为 OLED 已显示 `E:1 L:35 R:35` 时仍完全不转，问题已经缩小到 PWM 输出、方向脚、STBY 或驱动板供电/接线这一段。

## 210. 用户

不对不对不对我没开电，还是恢复到循迹PID

## 211. 助手

明白，是驱动板没上电，不是软件问题。我撤销刚才的“PB21 直驱测试版”，恢复到上一版：PB21 启动循迹 PID，八路循迹 + 右轮编码器速度 PID + 陀螺仪辅助。不会动其他文件，也不烧录。

## 212. 助手

循迹 PID 已恢复。现在按 PB21 会回到 `AUTO START`，目标 120 mm/s，启动基础约 22%，最大 30%，并使用八路循迹、右轮编码器和陀螺仪辅助；刚才的 35% 直驱测试逻辑已完全撤销。

## 213. 助手

已恢复循迹 PID 版本，刚才的直驱测试已撤销。

- PB21：启动/停止循迹 PID。
- 目标速度：120 mm/s。
- 基础 PWM：22%，最大 30%。
- 八路循迹 + 右轮编码器 + 陀螺仪辅助。
- 编译：0 错误、0 警告。
- 未烧录。
- Git 提交：`020e724`

测试时记得先打开电机驱动板电源；启动后 1.5 秒收不到右轮编码器脉冲仍会显示 `ENC LOST`。

::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}

## 214. 用户

纠正一下，你的启动是小车是在后退

## 215. 助手

确认，这是左右电机的正方向定义同时反了。我只交换 A、B 两路的正反方向脚：正速度改为 AIN2/BIN2，高层循迹 PID、PWM 和编码器参数不动。仍然不烧录，改完自动提交。

## 216. 助手

方向修正已完成：正速度现在驱动 AIN2/BIN2，负速度驱动 AIN1/BIN1；停止时仍然四个方向脚全拉低，30% 底层限幅和 STBY 安全逻辑保持不变。正在提交这一版。

## 217. 助手

已修正前进方向：

- 右电机正转：AIN2。
- 左电机正转：BIN2。
- 循迹 PID、编码器和陀螺仪逻辑不变。
- 编译：0 错误、0 警告。
- 未烧录。
- Git 提交：`ad94832`

重新烧录后，PB21 启动应改为向前行驶。

::git-stage{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}
::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}

