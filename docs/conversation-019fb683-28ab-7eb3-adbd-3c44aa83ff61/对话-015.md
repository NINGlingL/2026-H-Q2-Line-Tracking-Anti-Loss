# 对话记录 第 15 部分

会话 ID：`019fb683-28ab-7eb3-adbd-3c44aa83ff61`

## 243. 助手

.speed_target_mm_s        = 120,
.line_kp                  = 8.0f,
.line_ki                  = 0.3f,
.line_kd                  = 0.12f,
.line_filter_alpha        = 1.0f,
.line_center_deadband     = 0.0f,
.yaw_kp                   = 0.8f,
.yaw_ki                   = 0.02f,
.yaw_rate_per_position    = 5.0f,
.line_lost_stop_ms        = 50U,
.auto_timeout_ms          = 35000U
```

## 244. 用户

不是，我现在实际用的元器件有编码器吗？，不是陀螺仪和八路寻迹结合？

## 245. 助手

你说得对，我刚才把“代码里保留的编码器参数”和“实际参与控制的器件”混在一起了。

你当前实际循迹控制是：

- 八路红外：提供黑线位置偏差，是主要循迹依据。
- ICM20948：提供 Z 轴角速度，辅助差速转向、抑制转弯振荡。
- 电机：执行左右轮差速。
- 编码器：右轮虽然接了霍尔编码器，但此前一直显示 `V:0`，因此实际上没有参与PID控制。

`6880a79` 的逻辑是：

```c
if (encoder.valid != 0U) {
    // 使用编码器速度PID
} else {
    // 编码器无效，直接使用固定基础PWM
}
```

所以你现在有效的控制关系是：

```text
八路寻迹位置偏差
        ↓
    循迹PID
        +
ICM20948角速度辅助PID
        ↓
 左右轮差速PWM
```

编码器代码只是保留着备用，不影响目前跑圈，也不会因为 `V:0` 停车。后续调参应该主要关注循迹PID、陀螺仪辅助PID、直线/弯道PWM和软启动参数。

## 246. 用户

6880a79回到这个版本，但是可调参数放到empty.c，然后把停车的功能集成到6880a79，

## 247. 助手

明白：以 `6880a79` 的控制手感为基准恢复——固定基础速度、原始循迹PID、原始陀螺仪辅助PID，不保留后来加入的直线加速、中心死区、滤波和软启动逻辑；只额外保留两件事：

1. 所有常用参数集中在 `empty.c` 开头；
2. 离开起点后再次检测到中间六路启停线，立即停车并保留总时间显示。

我会保留 `6880a79` 当时的 30% 电机硬限幅，并且只编译、不烧录。

## 248. 助手

现在控制路径已经恢复为 `6880a79`：八路位置直接进入循迹PID，ICM20948角速度辅助PID直接叠加，编码器无效时使用固定 22% 基础PWM；后来加入的滤波、死区、直线/弯道分速和软启动均已移除。停车检测和OLED计时作为独立功能保留。正在编译验证。

## 249. 助手

编译通过：`0 Error(s), 0 Warning(s)`。核对结果是电机驱动 `moto.c` 已与 `6880a79` 完全一致（30%寄存器写入前硬限幅）；差异只剩参数集中化以及停车/计时功能。现在自动提交本版，SysConfig和Keil工程的既有改动不提交。

## 250. 助手

已恢复为 `6880a79` 的控制逻辑，并集成停车功能：

- 固定基础速度 22%，最高限幅30%。
- 恢复原始八路循迹 PID：`8.0 / 0.3 / 0.12`。
- 恢复原始陀螺仪辅助 PID：`0.8 / 0.02 / 0`。
- 编码器无效时不停车，直接使用固定基础PWM。
- 移除了后来增加的软启动、直线加速、弯道分速、中心死区和循迹滤波。
- 离开起点后，再次检测至少5路黑线便立即停车，兼容中间六路。
- OLED保留行驶时间、停车启用状态和圈数显示。

所有参数位于 [empty.c](D:/TI/mspm0_sdk_2_02_00_05/电赛备用2/empty.c:45)，后面均有功能注释。停车逻辑在 [control.c](D:/TI/mspm0_sdk_2_02_00_05/电赛备用2/control.c:384)。

Keil编译：`0 Error(s), 0 Warning(s)`。未烧录。

Git提交：`ff77fc9`

::git-stage{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}
::git-commit{cwd="D:\TI\mspm0_sdk_2_02_00_05\电赛备用2"}

## 251. 用户

你现在的效果非常好，但是速度太慢了，可以加快一点，但是我不会调参，之前调的很烂，相关关联参数也需要修改，注意ff3fb67版本，起步速度又太大了，不需要添加其他参数，允许修改电机限幅，当前跑完一圈是30s，我期望的时间是20s以内，当然时间越短越好，还有注意停车的精度，你可以先达到16s左右，等我试完，然后告诉我怎么修改参数时间能更快，

## 252. 助手

我按你现在这版“跑得稳”的控制结构只调现有参数，不重新加入 `ff3fb67` 的滤波、死区、直线分速或软启动参数。

目标先按约 16 秒配置：基础PWM从22%提高到40%，但明显低于 `ff3fb67` 起步时的55%；总限幅提高到55%。同时按速度比例增强转弯输出，并略降微分，避免只是加速后弯道转不过去。停车仍保持“启停线单帧检测后立即关断”，不增加确认延迟。

## 253. 助手

这版没有增加任何新调参项，只修改现有参数。按赛道总长约6.14米估算，30秒对应平均约0.205m/s；目标16秒需要约0.384m/s，所以编码器目标设为390mm/s，编码器仍无效时则靠41%固定PWM运行。现在进行纯编译验证。

## 254. 助手

编译已通过：`0 Error(s), 0 Warning(s)`。停车精度方面，这版把阈值从“至少5路”改为你确认的“6路同时压黑线”才立即断开PWM和STBY，可减少刚接触横线时过早停车；没有增加停车确认时间，因此高速下不会因为等待而继续前冲。现在自动提交本版。

## 255. 助手

已完成约16秒目标版：

- 基础速度：22% → 41%
- 电机限幅：30% → 55%
- 编码器目标：120 → 390 mm/s
- 循迹PID：`10.0 / 0.25 / 0.10`
- 循迹输出限幅：70 → 100
- 陀螺仪辅助限幅：30 → 35
- 停车判定：必须6路同时检测黑线，随后立即关闭PWM和STBY
- 未加入 `ff3fb67` 的55%高速起步、滤波、死区和分段速度

参数位于 [empty.c](D:/TI/mspm0_sdk_2_02_00_05/电赛备用2/empty.c:47)，电机55%硬限幅位于 [moto.c](D:/TI/mspm0_sdk_2_02_00_05/电赛备用2/moto.c:71)。

如果实测稳定、还想更快，可以先只这样改：

```c
.auto_base_pwm_permille = 430, /* 预计约15秒 */
.speed_target_mm_s      = 410,
```

每次建议只增加 `20` PWM。若高速后弯道开始向外冲，再把：


